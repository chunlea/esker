# Debts still open at v1.1

**One row carries over from v1 — #4 — nine are new, one of the nine closed between being
written and being committed** (#11, and it is left in place with what closed it), **and one was
ruled rather than paid** (#16: keep the behaviour, reason ADR 0083). So the count of debts a
reader has to act on is eight, and the table below has ten rows: a register that deletes a settled
row is a register that gets the same question asked again.

> **A row written from a gate log is one landing behind by construction, and three rows proved it
> in one night.** #4 said *"needs a PD-held graph, owner pdha"* and the measurement said the lock
> was missing rather than the graph; §J11 of `phase-16-mpp.md` said the join rewrite was *left* and
> `git log -S` found it built four days earlier, by a commit whose own message recorded the
> permission that section was still asking for; #10 asked for a fix that two commits had already
> landed, citing a gate log older than both. Each cost a unit being issued — #10's to two lanes at
> once. **So: before a row is acted on, re-read it against `git log` for the file it names.** One
> command, and it is cheaper than the unit.

**Then #17 was paid and two rows were opened by paying it** (#19, #20 — both found by measuring
the shapes rather than by reading the code, which is the argument for measuring), so the count is
eleven to act on across thirteen rows (#21 and #22 arrived the same way, from the same corpus; #22
was #12's consequence until b4's int4 ladder landed and it stayed). **#21 and #22 were then paid, and measuring each
first found a live defect in the thing it was said to be missing** — #21 two, in the `= ANY` that
already existed, and #22 a `22003` on a statement a real server answers. Paying #22 also opened
three rows (#23, #24, #25), because the measurement said the one row was three things: so the count kept moving on both sides. **Where it stands: §1 has 8 rows and all 8 are open** — #46, #49, #77, #88, #101, #105, #106 and #107 — (**#81 left and #101, #105 and #106 arrived on 2026-09-16**, all four in s2-plpgsql's unit H: one paid and three opened out of its own capture, so the list moved by more than the number did — counted from the tables after the edit) (**#89's test half was paid and #107 arrived on 2026-09-16**, unit Q's census of the file's clocks: one row out and one in, so the number did not move and the list did, which is the case this sentence has got wrong before. #107 is the store half #89 could not answer — counted from the tables after the edit) (**#84 left on 2026-09-16**, paid by unit Q as ADR 0116 recommends — counted from the tables after the edit) (**#96 arrived and left on 2026-09-13**, unit L's measurement of ADR 0114's step-5 window, registered when unit M began and paid by it — counted from the tables after the edit) (**#90 and #91 left on 2026-09-13**, both paid when ADR 0114 §2 was built: #91 was #90's second half, and it was the last of #90's three parts — counted by walking the tables after the edit) (**#91 arrived the same day, split out of #90's second half** once measuring #90 found two defects behind one failure — counted from the tables after the edit) (**#58 left and #90 arrived on 2026-09-13**, both out of run 127 attempt 6: one row out and one in, so the number did not move and the list did, which is the case this sentence has got wrong before — counted from the tables after the edit) (**and it was wrong again, by four and in both directions**: it still listed #50, #51, #54, #60, #68 and #80 after they had closed and had never gained #84 or #88, which is the seventh time. Counted here by walking the tables, and the four that moved on 2026-09-12 — #60, #80, #86, #87 — each had **the test its own row names** re-run before it was moved, which is the part that makes this a count rather than a claim) (**and this sentence was wrong again, by two**: it said eleven while #77 and #80 sat in the table, which is the sixth time and the reason the instruction is to count the rows rather than to edit the number) (**and the sentence was wrong again between two readings**: it said seven while #68 had arrived from main, which is the fifth time and the reason the instruction below is to count the rows rather than to add one. #73, #75 and #76 were all opened on 2026-09-11 by the same unit — #57's last readers — and none of the three is #57's: an integer literal's width, an unknown literal's, and a pass that does not recurse. #76 is the one that cost something to find, because it only became visible when the rule above it was written: refusing on an arm's missing type turned answering statements into `42804`s, and the suite said so. Next free number: **#108** — **#101**, **#105** and **#106** are s2-plpgsql's unit H's three, registered in §1 on 2026-09-16: the recursion depth limit that covers a statement and not the values inside it, the set operators this node does not implement, and the domain an `array_agg` loses — **#107** is #89's store half, issued on 2026-09-16 when the test half was paid and the store half was neither answered nor attempted, and it sits in §1 awaiting a reproduction window; **#101** to **#106** were issued by the coordinator to other units the same day and are not in this register yet — **#99** and **#100** are the frame cut and the write no frame can carry, both issued with unit P out of #98's own design and paid by it on 2026-09-16 without ever sitting in §1 — counted from the tables after the edit — **#98** is the frame-size estimate that refused a request the transport could have sent, found by r1's run 127 attempt 7 and opened and closed on 2026-09-15 without ever sitting in §1, the way #83, #78 and #79 were — counted from the tables after the edit — **#97** is a sequence's counter written by the statement's transaction as well as by transactions of their own, found by s2-plpgsql's unit F, registered in §1 when unit G began and moved to §2 by the same unit on 2026-09-15, so the count of §1 did not move — counted from the tables after the edit — **#96** is the READ COMMITTED `FOR UPDATE` window, paid by unit M and in §2 — **#94** and **#95** are a sequence created after a rolled-back one carrying on from its numbers and a column default naming its sequence qualified whatever the search path, both found by s2-plpgsql's #93 capture, registered in §1 when unit F began and moved to §2 by the same unit on 2026-09-13, so the count of §1 did not move — counted from the tables after the edit — **#93** is a `serial` or identity column on a table with no key, registered in §1 when s2-plpgsql's next unit began and moved to §2 by the same unit on 2026-09-13 — **#92** is the constraint-naming family B5's capture found, registered in §1 when s2-plpgsql's unit D began and moved to §2 by the same unit on 2026-09-13, so the count of §1 did not move — counted from the tables after the edit — **#91** is #90's second half given a row of its own: at READ COMMITTED `FOR UPDATE`'s eager `Check` lock is validated at `start_ts`, so a row committed after `BEGIN` is `40001` for every `lock!` and not only for the duel, and fixing it is a format change that waits for a ruling. — **#90** is attempt 6's one divergence from the fake-backend baseline, two `find_or_create_by` threads racing one unique key: refused `40001` where PostgreSQL waits and answers `23505`, a refusal at the wrong isolation level rather than a wrong answer, and what ADR 0114 is written for. — **#87** is attempt 5's silent wrong answer, opened with no mechanism and with four of them ruled out, which is the useful half of a row that cannot be closed yet. — **#86** is the `joint_gate` disagreement of the same night, and it was issued as #85 by a coordinator who did not yet know #85 had been spent an hour earlier on the row below it; the register's own rule decided it, **the later committer moves**, which is the fourth time that rule has been applied this week. **#85** was opened on 2026-09-12 out of #77's own fixture, which is the third time this register has gained a row from a *test that would not go green*: the construction hung three times at one call, and the run that made it pass did so by committing one unrelated write, which is a product answer wearing a fixture's clothes. — **#83** was opened and closed on 2026-09-12 without ever sitting in §1 (and this line was wrong again across the merge that brought #82 and #84 in — the seventh time, and both branches were right about their own half: it is counted here from the tables, which is the standing instruction), the way #48, #53, #78 and #79 were: r1's attempt 5 printed a `tokio` panic into the node log, and reading what caught it turned the row from "a destructor does blocking I/O" into "the session's ending never ran on four of seven exits, and the panic that named it was caught" — the backtrace was the *symptom* and the silence was the defect. #77 is the row above; **#78** and **#79** were both opened and closed on 2026-09-11 without ever sitting in §1, the way #48 and #53 were. #78 is the hypothesis the unit was issued for: written from reading two files against each other, confirmed by a red test inside the hour, fixed in the same unit — **and then disproved as the cause of the P0 it was opened under**, by a dump of the preserved data directory. #79 is what the dump found instead, and it is the larger of the two: a scan that answered with a prefix of its range and nothing that said so.) (#57 closed 2026-09-11, the day after it was opened: its walker half went with the unit that found it and its three type-identity shapes the same day, which is what a row measured by *trying the cheap routes first* buys. **Counted from the table, not from this sentence** — which had said five while #58 and #60 sat above #46, arriving from main between one reading and the next.) and §2 holds **eighty** that are done. (#54 arrived 2026-09-10 out of #49's own acceptance test: the reads went and the clock did not, which is the cleanest way a register gets a row — the test that fails to close one row is the evidence for the next.) (#57 arrived 2026-09-11 the same way one step further out: a *diagnosis* asked for by the coordinator — wire v3's F11 — turned up five `XX000`s and a refused write on the way past, none of which F11 itself touches. Half of it was paid in the same unit because it was a walker and the other half is a type-surface row; the number was issued by the coordinator.) (**Counted after a merge whose only conflict was this sentence**: two branches each rewrote it in the same hour, one adding a row to §1 and one moving two into §2, and both were right about their own half. The numbers here are from the tables of the *merged* file, which is the only place either half was ever visible.) (#50 arrived 2026-09-10 from ADR 0104 §4's producer diagnostic: the message that had said only "a lock could not be cleared" for three different callers started saying which, and the first thing it named was the catalog — [ADR 0105](../adr/0105-a-catalog-read-never-waits.md) closes it.) (#37 and #19 both closed on 2026-09-10, as shape A of ADR 0103 and as three of ADR 0096's four rules; #19's fourth is pinned as a test rather than left in a sentence. The count in this line has been wrong about itself four times, so both numbers here are counted from the tables by a script and never read off the previous sentence — which is also the standing instruction for whoever edits it next.) (#48 was opened and closed on 2026-09-10 without ever sitting in §1: r1 measured it on a real cluster in the morning and one grep named the cause.) (#53 did the same that evening, and the count above went from forty to forty-two in one edit because the sentence had already been wrong by one when #52 moved — which is the fourth time, and the reason the standing instruction is to count the rows.) (#43, #42, #47 and #44 all moved on 2026-09-10; #42 on top of #43, #47 opened and closed the same day from r1's run-112b evidence, and #44 turned out to be seven mechanisms wearing two group names.) **Counted from the tables and not from the sentence before it**, which is the third time this line has been wrong about itself: two lanes rewrote it in one afternoon and both were arithmetic about a table neither had just read. (#34 and #40 closed together on 2026-09-09, by one experiment whose verdict rule was written before its numbers.) (#41 was written as #40 and renumbered before it landed: h1 claimed that number for the pre-vote livelock and got there first. Third time this week, and the rule holds — the later committer moves.) #38 and #39 came out of #35 the way #26 and #27 came out of #19: closing a row measures the two shapes next to it. #33 closed the day it was written, by the method note above it: the second reader was found by asking who else decides the same thing. #35 and #36 came out of groups 5 and 6 the way #26 and #27 came out of #19: the measurement that closed a type also said what the *next* thing about it would cost. (#35 was written as #34 and renumbered, h1 having claimed that number first — the register's own rule, and the second time it has been applied this week.) #26 and #27 arrived from #19's own family and were paid the same night, which is the pattern this register keeps producing: a measurement finds more than the row it was taken for, and most of what it finds is smaller than the row, with #11, #16, #17, #21 and #22 settled. A register that grows
while debts are paid is a register that was wrong about the size, which is the whole reason each
row now says how it was measured. #17 moved to §2 with what closed it, which is this file's own
rule: close a row where the row lives, in the commit that closes it. The numbering continues
[`debts-v1.md`](debts-v1.md)'s rather than restarting, because the numbers are quoted across lanes
and a register that renumbers makes every quotation of it wrong. A closed row leaves a **gap**.

**A third rule, and it is about where a defect hides rather than about this file.** Twice in one
week a bug turned out to be **two readers of one fact**, disagreeing because one of them was
written later and nobody joined them up: `parse::lower`'s constant *fold* decided which casts exist
without ever asking `pg_catalog::casts_to`, which is the gate every cast over an *expression* goes
through — so `'101'::bit(3)::int2` answered `101` where the same pair through a column was `42846`
— and g1's `BETWEEN` splitter read a bound the deparser had already re-parenthesised, which is the
same shape one layer up. Neither is visible from either reader alone; both are visible the moment
somebody asks *who else answers this question*.

**Its sibling: a fix that makes a type reachable promotes the next face of the same question.**
Making `regclass` a column type (#35) let `ARRAY['t'::regclass]` start *folding* through
`Datum::from_text`, which has no catalog — a path that had been unreachable while the type could
not be declared, and which the runtime constructor already had a seam for one arm over, written for
`regtype` for exactly this reason. So when a change makes something newly expressible, grep for the
paths that were only unreachable rather than absent. **So when a row below names a wrong answer, grep
for the second site that decides the same thing before sizing it** — three of the fixes that came
out of this register were one line once the pair was found, and none of them was findable from the
site the symptom appeared at.

A third comes from this register's own reversals, and it is a method rather than a rule about
where a row lives:

> **When a fix lives somewhere else, leave the mechanism behind as an assertion, not a comment.**
> #40's livelock is a property of `esker-raft` and its fix is in the driver that decides how many
> ticks travel together, so the core's test now asserts that batched delivery *still* livelocks it.
> A comment saying "this is why the driver does that" rots in silence; an assertion that stops
> holding tells the next person the ground moved. The same shape closed
> [ADR 0100](../adr/0100-a-region-between-leaders-waits-on-the-callers-deadline.md): its
> measurement refused the change, so the test that would have driven it prints the numbers and pins
> today's behaviour instead.

Two rules come from that file's own failures, and both are applied here rather than restated:

> **Close a row where the row lives, in the commit that closes it.** Every lag `debts-v1.md` had
> came from a closure recorded somewhere the register does not read — once in an ADR whose first
> line said it closed two rows of it.
>
> **Asking "is the site still there" is not asking "is the symptom still there."** Six of that
> file's eight rows turned out to be closed when somebody looked. So every row below was put to the
> tree or to a log before it was written, and each says which; the two that could only be taken
> from another lane's evidence say that too.
>
> **A reader nobody wrote for is invisible to a register that reads code.** Six readers of a
> stored expression had a writer that shaped the text they print; the seventh — an exclusion
> constraint's predicate — had none, so `pg_get_indexdef` on the index behind it printed whatever
> the user typed, and no row here said so because nothing in the tree looks wrong: the writer that
> is missing has no site to inspect. It took a cross product to name it. The same shape put
> [ADR 0090](../adr/0090-a-stored-expression-is-deparsed-by-the-statement-that-writes-it.md)'s own
> decision — *every reader returns those bytes unchanged* — in the odd position of being **an
> aspiration its file recorded as a fact** for as long as two of its readers were re-parenthesising
> on the way out. A decision record is checkable: walk its sentence over every caller, not over the
> ones it lists.
>
> **The direction where this node is *more permissive* than PostgreSQL had never been asked by a
> corpus.** Every capture here was taken by writing what a real server accepts and comparing the
> answers, so a row could only be opened where the node refused or answered differently — and a
> shape the node accepts and a real server rejects produces no capture at all, because nobody
> wrote the statement. The deparse census found the same blind spot one layer down, in printing:
> a reader that was measured on the shapes that happened to reach it is **wrong and green**, and
> stays that way until something asks it the cross product. Both are the same rule — *the corpus
> can only answer the question it was written to ask* — and both are cheapest to fix by writing
> the enumeration rather than the example.

[`docs/acceptance/v1.1.md`](../acceptance/v1.1.md) carries the numbers — run 107 on main
`9d642ef0`, 10,081 of 10,134 — and points back here for what is left. This file is the what
is left.

---

## 1. Open

**Only open rows live here.** A row that closes is struck and moves **whole** into §2, carrying the
note that says who closed it and how — ruled 2026-09-09, after three rows sat here struck-in-place
and a reader could no longer tell "still open" from "in this section". §1's six columns fold into
§2's three: the site, the size, the owner and the evidence go on the end of *Closed by*, because a
closed row's sizing is the useful half of a register and deleting it is how the same debt gets
re-measured a month later.

| # | Debt | Site | Size | Owner | Verified how |
|---|---|---|---|---|---|
| 106 | **`pg_typeof(array_agg(<domain>))` answers `text[]` where 19beta1 answers `d[]`.** An array *of the domain* against an array of its base. Found by s2-plpgsql's unit H as the guard on a patch that was aimed at the wrong arm, and the pair is what makes it a rule rather than a coincidence: `min` and `max` of a domain resolve to the **base** (`text`), because the aggregate is resolved to the base type's operator family, and `array_agg` of the same column **keeps** the domain — so the two cannot share an answer. The node gets the first right, since unit H fixed it, and the second wrong. **The mechanism** is that nothing carries a user-defined type out of an `array_agg`: `AggregateFunc::keeps_its_argument_type()` is `Min | Max` and nothing else — which is right, because an `array_agg` does not answer its argument's type but an *array* of it — and `branch_user_type` has no arm for the array it does answer, so the type falls back to the storage's array. | `crates/esker-sql/src/exec/query.rs` (`branch_user_type`, `pg_typeof_of`) and whatever builds an array type from an element type (`esker_keys::array::ArrayValue::array_over`). | **Small**, and it is one arm plus the array of a `TypeDef` — the same shape #81's enum work built for a scalar. | s2-plpgsql | **Measured on 19beta1 2026-09-16** (`esker-coord/s2-h81d.out`, one `BEGIN … ROLLBACK` over a domain column, its aggregates and all four branch constructs). The assertion is written and deliberately not asserted: `tests/enum_unknown_literal.rs`'s `a_domain_is_kept_where_postgresql_19_keeps_it` carries it as a comment naming both answers, so paying this row is uncommenting one line and watching it go green. |
| 105 | **`EXCEPT` and `INTERSECT` are `0A000`, and `UNION` is the only set operator this node implements.** Found by s2-plpgsql's unit H while capturing #81, whose corpus asks all four constructs of `select_common_type` the same question: 19beta1 answers `SELECT 'r', m FROM h_t WHERE id = 1 EXCEPT SELECT 'r', 'sad'` with a row and answers the `INTERSECT` beside it with a row, and this node refuses both outright. It is visible in the error text as well as in the executor — `SqlError::SetOperationTypes` hard-codes the word `UNION` because the other two never reach it (`error.rs`), and `exec/mod.rs` notes that a `Describe` of one is `0A000` rather than a shape this node would then fail to run. **The refusal also hides the rest of a session**: it aborts the transaction, so the seventeen statements after the `EXCEPT` in that corpus and the sixteen after the `INTERSECT` measured nothing until each was given a `SAVEPOINT` of its own. | `crates/esker-sql/src/exec/query.rs`'s `append`, which unifies arms and then appends them, and the lowering that reaches it (`parse/set_operation.rs`, `plan::SetOp`). | **Unknown until the plan page is written** — the coordinator's own sequencing, and the question it has to answer is how much of the existing `UNION` path an `INTERSECT` and an `EXCEPT` can reuse: the arm unification and the type settlement are the same rule, and what differs is the row combination, which `UNION ALL` does not do at all and `UNION` does by deduplication. | s2-plpgsql | **Measured on 19beta1 2026-09-16** inside `BEGIN … ROLLBACK`, and pinned as two declared divergences of `crates/esker-sql/tests/corpus/pg19_enum_unknown_literal.txt` (lines 43 and 46) so the ratchet says the day either one stops needing its declaration. |
| 101 | **The recursion depth limit covers the statement and not the values inside it.** Four bounds exist and all four are read off **statement text**: `MAX_NESTING_DEPTH` (1,000) counts the brackets a scanner sees, `MAX_BOOLEAN_CHAIN` its `OR`s, a third count its set operators, and `MAX_PLAN_DEPTH` (42 debug / 204 release) the lowered `plan::Expr`. That is a good design and its own doc says why — *a tree that cannot be deeper than this cannot overflow any of them, including the ones not written yet* — which is how forty-odd recursive walks are made safe by one number. **The hole is that the scanner never looks inside a string literal**, which is also deliberate: it is how `UNION` as a column name is counted and a comment is not. So `'[[[[…]]]]'::jsonb` is one token at depth one and every tree behind it is unbounded. A census of the crate (`scratchpad/h/census_recursion.py`): 210 self-recursive functions, 78 walking a tree the input can nest; the ones over `plan::Expr`/`Node`/`Select`/`Statement` are covered by the bounds above, and what is left is exactly the **value** parsers — `value::json`'s recursive descent and its walkers, `value::tsquery`'s and the six walks over the `Node` it builds, and `value::xml`, `value::ltree`, `value::regex`, `value::range`, none of which mentions depth at all. `value::array` threads a `depth` for dimension bookkeeping and never refuses on it: there is no `MAXDIM` here and `54000` appears nowhere in the crate. **19beta1** (`esker-coord/s2-h101.out`, `s2-h101b.out`): `jsonb` nested 10,000 answers and 100,000 is `54001 stack depth limit exceeded` with `HINT: Increase the configuration parameter "max_stack_depth" (currently 2048kB), after ensuring the platform's stack depth limit is adequate.`; `tsquery` at 10,000 is the same; `jsonpath` says `42601 memory exhausted at or near "(" of jsonpath input` in its own words; `xml` stops at libxml's `2200N … Excessive depth in document: 256`; a 7-dimension array is `54000 number of array dimensions exceeds the maximum allowed (6)`; and a recursive PL/pgSQL call answers at 500 and is `54001` at 1,000. **Beside the hole**: the one bound a statement can reach here is `MAX_DEPTH = 16` in `exec::plpgsql` (bodies inside bodies), where a real server answers 500 — contract C1 tolerates a limit but not a *low* one — and a PL/pgSQL **function** call is not a call at all, since `inline_user_function` inlines a `sql`-language zero-argument single-expression function and refuses the rest with `0A000`. | `crates/esker-sql/src/value/json.rs`, `value/tsquery.rs`, `value/xml.rs`, `value/ltree.rs`, `value/regex.rs`, `value/range.rs`, `value/array.rs`, and `exec/plpgsql.rs`'s `MAX_DEPTH`. **Census only, other lanes' crates**: `esker-txn` has 7 self-recursive functions and none over a nestable tree (a first census said 14 and was wrong — `impl … for &T` methods forwarding to `(**self)` are not recursion); `esker-store` has 46 and none over a tree; `esker-columnar` bounds the **reader** of a pushed-down filter (`fragment::codec::take_expr`, `MAX_EXPR_DEPTH`) and leaves the **writer** and the **evaluator** of the same tree unbounded (`put_expr`, `fragment::expr::evaluate`, `conjuncts`, `fragment::check`). | **Medium.** One bound, but it needs a per-level measurement the way `MAX_PLAN_DEPTH` has one, and the red test has to run in a **child process** because a stack overflow aborts the test binary — the shape `tests/lowering_depth.rs` already carries. | s2-plpgsql (unit H) | **Measured on 19beta1 2026-09-16**, two sessions inside `BEGIN … ROLLBACK` with a savepoint per refusal and a final count proving nothing was left behind; the census is a script over the four crates rather than a reading. Not yet reproduced on this node — that is the unit's first step, and invariant 9 says the answer must be a refusal and never a crash. |
| 107 | **Whether region 1 was genuinely leaderless for thirty seconds, or the client's routing kept missing a leader that was there.** This is **#89's store half**, given a row of its own on 2026-09-16 when #89's test half was paid and this half was neither answered nor attempted. The gate of `7673adac` gave up after nine attempts with *peer is not the leader of region 1*, and nothing since has said which of the two it was. On the client side the shape is **#59 face 1**, which `esker-client`'s own routing documentation already describes and already mitigates — *"a follower that has not yet noticed its leader is gone still names it … which is how a client with two live replicas spends nine attempts reaching none of them"* — while deliberately not resetting the budget on a `NotLeader` hint; on the store side it is an election that genuinely did not finish. **What #89 changed makes this row answerable and does not answer it**: `settle` and `rows` count attempts, and the three readiness waits now report the elapsed against a named bound, so the next occurrence arrives **with a diagnosis** instead of as a timeout. | whichever of `esker-client`'s routing or `esker-store`'s election is behind the leaderless window; the reproduction is `crates/esker-sql/tests/routing_differential.rs` run as a whole binary, and the harness installs the subscriber at `Gate::start_with`, so `RUST_LOG` reaches it | **Unsized until it reproduces**, which is the first piece of work and not a preliminary to it: three runs never reproduced it, so there is no log of the red, and five consecutive runs of the file on 2026-09-16 were green. A reproduction window comes before any estimate of the fix | s1-sql (awaiting a reproduction window, sequenced after #104) | **Not attempted.** The method #89's row prescribes is untried: run the whole binary under `RUST_LOG=esker_store::peer=debug` until it reproduces and read who led region 1 across the window. Issued as #107 by the coordinator on 2026-09-16 with a thirty-minute window to be given after #104 |
| 88 | **A fragment refuses a whole scan where the row path refuses one key.** #86's fix is right and blunt: a columnar copy holds no version for a key whose transaction committed its primary and left this secondary locked, and a learner cannot resolve a lock — so the fragment answers `TooFarBehind` and the planner falls back to the row path, which resolves it. The row path's own answer to the same problem is **per key**: `txnkv::user_keys_in` collects the locked key, `esker_txn::read` refuses *that* key with the lock attached, and the client resolves it and asks again. A fragment has no way to say which key, so one in-flight write anywhere in a region's share of a table sends the whole scan back to rows — on a hot table that is every scan, and the columnar path stops paying for itself exactly when it is most wanted. **The shape of the fix is known**: the answer carries the locked keys, the SQL node resolves those and re-asks, which is what the row client already does with `LockInfo`. | `crates/esker-store/src/columnar/region.rs::unresolved_lock` (the check), `crates/esker-store/src/server.rs`'s fragment service (the refusal), `crates/esker-sql/src/exec/fragment.rs::evaluate` (the caller that would resolve and retry), and `esker_proto::fragment::FragmentResp` (the field) | **Medium, and most of it is the wire — and the frequency half is now measured** (the cell to the right): one scan in 5.5 falls back when a transaction is open essentially all the time, and the fallback costs about +27% on a four-hundred-row table. `FragmentResp::Refused` carries a `RefusalReason` and a human `detail`; this needs the keys, which is a format change with a golden test behind it — so an ADR, a version bump, and every decoder of that enum gated before it lands. The store side is the check it already does, returning what it found instead of the first hit; the SQL side is the resolution the row client already has | unassigned | **The correctness half is pinned**: `joint_gate.rs::a_fragment_refuses_while_a_committed_transaction_is_still_locked` fails in 2 s without the refusal, with the copy answering four rows against the row store's five. ~~**The cost half has no measurement at all**~~ — **it has one since 2026-09-16** (below), and the rest of this sentence is what was true before it: — nobody has counted how often a fragment meets a lock under a real write workload, and that number is what says whether this is a day's work or a note. Measuring it is the first thing this debt owes **Measured 2026-09-16 by s1-sql's unit Q** (`crates/esker-sql/tests/routing_differential.rs::what_a_fragment_meeting_a_lock_costs`, `#[ignore]`d, the file's own three-store cluster, the fragment source wrapped in a counter so refusals are tallied where they happen rather than read out of `EXPLAIN`). A **hot** table with a writer beside it and a **cold** table without one, 120 scans each: **hot 22 of 120 met a lock — one in 5.5 — and cold 0 of 120**, with a transaction open for 10.46 s of the hot table's 10.42 s scanning window, i.e. **near-total lock occupancy**, which makes this close to a worst case rather than an average. The fallback's cost there is **86.6 ms against 68.2 ms** for a clean scan of the same table (+27%); a clean *cold* scan is 10.9 ms, so most of the hot number is the write load and not the fallback. **What this does not measure is the cost that matters most**: both tables hold four hundred rows, so falling back to the row path is cheap by construction — on a table where the columnar copy earns its keep the fallback is the whole debt, and that number still has no measurement. The first attempt measured the writer instead of the system (an autocommit writer leaves a lock for microseconds; 1 encounter in 60), which is why the writer now holds its transaction open and the test records how much of the window a transaction was open at all. |
| 77 | **Nothing tests that the snapshot path closes the columnar copy.** #74 (a) landed the fix — `Store::fetch_snapshot` closes the region's `ColumnarSlot` where it adopts, because a snapshot writes committed versions straight into the column families with no entry to apply, so the copy that survives the transfer is short everything the snapshot brought. `ColumnarSlot::saw` already caught that gap, and its own documentation names the limit it has: *"the first entry applied after the transfer is the gap, and this is where it is caught"* — **a learner that takes a snapshot and then goes quiet has no next entry**, which is why the `joint_gate` disagreement only appeared under load. What is pinned is the repair, at the unit: `columnar_region::a_copy_opened_before_the_rest_of_the_history_arrived_still_answers_for_it` shows an open copy missing a row that reached the engine without the tee, and shows `close()` repairing it. What is **not** pinned is that `fetch_snapshot` calls it: back the call out and every test still passes. This is the *every caller is a test* shape — a mechanism built, wired and tested, with nothing asserting that production invokes it. | `crates/esker-store/src/server.rs`'s `fetch_snapshot`, and `crates/esker-store/tests/snapshot.rs`, which is where the test belongs | **Small, and blocked on a fixture rather than on the idea.** The test is "a columnar learner whose copy is **already open** falls behind, the leader compacts past it, a snapshot repairs it, and the fragment answers for what the snapshot brought". `snapshot.rs`'s `open()` builds each store on a `tempfile::tempdir()` it owns and `Node::stop(self)` consumes the node, so there is no way to stop a store and reopen it on the same directory — which is what "falls behind" needs. Either that fixture gains a reopen (`tests/stop_and_reopen.rs` has the shape) or the fall-behind is produced another way | unassigned | The near miss is `snapshot.rs::a_columnar_learner_caught_up_by_a_snapshot_answers_for_what_it_brought`, which covers the **other order** — a snapshot before any copy exists — and passes, with a denominator asserting the leader's log really was truncated past the history first  · **h2 attempted it 2026-09-11/12 and got two thirds of the way; the fixture is now named more precisely than the row did.** (1) **The helper does not transfer.** `announce_a_snapshot_and_await_the_retire` is written for a learner `AddPeer` has promoted to a **voter**: it moves the office back and writes twenty filler keys a round through `put(&[&first.store, &second.store], …)`. A columnar learner is never a voter, and `put` to a store that cannot accept a proposal re-asks for **thirty seconds a key** — one run reached 600 s with no end, which is what made it look like a deadlock rather than a wrong helper. So: filler goes to the leader only, and the announcement is inlined. (2) **The race is unnecessary — there is a construction.** `Operator::RemovePeer` stops the learner's **peer** while `ColumnarSlot` is keyed by region on the *store*, so the copy survives the removal: open the copy over four rows with a fragment, remove the peer, commit the fifth row and truncate the log with nothing applying on that store, then place the learner again — a truncated log leaves a snapshot as the only road, and the copy that meets it is the one holding four. That removes the only race the first attempt had. (3) **The hang is named, by one run: it is the *first* `fragment_ids`, and it is a wait with no end rather than a slow one.** Everything before it costs **0.56 s** — the leader, the history, the second store, the placement, the catch-up — and then nothing. The two earlier rounds were killed by a `timeout` at 300 s and 200 s and narrowed nothing because the process was shot; wrapping the construction's four unbounded awaits in `snapshot.rs`'s own `within` turned the same wait into a **panic with a message at 60 s**, which is the whole difference (`trace()` had printed nothing because it is `EnvFilter::from_default_env` and `RUST_LOG` was unset — the test was running the whole time). The wait is `fragment_ids` → `Store::serve_fragment` → `Store::catch_up` → **`peer.read_index_as_learner().await`**, a oneshot that resolves only when the core answers the ReadIndex and errors only when the peer stops; it is the only unbounded await on the path, because `catch_up` *refuses* on `min_apply_index` rather than waiting for it. **And the likeliest cause is the fixture's own argument, not the product** — recorded as a hypothesis because the one run allowed was spent naming the line. The green twin `a_placed_columnar_learner_answers_for_the_rows_that_predate_it` makes the same call on the same two-store topology and differs in exactly one argument: it passes `LogCompaction::new()` (`threshold 4096, keep 1024`) where this construction passes `{ threshold: 8, keep: 2 }`, **512× more aggressive and in force from the first entry rather than from the point a truncation is wanted**. `commit_the_history` is five transactions of two entries each, so by the placement the leader's log holds its last two and a replay is impossible — the learner can only arrive by snapshot, which is precisely what step 1 must not be. The guard did not catch it either: `wait_for("the learner to catch up by replay", \|\| peer.applied_index() >= caught_up)` was satisfied in about **ten milliseconds**, so it names a mechanism it does not prove. **The next step is one change and one run**: default compaction for steps 1–3 with the truncation at step 4 produced by writing past the default threshold, and a replay wait that proves a replay (`truncated_index` below what the learner needed, read the way step 4 already reads it) so that a learner arriving by snapshot fails the step instead of passing it. **And the second run says the compaction was not it either — the learner never replays at all.** With the default compaction, the leader's `truncated_index` is **0** at the placement (asserted, and it passed), so every entry is still there and a replay is available — and the learner still arrives with its **own** `truncated_index` at 12, which is a snapshot. So *“catches up by replay” was never true in this construction under either setting*; a newly placed peer is seeded from a snapshot of the region and receives only the entries after it. **Which means step 1 does not need a replay and never did**: what it needs is that the copy is open over *exactly four rows* before anything else is snapshotted, and the line after the guard already asserts that (`opened == vec![1, 2, 3, 4]`) — `ColumnarSlot::ensure` walks the `write` column family, which a snapshot populates, which is why the green twin answers for the four rows that predate it. The guard asserts more than the step needs and is what should go. **Still unknown: why the first `fragment_ids` hung under `{ threshold: 8, keep: 2 }`** — run 2 failed at the guard and never reached that line, so run 1's hang is neither reproduced nor explained. One fact found by reading, for whoever takes it: `PeerCore::hold_for_lagging_peers` holds a compaction back for a lagging peer, but a region whose membership is still **one** peer takes the *“a region of one has nobody to feed but itself”* branch and compacts by the tail rule alone — so under that setting the leader had already truncated to `applied - 2 = 10` **before** the learner was placed, and that is the state the hang happened in (`SLOW_PEER_LOG_ALLOWANCE` is 8,192, so once the learner *is* in the membership the hold is generous). **The next attempt is one line**: delete the replay guard, keep `opened == vec![1, 2, 3, 4]` as step 1's proof, and see whether the first `fragment_ids` still hangs with the default compaction — that single run answers run 1's question. Two runs were spent because the first “one change” was really two (the compaction and the guard) and the guard fired first, leaving the compaction change untested. **And the third run settles it: the compaction was not the cause.** With the guard gone and `LogCompaction::new()`, the first `fragment_ids` hangs exactly as in run 1 — 60 s, same call, same message — so run 1's hypothesis is **disproved**: `{ threshold: 8, keep: 2 }` was a real fixture bug (it denies the learner a replay and compacts before the placement) and is not what makes the fragment wait. **What the three runs leave is one structural difference from the green twin**, and everything else about the two is now identical: `a_placed_columnar_learner_answers_for_the_rows_that_predate_it` places the learner, **commits row 5**, takes the leader's applied index and waits for it, and only then asks — where this construction asks **immediately after the placement**, with nothing committed since the conf change that placed the learner. So the candidate is *asking a columnar learner for a fragment with no entry committed after its placement*: the ReadIndex round inside `Store::catch_up` is what does not come back, and the twin always has a post-placement append behind it. The product reading is the more interesting one — a freshly placed columnar learner asked before anything else commits would wait for ever in production too. **And the fourth run answered it: the hang is gone, and the answer is the product.** One unrelated raw `put` between the placement and the first `fragment_ids` — a `put` and not a row, because a sixth table row would open the copy over five and take the fixture away — and the fragment answers `[1, 2, 3, 4]` at once. So **step 1 is done**: the copy opens over exactly four rows, with the default compaction and a learner that arrives by snapshot. **The fact to carry forward is that step 1 needs one commit after the placement before the copy can be opened at all** — for the fixture that is an ordering requirement and one line; the ordering requirement itself is now **#85**, because a SQL node has no reason to commit something before asking and inherits a wait with no end. **The next wall is step 2**: `Operator::RemovePeer` issued at the epoch read immediately before it, and twenty seconds later the learner still hosts region 1 — a bounded failure with a message, and the first time the construction has reached that line at all. Full timeline in `esker-coord/h2-77-bisect.txt`; the instrumented construction, which is what should be picked up, is `esker-coord/h2-77-construction-instrumented.rs.txt`. The whole test is kept verbatim in `esker-coord/h2-77-construction.rs.txt` — it is **not** in the tree, because a test that hangs for five minutes is a landmine for anyone running `--run-ignored all`. The counterfactual, when it runs, is removing `self.columnar_slot(region_id).close();` from `server.rs` |
| 46 | **An index on a `jsonb` column**, which a real server builds and this node refuses with `0A000 an index on a column of type jsonb`. Every other row of the no-equality census is closed; this one is left because it is not the same kind of work. A comparison only has to decide *which* of two values is larger, and `crate::value::json::compare` does — that is what made ordering a `jsonb` column answer (`SortKey::ty`). An **index** key has to carry that order in its *bytes*, memcomparable, because the engine compares keys without ever seeing a type. So the encoding must put the kind first — `[] < null < string < number < boolean < array < object`, measured — then, inside a kind, a `numeric` ordering for numbers (`1.0` and `1.00` are one value and `2 < 10`), a length before contents for arrays, and a pair count before keys for objects. That is a **new on-disk key format for a type this node already stores**, so by the constitution it is the user's call, not a lane's: existing `jsonb` columns have no index to rebuild today, which makes now the cheap moment and a later one expensive. **The empty array is the trap**: it sorts below every scalar and `[1]` does not, so a kind-then-value encoding is wrong on it — the same cell that a model of the ordering was refuted by. Measured in `tests/captures/pg19_jsonb_order.txt`, 53 cells, and the census row is line 108 of `tests/corpus/pg19_no_equality_types.txt`. **Pushed to v1.2 by the user, 2026-09-10** — it is an encoding change and the moment to make one is when the format is being turned anyway, not at a tag. Owner: unassigned. *(This row is one cell where the table has six, which is why it renders as a run-on: it was written that way and splitting it means deciding where its site, size, owner and evidence begin, which belongs to whoever takes it. Noted here rather than guessed.)* |
| 49 | **Catalog *introspection* takes half a second a statement on the real topology, and it is slow somewhere nothing measures.** Three from run 113: `pg_index ⋈ pg_attribute` **790 ms**, `obj_description` **546 ms**, `pg_inherits` **793 ms**. These are the statements a client sends to find out what a table looks like — `ActiveRecord`'s schema reflection is nothing else — so a suite pays them once per model and a `rake db:migrate` pays them per step. **It is not the catalog's version read**: [ADR 0102](../adr/0102-the-catalogs-read-path.md) closed on 2026-09-10 with that at **0.4%** of a statement's latency, ceiling, measured on the same run. Whatever these half-seconds are, they are outside the read path that instrument watches. | **The instrument exists now** (2026-09-10): `esker_sql::stmt_stats` counts a statement's point reads and range scans at the store boundary, `esker_client::stmt_stats` counts its wire round trips and the distinct regions they addressed at `Router::call` — the one place that knows both — and the executor's `statement_guards` is the boundary. Off unless `ESKER_STMT_STATS` is set, ten-second summary plus a line per statement over `ESKER_STMT_STATS_MS` (default 100). **And the first numbers are in**, `ActiveRecord`'s primary-key reflection verbatim against a `Gate` — real stores, real placement driver: **23 point reads, 12 range scans, 35 round trips, 1 region**, 17.2 ms. **35 = 23 + 12**: every KV read becomes exactly one wire call, nothing is batched and nothing is answered from the client's buffer. One region, which is ADR 0102's context — the catalog is on the path of every statement. The **counts** are what transfers to run 113's topology; the 17.2 ms does not, because this Gate's stores are in one process. What is still unknown is the site: which of the 35 is avoidable, and whether the 12 scans are one per row of a join's outer side. The candidates are the ones no instrument here can separate today: how many **KV reads** one introspection statement makes, how many **RPC round trips** those become, and which **regions** they land on — a join over two catalog views could be a scan per row of the outer side, which would make the cost a product rather than a sum. `crates/esker-sql/tests/routing_differential.rs`'s Gate measurement is the nearest thing that exists and it counts *views*, not reads. | **Measurement first, and it is the whole of the next milestone's entry.** No fix is proposed here and none should be until the three numbers above exist per statement: a fix chosen now would be chosen from the shape of the SQL rather than from where the time goes, which is what ADR 0102 was written to avoid and what its own numbers then showed was right. The instrument is the unit; the fix is whatever it names. **The design is written and nothing is built: [ADR 0106](../adr/0106-what-a-statement-reads-below-the-sql.md), proposed 2026-09-10, three shapes with a recommendation that waits on the user.** What moved the argument was making the instrument say *which key* each read touched — a count cannot tell a cache from a batch — and the census ([`docs/bench/statement-reads.md`](../bench/statement-reads.md)) is: `pk_and_sequence_for` **60 reads** (37 point, 23 range), an ordinary `SELECT … WHERE id = $1` **9**, an `INSERT … RETURNING` **8**, and `BEGIN`/`SAVEPOINT`/`RELEASE`/`COMMIT` **0**. **Eight of the point select's nine reads are catalog and one is the row**, which is where the three quarters run 114 could not price actually is: the 907 ordinary statements are not cheap ones, they are ones that pay eight catalog reads for one row read. Inside the sixty: one key (`schema(t1,"esker")`) read **sixteen times** at one snapshot; one hydration bundle repeated **five times**, once per relation in the `FROM` list, four of them exact repeats; and **fifteen of the twenty-three scans tenant-wide**, so O(relations), twelve of those fifteen exact repeats. Every catalog read in the census is cacheable by the version counter except the two version keys themselves, and every one shares the `'m' ++ "sql"` prefix — though one prefix is not one region, since the placement driver may split anywhere. **Transaction control's zero is written as "nothing to save below this door" and not as "free"**: a commit's prewrite and commit are round trips this instrument does not count. **Option (b) is chosen and built, 2026-09-10** (ADR 0106 accepted, `docs/plans/debt-49-catalog-cache.md`): the cache gained six maps and, the one the plan's file list did not name, the whole `pg_relations::Relations` bundle — caching the parts leaves the scan of the name records, and the scan is the term that grows. A repeated introspection now reads **4** keys where it read **60**, cold **12**; a point `SELECT` 9 → 5; `ALTER … DISABLE TRIGGER ALL` 17 → 10; `DROP TABLE` 29 → 22. Four is the floor this ADR left standing — two catalog views, two version counters each, which is #50's key. **What this row now waits on is r1's pricing on the real topology**: 2.38 s at 35 round trips should be well under 0.3 s at 4, and that number closes it, not the in-process one. **And the wall-clock half of the acceptance became [#54](#1-open)**, a planner defect the ADR predicted and none of its three shapes touches. | **b4** for the instrument, ADR 0106 and option (b) (all three done); **r1** for the closing number | **measured on the real topology by r1, run 113**, three statements timed individually rather than as a suite average, which is what makes them a family rather than a mean. `esker-rails-harness/results/run-113.md`. The negative result beside them is what makes the row worth opening: ADR 0102's instrument was on for the same run and accounts for 2.2 ms of the 506 ms, so the 0.5–0.8 s is not a slower version of a known cost — it is an unmeasured one. |


## 2. Closed since v1, and recorded here because the register is where a reader looks

| # | Debt | Closed by |
|---|---|---|
| 81 | ~~**An unknown literal beside an enum does not take the enum, and the arms are refused instead.**~~ #75 gave every other unknown arm the rule PostgreSQL has — it takes the type of the arms that *have* one and is then read as it — and stopped at the enum: `SELECT m FROM t UNION SELECT 'sad'` is two rows, `sad` then `ok`, on 19beta1 and a `42804` here, and `SELECT m FROM t UNION SELECT 'nope'` is `22P02 invalid input value for enum mood: "nope"` there, which is the same rule's failure case. The row read the mechanism as **a missing view, not a missing rule**: reading `'sad'` as a `mood` means looking a label up in the catalog, and `exec::query::append` has none. **Measuring it before fixing it found the row was the smaller half.** `select_common_type` is one rule over four constructs — a set operation's arms, a `CASE`'s results, a `COALESCE`'s arguments and `GREATEST`/`LEAST` — and **none of the three expression constructs carried an enum at all**, so the identity was lost with the *same* enum on both sides: `CASE WHEN true THEN m ELSE m END`, `COALESCE(m, m)` and `GREATEST(m, m)` each answered `2`, the ordinal, under `pg_typeof` `smallint`, where 19beta1 answers `ok` and `h_mood`. The unknown branch beside them was read as the **storage** — `22P02 invalid input syntax for type smallint: "sad"` — which is the one thing a client must never be told about an enum (ADR 0050), and the refusals named that `smallint` too. | **Closed by s2-plpgsql's unit H, 2026-09-16.** The fix is that **the identity settles before the storage does, everywhere the question is asked**. The branches settle a user-defined type the way a set operation's arms already did (`unify_user_type`, now told which construct is asking so the refusals say `CASE/WHEN`, `COALESCE`, `GREATEST` or `LEAST`); an `unknown` among them is read as a label of it (`give_branch_enum`, `22P02 invalid input value for enum …` when it is none); and the column a client is told, through the `RowDescription` and through `pg_typeof`, is the enum (`branch_user_type`). **There were three readers of `select_common_type` and teaching one was not enough**: the resolution settled the enum and `expr_type`'s own reader then refused the same pair in the storage's words, so `CASE WHEN true THEN m ELSE 'sad'::text END` stayed `42804 CASE types text and smallint` after the enum had been named — 19beta1 says `text and h_mood`. Both ask the identity first now, which is what keeps one pair from getting two sentences. **The size cell's question is answered: neither route was needed.** `append` did not want a catalog view and the planner did not have to be re-shaped — the identity is the `TypeDef` the *other arm* already carries, and the literal's own text now travels beside it (`OutputColumn::unknown_text`), so the label is looked up where the enum is known and the ordinal is projected in place of the arm's column. It came to about 220 lines of one file, which is **Small**, and the pricing was the expensive part rather than the change. One thing outside the row's subject moved with it: `min` and `max` stop handing back a **domain** — measured, a column of an `information_schema` domain is `information_schema.sql_identifier` and `pg_typeof(min(…))` of it is `text`, because the aggregate resolves to the base type's operator family while an enum has no base to fall to. The full suite caught that where the targeted tests could not, and the first patch for it was aimed at the branch constructs on a guess that the oracle refuted (a `CASE` over one domain *keeps* it), so the guess was reverted before it landed. No format change. Site `crates/esker-sql/src/exec/query.rs` (`append`, `unknown_arms_as_labels`, `coerce_arm`, `resolve_case`, `resolve_coalesce`, the `GREATEST`/`LEAST` arm of `resolve`, `branch_result_type`, `branch_user_type`, `output_columns`, `pg_typeof_of`) and `crates/esker-sql/src/error.rs`; size Small; owner s2-plpgsql, which the row had as *unassigned*. Evidence: `tests/corpus/pg19_enum_unknown_literal.txt`, 87 statements over three sessions with every refusal in its own savepoint, replayed by `tests/enum_unknown_literal.rs` whose focused tests pin one construct each; the two rows `tests/set_operation_enum.rs` declared for this — an unknown arm and a bare `NULL` arm beside an enum — are row assertions now, which is what says the day it closed. The capture also found what it was not looking for, and neither is this row's: **`EXCEPT` and `INTERSECT` are `0A000`** (`UNION` is the only set operator implemented), declared as divergences of that corpus, and `pg_typeof(array_agg(<domain>))` is `text[]` where 19beta1 says `d[]`, recorded beside the test that found it. |
| 89 | ~~**`routing_differential` is not stable under its own parallel run on a loaded box, and one face of it is a thirty-second wall clock.**~~ The gate of `7673adac` lost `a_pinned_snapshot_does_not_move_when_later_rows_commit` with *``INSERT INTO t VALUES (1053,…)`` never settled: could not reach the store: gave up after 9 attempts: peer is not the leader of region 1*. **What that message means is bigger than one attempt**: the test's `settle` retried for a **thirty-second** wall clock and the client's `Router` gives up after **nine** attempts, so the statement needed ≈ 30 continuous seconds in which no leader of region 1 was reachable. **Not reproduced in three runs** — two solo, green at 36.6 s and 61.2 s, and one of the whole binary where the named test was cut off by the deadline. What the third run *did* show is the row's real subject: the same binary's tests ranged from **4.2 s to 252 s** at load 3.6–6.3, and it failed **two tests under its own parallelism both before and after** any change — different tests each time (`a_join_over_columnar_tables_answers_what_the_row_engine_answers` at 252 s with *the columnar learner never answered a fragment over f*, then `a_join_the_rewrite_cannot_express_stays_on_the_rows` at 194 s where the run before had passed it in 4.26 s). So this is not one flaky test. | **The test half is paid, 2026-09-16 by s1-sql's unit Q; the store half is now [#107](#1-open) and is not attempted here.** A census of every clock in the file found no speed assertion left — the `GIVE_UP_AFTER` bounds sit inside `#[ignore]`d measurements and are recorded rather than asserted, `timed_engine`'s medians are printed, and the rest are config ticks, backoff sleeps or elapsed printed into a diagnosis. What it did find is three **readiness waits** that assert their bound and then say nothing about it; the rule for that shape is that the bound stays — removing it turns a false red into a hang — and that the message reports the elapsed, so a reader can tell "the box was slow" from "this never happens". All three now do, and the bare 120-second literal became `ROUTED_WITHIN` beside the `PLACEMENT_WITHIN` that already had a name. **What is still unanswered is the row's own second half**: whether region 1 was genuinely leaderless for thirty seconds (store liveness) or the client's routing kept missing a leader that was there (#59 face 1's shape). Three runs never reproduced it, so there is no log of the red; the method the row prescribes — the whole binary under `RUST_LOG=esker_store::peer=debug` until it reproduces — is untried, and it is **#107**, owned by s1-sql and waiting on a reproduction window, rather than a half riding a closed row. *Verified in:* **`ceb55919`** — a commit whose message describes only #88's lint fix, because both units were live in one file and it was staged whole; the hunk-by-hunk attribution is `esker-coord/s1-unit-q/q89-ceb55919-ranges.md`. *Evidence:* five consecutive runs of the file in the container on 2026-09-16, 02:21:20-02:27:56, **5/5 green** (11 tests each, 12 skipped; `q89-run-1.log` … `q89-run-5.log`, `q89-runs.steps` and the series' own marker in `esker-coord/s1-unit-q/`). The rounds took 39.2 s, 50.9 s, 59.9 s, 103.8 s and 138.3 s — **a 3.5× spread over one tree on one machine** — and nearly all of it is one test: `a_split_child_is_led_by_the_store_that_led_its_parent` took 86.7 s and 120.2 s in the two slow rounds against a next-slowest of 10.4 s, and was marked SLOW in three of the five. So the file is stable in **outcome** under its own parallel run and not in **duration**, and the test whose duration moves is the one about which store leads a split child — the neighbourhood #107 is about, recorded as an observation and not as a reproduction. *Site:* `crates/esker-sql/tests/routing_differential.rs` (the `Gate` harness and its helpers), and whichever of `esker-client`'s routing or `esker-store`'s election is behind the leaderless window · *Size:* **The test half is small and done; the store half is unsized and unstarted.** · *Owner:* s1-sql (the test half) · *Measured before:* **Paid in part.** `settle` and `rows` count **attempts** instead of seconds — the same change #68 made to `statement_across_a_leader_kill`, and what the lane rule against wall-clock assertions in a gate test says in general: thirty seconds is a property of the box and not of the statement. Forty attempts with a rising capped backoff, the elapsed in the message, and the last answer named, so a genuine liveness hole still fails **with a diagnosis** rather than as a timeout. **What is NOT answered, and must not be read as answered**: whether region 1 genuinely had no leader for thirty seconds (store liveness) or the client's routing kept missing one that was there (#59 face 1's shape). Three runs did not reproduce it, so there is no log of the red — and the router's own documentation already describes the near miss and already mitigates it (`without_the_corpse`: *“a follower that has not yet noticed its leader is gone still names it … which is how a client with two live replicas spends nine attempts reaching none of them”*), while deliberately **not** resetting the budget on a `NotLeader` hint. **To answer it**: run the whole binary under `RUST_LOG=esker_store::peer=debug` until it reproduces, and read who led region 1 across the window — the harness installs the subscriber at `Gate::start_with` so `RUST_LOG` reaches it |
| 84 | ~~**A timestamp oracle with no physical half makes an orphaned lock immortal, and the single-node `esker-sql` binary ships one.**~~ A lock left behind by a prewrite whose commit never landed — a store killed in the window, a client that died — has exactly one way out that does not need its owner: its lease running out. `esker_txn::is_expired` compares that lease in the timestamp's **physical** half (`physical_ms(now) > physical_ms(start) + ttl_ms`, `physical_ms(ts) = ts >> 18`), and `esker_client::CountingOracle` hands out consecutive integers from a small start — so every timestamp it will ever mint reads as physical millisecond **zero**, `is_expired` is false for ever, `Transaction::classify` answers `Alive` about the orphan for ever, and every later statement touching that key spends the client's resolution budget and answers `40001 … a lock from the transaction at N could not be cleared for a read`. **Restarting the SQL node does not help** — the counter starts over, still at physical zero. Found from the other end: it cost `statement_across_a_leader_kill` a night of red gates (120 attempts, 632 s, the same message every time) before the arithmetic was read. **The comment at every site argues about *ordering* and is silent about *leases***, which is how the same omission reached three of them: the binary's "a local counter is a correct oracle for exactly one node", the fixture's module header, and `MemoryBackend`'s frozen clock before it was repaired (that one is already repaired — `backend::Versions`, whose mark is *"the greater of the last timestamp handed out and the wall clock"* — cited by name because those line numbers are `read_set_covers` today — and is cited here as proof the class gets built, not as an open instance). | **Paid 2026-09-16 by s1-sql's unit Q**, as [ADR 0116](../adr/0116-a-single-node-oracle-needs-a-physical-half.md) recommends — and the fix was already in the repository twice. `joint_gate.rs` and `routing_differential.rs` each carried a private `WallClockOracle` with the same body, because each test strands a lock and a counter's physical half is zero for ever; unit Q promoted it to `esker_client::WallClockOracle`, pointed the binary's no-`--pd` arm at it, and let both tests take the promoted one, so the rule has one copy again. **No format change and no wire change**: a timestamp is a `u64` wherever it is written or sent, and only which numbers a local oracle mints changes. The arm's comment, which argued about ordering and said nothing about leases — the omission this row says reached three places — now says both. *Site:* `crates/esker-sql/src/bin/esker-sql.rs:414-423` (the no-`--pd` arm, the production one); `esker-txn/src/lib.rs:82` and `esker-client/src/txn.rs:2082,2024` for the mechanism; the fixtures still pairing a counting oracle with a path that can orphan a lock are `crates/esker-sql/tests/cluster/mod.rs` and `crates/esker-sql/tests/multi_region_rows.rs` (this row used to name `routing_differential.rs` and `joint_gate.rs`, which is backwards: those two each held a private wall-clock oracle, and unit Q promoted it out of them) · *Size:* **Medium, and the sizing is the choice between two fixes.** Either lock resolution gets a **second path** for the case a rollback could not reach — the owner's fate is already decided atomically by `settle_primary`, so the question is what may consult it when the lease says "alive" and the oracle's clock is known not to move — or the oracle side is closed instead: refuse to build a client whose oracle has no physical clock on any path that can orphan a lock, which is small but only moves the failure to start-up. Deciding which is the unit; neither is more than a day · *Owner:* s1-sql · *Evidence:* `crates/esker-client/tests/lock_expiry.rs`, against a real store over a real socket because a lease is settled by a round trip: `a_stranded_lock_is_resolved_once_its_lease_runs_out` (an eager lock whose transaction is dropped — `Drop` only forgets the renewal — and the key comes back after the lease), `a_counter_never_lets_a_stranded_lock_expire` (the same with a counter, and it never does), and `a_wall_clock_oracle_mints_a_physical_half_and_never_goes_backwards`. Three of three green; `joint_gate` and `routing_differential` each run once after taking the promoted oracle, both green; `esker-client` all features green; the gate's clippy green. **Counterfactual** (`q84-cf.py`, an asserted replace undone by its inverse): degrade the promoted oracle to a counter and 2 of the 3 go red — the two that need a physical half — while the counter case stays green because it never used the promoted one; `joint_gate` loses 2 of 8, including `a_lock_the_ttl_kills_resolves_the_same_way_on_both_engines`, and `esker-client`'s own suite goes red. `routing_differential`'s default subset stays green: the cases that strand a lock there are `#[ignore]`d · *Measured before:* **Arithmetic, not a run** — `physical_ms(1) == 0` and `is_expired(1, LOCK_TTL_MS, anything a counter will mint) == false`, pinned by `statement_across_a_leader_kill.rs::a_lock_this_fixture_mints_can_run_out_of_lease`, which asserts both halves and goes red if the counting oracle ever grows a clock. The production instance has **no test** — nothing starts `esker-sql` without `--pd`, kills a store mid-prewrite and reads the key again, and that test is the first thing this debt owes |
| 100 | ~~**One write larger than a frame was refused without saying which key it was.**~~ No cut of a batch can carry a single mutation wider than the transport's frame, so the refusal is right — but a transaction of a quarter of a million keys cannot be searched for the one that is too big. Issued with unit P. | **Paid 2026-09-16 by s1-sql's unit P.** `Error::KeyTooLarge { key, bytes, limit }`, raised by the same cut that decides the chunks, so the key it names is the one whose own mutation would not fit; nothing is sent, and `changed_nothing()` says so. The ceiling is real and stated where the error is defined: PostgreSQL's own is a gigabyte a field, this transport's is one frame. *Site:* `crates/esker-client/src/{error,txn}.rs` · *Size:* small, as sized · *Owner:* s1-sql · *Evidence:* `crates/esker-client/tests/txn.rs::a_write_no_frame_can_carry_is_refused_by_name` — red before the fix with a message that named no key, green after, with no call made. |
| 99 | ~~**A batch the transport could not carry in one frame was refused rather than sent in several.**~~ #98 made the size the encoding's own, and left the answer binary: fit, or `XX000`. A 65,536-row fixture load is a legal statement — PostgreSQL has no user-visible frame ceiling — and one over 16 MiB still failed. Issued by the coordinator with unit P from s1-sql's design of 2026-09-15. | **Paid 2026-09-16 by s1-sql's unit P.** The cut is by bytes and it happens where the request is built, so it sizes the objects that will be sent (`TxnMutation::encoded_len`, `bytes_len`) rather than a second model of them: `Transaction::in_frames` for the key lists (`Commit`, `Rollback`, `ReleaseLock`) and `runs_that_fit` over the built mutations for `Prewrite`, which now sends one request per frame and keeps its status alignment inside each. `grouped` is untouched — it still cuts by region and re-cuts on stale routing, and the byte cut sits inside the send it calls, so a re-cut group is re-cut by bytes too. **No wire change and no isolation change**: Percolator already sent a transaction's keys one group per region and hangs its atomicity on the primary's commit record, so more frames are the same per-key work. `ResolveLock` needs no cut: its key list comes from one prewrite's answer, which is now at most one frame's worth. *Site:* `crates/esker-client/src/txn.rs`, `crates/esker-proto/src/{codec,txn}.rs` (the two length functions the client needs) · *Size:* small, as sized · *Owner:* s1-sql · *Evidence:* `crates/esker-client/tests/txn.rs::a_prewrite_too_large_for_one_frame_is_sent_as_several` — 400 rows under a 12,000-byte limit go out as three prewrites or more, every frame inside the limit and every key carried exactly once; `::a_batch_that_fits_one_frame_is_still_one_request` — the same 400 rows at the real limit still cost two prewrites and two commits, so the cut buys no round trips it does not owe; `::a_chunk_that_fails_takes_the_commit_with_it` — a failed chunk fails the commit and the primary is given back, which is the path a refused *group* always took. Measured on three real stores in `crates/esker-sql/tests/frame_cut_cost.rs` (`#[ignore]`, lock_cost's idiom): 45,000 rows go out as **three** prewrites where 4,000 rows take two, so the cut costs one extra prewrite for a statement that was refused outright before, and the per-row cost does not move — 90,014 round trips for 45,000 rows against 8,008 for 4,000, 2.00 a row either way, which is the read-before-write the insert does and not the batch. The mutation counts also settle by measurement what had been read from the code: 180,000 for 45,000 rows is **four stamped mutations a row** — the row key and three `t.references` index entries. |
| 98 | ~~**A request the transport could have sent was refused by an estimate of its size.**~~ `txn_payload_size` charged a flat six bytes a field and, since ADR 0114 §2 gave a mutation a read timestamp, six more for a varint the encoder had been writing all along — so the estimate stood six bytes above every **stamped** mutation, which at READ COMMITTED is every write a statement makes (ADR 0057 §4). Found by r1's run 127 attempt 7 (2026-09-15), `associations/eager_test.rb`'s two `EagerLoadingTooManyIdsTest` tests: the `citations` fixture is 65,536 rows over a row key and three `t.references` index entries — **262,144 stamped mutations** — and its load was refused inside `esker-sql`, before a frame went out, as `XX000 request of about 17334584 bytes exceeds the 16777216-byte frame limit`. *Measured on both builds* (`esker-coord/s1-unit-o/o-probe-*.log`): the same prewrite encodes to the **same** bytes at 98958255 and at 7193e5d9 — 4,259,886 for 65,536 stamped `Put`s — while the estimate of it moves by exactly 6 × 65,536 = 393,216. How many mutations a row writes is read from the code rather than measured: `write_row` puts the row key and one entry per maintained index, and `citations` is three `t.references`, so four — 1,572,864 bytes of estimate, and attempt 6's number for the same request was 15,761,720, under the limit. Any count of two or more explains that; what is measured is that **the request never grew and the estimate did**. | **Paid 2026-09-15 by s1-sql's unit O.** A request is measured by what it encodes to: `encoded_len()` beside every `encode()` in `esker-proto` — `TxnKvReq`, `TxnMutation`, `RawKvReq`, `RequestHeader`, `Epoch`, and the length helpers the codec's own rules define — with the client asking it (`payload_size`, `txn_payload_size`) instead of estimating, and `Router::call` adding `MAX_REQUEST_ENVELOPE` for the framing it had been ignoring rather than slack for the payload it had been counting twice. The refusal's message drops its "about". No format change: not one byte on the wire moves. *Site:* `crates/esker-proto/src/{codec,region,messages,txn}.rs`, `crates/esker-client/src/{wire,router,error}.rs` · *Size:* small, as sized · *Owner:* s1-sql · *Evidence:* `crates/esker-proto/tests/encoded_len.rs` — every variant of both request bodies, stamped and unstamped, has to equal what the encoder writes, so a field added without its length fails rather than drifts; `crates/esker-client/tests/txn.rs::a_prewrite_is_refused_by_its_frame_and_not_by_an_estimate_of_it`, red at `RequestTooLarge { bytes: 28378, limit: 27200 }` over a frame of about 26.0 KB; and `crates/esker-sql/tests/bulk_insert_frame.rs` against three real stores — the same shape at 36,000 rows, red at 17,028,001 estimated bytes over a frame of about 16.16 MB, and green in 26 s. Counterfactual: the estimate put back in the client alone turns both red again while the exactness test stays green. |
| 97 | ~~**A transaction that made a sequence and drew from it drew `1` for `START 7` and was refused at `COMMIT`; a `REPEATABLE READ` block that drew and then dropped what it drew from was refused at `COMMIT` too.**~~ Found by s2-plpgsql's unit F while capturing #94 (`esker-coord/NOTE-s2-sequence-counter-in-two-transactions.md`) and issued as unit G on 2026-09-15; registered in §1 when the unit began and moved here by the same unit. **PostgreSQL 19beta1:** `CREATE SEQUENCE … START 7` reports `last_value` `7` with `is_called` false in the transaction that made it and hands out `7` and then `8`; that transaction commits and the next value is `8`, in a block and in a `DO` block alike; a `REPEATABLE READ` block that draws and then drops the sequence, the table that owns it or the column commits; `TRUNCATE … RESTART IDENTITY` goes back to `START` — `50`, not `1`; a dropped sequence made again under its name starts at `1`, and a rolled-back drop keeps the numbers (`esker-coord/s2-f94b.out`, `s2-f94d.out`, `s2-g97.out`). **The mechanism — one key, two kinds of writer.** The counter `record::sequence_value_key(tenant, sequence_id)` is written by transactions of their own: `Executor::next_sequence_value` → `catalog::allocate_sequence_values` (every `nextval` and serial fill), `Executor::set_sequence` → `catalog::set_sequence_value` (`setval`), `Executor::restart_sequence` → `catalog::restart_sequence` (`RESTART IDENTITY`), `Executor::delete_sequence_counters` → `catalog::delete_sequence_counter` (#94). It was also written in the statement's transaction: `exec::ddl::create_sequence` → `catalog::set_sequence_value` put the `START` there, and `catalog::drop_sequence` (`DROP SEQUENCE`, `DROP SCHEMA … CASCADE`), `catalog::drop_sequences` under `drop_table` (`DROP TABLE`) and `catalog::replace_table` (`ALTER TABLE … DROP COLUMN`) deleted it there. A draw could not see the uncommitted `START` and began at `1`; its commit gave the key a version after the statement's snapshot, and the statement's own write to the key was then refused at `COMMIT` by the write-write rule (`MemoryBackend`'s `written_since`; Percolator's prewrite on a store) — at READ COMMITTED for a `CREATE` whose statement came before the draw, at `REPEATABLE READ` for a drop. | **Closed by s2-plpgsql's unit G, 2026-09-15** (the commits that carry this row). The statement's transaction no longer writes the key. `CREATE SEQUENCE` writes no counter, and a sequence with none hands out and reports its record's `START` (`catalog::allocate_sequence_values`, `catalog::sequence_state`), which the relation a sequence is read as now carries to the plan (`catalog::sequence_relation_def`, `plan::Node::SequenceRead`); a drop leaves the counter behind, as `catalog::drop_table` already leaves the row-id allocator's key and for the same reason — a dropped sequence's creation committed, so its id is never taken again and nothing reads the key. `TRUNCATE … RESTART IDENTITY` going back to `START` follows from the first half. No format change. Site: the key and the writers above; changed `crates/esker-sql/src/exec/ddl.rs`, `catalog/mod.rs`, `exec/mod.rs`, `exec/query.rs`, `exec/dml.rs`, `plan/query.rs`; size Small; owner s2-plpgsql. Evidence: `tests/corpus/pg19_sequence_counter_writers.txt` (60 statements) for the replay — two rows declared, neither this mechanism: `last_value` after a draw is the reserved block's end, the trade `tests/add_column_not_null.rs` declares — and `tests/sequence_counter_writers.rs`, five of whose seven tests were red before the fix (`1 | f` for `7 | f`, `1` for `7`, `40001` at `COMMIT`, `1` for `50`, and the replay with 17 of its 60 statements disagreeing) while its two guards were green, and under the counterfactual on `91048c3e` seven mutations, each compiled afresh and taken back byte for byte, turned red the tests that pin them — the counter written in the statement's transaction again (every `COMMIT` after a draw refused), each of the three drops deleting it again (one `REPEATABLE READ` `COMMIT` each), a missing counter drawn from `1` (`7`, `50`, and #94's `START 101` row as well), a missing counter reported as `1`, and a plan that carries no `START` (both `last_value` tests and the corpus's read before the draw) — after a first round showed that read shared its statement text with the declared read after the draw and was excused with it, so the two reads are labelled apart now; the row #94's corpus declared for this finding, `pg19_sequence_after_rollback.txt:75`, agrees now and its declaration is gone. |
| 95 | ~~**A column default named a sequence outside `public` qualified whatever the search path was, and one in `public` bare whatever it was.**~~ Found by s2-plpgsql's #93 capture (`tests/corpus/pg19_serial_without_a_key.txt:65`: with `SET search_path = s2sq2, public`, PostgreSQL printed `nextval('ser_id_seq'::regclass)` and this node `nextval('s2sq2.ser_id_seq'::regclass)`) and issued with #94 as unit F on 2026-09-13; registered in §1 when the unit began and moved here by the same unit. **PostgreSQL 19beta1** prints the `nextval('…'::regclass)` a default holds the way `regclass` output prints a relation: bare when its schema is on the search path and qualified when it is not, `public` included — `nextval('s2fq.t_id_seq'::regclass)` by default, `nextval('t_id_seq'::regclass)` once `s2fq` is on the path and `nextval('public.s2f_pub_id_seq'::regclass)` once only `s2fq` is, alike through `pg_get_expr` and `information_schema.columns.column_default`, and a quoted name keeps its quotes (`esker-coord/s2-f95.out`). **The mechanism:** `catalog::pg_attribute` qualified by the schema — bare in `public`, qualified anywhere else — and the catalog views that print a default had no search path to ask. | **Closed by s2-plpgsql's unit F, 2026-09-13** (the commit that carries this row). The session's search path goes into `CatalogView::rows_of` beside the rendering and down to `pg_attribute::default_expression`, and the default names its sequence by the rule `::regclass` and `format_type` already followed — now one function in the catalog, `catalog::schema_on_path`, where it was a private one in `crate::exec::cursor` the catalog could not reach. No format change. Site `crates/esker-sql/src/catalog/pg_attribute.rs`, `catalog/pg_catalog.rs`, `catalog/information_schema.rs`, `catalog/mod.rs`, `exec/cursor.rs`; size Small; owner s2-plpgsql. Evidence: `tests/corpus/pg19_default_sequence_name.txt` (20 statements, one session) and `tests/default_sequence_name.rs`, both red before the fix (5 of the 20 statements disagreed) and red again with the schema rule put back under the same counterfactual (five rows of that corpus, the test, and row 65 of the #93 replay); the #93 corpus's declaration of it is gone. |
| 94 | ~~**A sequence created after a rolled-back one carried on from its numbers: `3` after two, where PostgreSQL starts again at `1`.**~~ Found by s2-plpgsql's #93 capture — the second session of `esker-coord/s2-d93a.out` answered `6` where PostgreSQL answers `1` — and issued with #95 as unit F on 2026-09-13; registered in §1 when the unit began and moved here by the same unit. **PostgreSQL 19beta1:** a sequence that was rolled back is gone, numbers and all — the next serial, or a sequence of the same name, starts at `1`, and its `currval` is `55000` until this session draws from it; a `ROLLBACK TO` takes back what was made after the mark, whether `CREATE TABLE`, `CREATE SEQUENCE` or `ALTER TABLE … ADD COLUMN` made it, and so does the implicit transaction of a `DO` block that raised; and a rollback still gives no number back to a sequence that survives it (`esker-coord/s2-f94.out`, `s2-f94b.out`, `s2-f94c.out`, `s2-f94e.out`, `s2-f94f.out`). **The mechanism:** `catalog::allocate_id` takes a relation id inside the creating transaction, so a rolled-back creation gives its id back and the next `CREATE` takes the same one; a sequence's stored counter, advanced in a transaction of its own, and the block a node reserved from it are keyed by that id, and both outlived the rollback — so the new sequence carried on from the old one's block on the node that drew (`3`) and from its committed counter on any other (`33`), and the session that drew answered `currval` for a sequence it had never drawn from. | **Closed by s2-plpgsql's unit F, 2026-09-13** (the commit that carries this row). The executor records every sequence a transaction creates — through `CREATE TABLE`, `CREATE SEQUENCE` and `ALTER TABLE … ADD COLUMN` — and when what created it does not commit (a `ROLLBACK`; a `ROLLBACK TO`, from the mark on, which a savepoint now carries; a statement outside a block that fails or whose commit fails; a statement restarted after a wait; a failed `COMMIT`; a session that ends inside its block) gives back the node's block, this session's `currval` and the stored counter — the counter deleted in a transaction of its own and only when there is one, so a sequence nothing drew from writes nothing (`Executor::forget_the_created_sequences`, `catalog::delete_sequence_counter`). It is the dual of a dropped sequence's block being forgotten when the drop commits, and the three comments that said a relation id is never reused now say which ids are not. No format change. Site `crates/esker-sql/src/exec/mod.rs`, `exec/savepoint.rs`, `exec/ddl.rs`, `catalog/mod.rs`, `sequence.rs`; size Small; owner s2-plpgsql. Evidence: `tests/corpus/pg19_sequence_after_rollback.txt` (84 statements) for the replay — one row declared, and not this mechanism: `START` in the transaction that made a sequence answers `1`, and once it has drawn that transaction cannot commit, **a finding reported for a number of its own** — and `tests/sequence_after_rollback.rs`: six of the eight tests first written were red before the fix (`3` three times, `33`, a `currval` of `3`, and the replay with 14 of its then 66 statements disagreeing) while its two guards were green; three more were added beside the fix, and under the counterfactual on the merged tree (`000312b2`) fourteen mutations, each compiled afresh and taken back byte for byte, turned red the tests that pin them — each place that gives back (`ROLLBACK`; `ROLLBACK TO`, and from the mark rather than the start of the block; a statement that fails; a statement whose commit fails; a session that ends), the counter and the block kept one at a time, the counter deleted without being read first (the `REPEATABLE READ` block's `COMMIT` answered `40001`), the two commits that clear the record, and the three places that make one — and the one test a kept counter left green is the sequence made again outside a block, whose own `CREATE SEQUENCE` writes the counter afresh. The #93 corpus's two declarations of this finding are gone. The statement restarted after a wait and the failed `COMMIT` have no test of their own. |
| 93 | ~~**A `serial` or identity column on a table with no key did not take its number: the insert was `23502`.**~~ Found by s1's probe during unit J (`esker-coord/s1-unit-j/j-probe2.log`): `CREATE TABLE ser_nokey (id serial, v text)` then `INSERT INTO ser_nokey (v) VALUES ('x')` answered `23502 null value in column "id" of relation "ser_nokey" violates not-null constraint`, for `serial`, `bigserial` and both kinds of identity alike, while the same tables with a primary key answered `INSERT 0 1`. Issued as s2-plpgsql's next unit on 2026-09-13; registered in §1 when the unit began and moved here by the same unit. **PostgreSQL 19beta1** numbers every one of them `1`, `2`, `3` with or without a key — beside a `UNIQUE` alone, with the column anywhere in the row, and when the column is added to a table that already has rows — and `pg_get_serial_sequence` names the sequence for that column and no other, and `DROP COLUMN` takes it along (`esker-coord/s2-d93a.out`, `s2-d93b.out`). **The mechanism:** a table with no primary key carries an internal row id at position 0, so every declared column sits one later in the row than in the statement; `CREATE TABLE` recorded the column a sequence fills by its position in the **statement**, so the sequence filled the row id's slot — and `pg_get_serial_sequence` and `DROP COLUMN`, which find a sequence by its column, found the neighbour's. | **Closed by s2-plpgsql's unit #93, 2026-09-13** (the commit that carries this row). `crate::exec::ddl::sequences_for` finds the column by name in the columns the table will have — what `catalog::inherited_sequences` and `ADD COLUMN` already did. **Found on the way and fixed with it:** `pg_get_serial_sequence` looked its table up in `public` only, so with `SET search_path = s, public` a bare name was `42P01` for a table the same session could insert into; it now resolves the name by the rule `'t'::regclass` does (`crate::exec::cursor`), and a name that is nowhere is still `42P01 relation "zomg" does not exist`. Site `crates/esker-sql/src/exec/ddl.rs`, `crates/esker-sql/src/exec/cursor.rs`; size Small; owner s2-plpgsql. Evidence: `tests/corpus/pg19_serial_without_a_key.txt` (51 statements, two sessions) for the replay — four of its rows declared, none of them #93's mechanism: the `ADD COLUMN … serial` on a table with rows this node refuses by name, a default that prints its sequence qualified whatever the search path, and **a finding reported for a number of its own**, that a sequence created after a rolled-back one continues its numbers — and `tests/serial_without_a_key.rs`, whose four tests then written were red (both inserts `23502`, `pg_get_serial_sequence` NULL for `id` and a sequence for `v`, and the replay) before the fix and all five red — the search-path test, added with its fix, among them — with the two mutations (the statement's column position again, the table name read as written again) taken back byte for byte under its counterfactual. |
| 96 | ~~**At READ COMMITTED a `SELECT … FOR UPDATE` whose row another transaction commits after the statement's snapshot and before its lock is refused `40001`, where PostgreSQL 19 locks the row and returns its new version.**~~ Two sessions interleaved by `pg_sleep` (unit L, 2026-09-13): **g1** — the commit lands inside the window, a `pg_sleep` in the locking query's target list holding the statement between reading the row and locking it — answers `40001 could not serialize access due to concurrent update: a commit at 1008 beat this transaction at 1004`; **g3** — the lock waits for a holder, which then commits — answers the same after a one-second wait, and that is the shape Rails meets: `lock!` behind another transaction's update. PostgreSQL 19 answers the new row, `11`, in both (its `LockRows` re-reads the newest version at READ COMMITTED); at REPEATABLE READ both answer `40001` (g2). `lock_rows`' own doc says a held row is waited for and then the statement runs again, which is ADR 0057's mechanism for writes; on the lock path the node does not do it. | **Paid 2026-09-13 by s1-sql's unit M.** At READ COMMITTED a lock refused because its row moved after the statement's snapshot re-runs the statement at a fresh snapshot — `exec::lock_or_restart`, through which both `lock_rows`' first attempt and `wait_for_the_lock`'s retry go, asking `changed_since_statement` after the refusal — so g1 and g3 lock the row and answer `11` as PostgreSQL 19 does, and REPEATABLE READ's g2 stays `40001`. No format change and no client change: a lock the client did not take leaves no stamp, and the re-run's lock is stamped at its own snapshot. The in-memory backend's cluster lock validates as the store's does, where it had locked the version it read and answered g1 with a stale `10`. *Site:* `crates/esker-sql/src/exec/mod.rs` (`lock_rows`, `wait_for_row`); the eager lock's statement stamp in `crates/esker-client/src/txn.rs` · *Size:* small, as sized · *Owner:* s1-sql · *Evidence:* `concurrent_unique_insert.rs` (three real stores) and `read_committed.rs` (`MemoryBackend`), `a_for_update_whose_row_commits_inside_its_window_locks_the_new_row`, `a_for_update_that_waited_for_the_committer_locks_the_new_row` and `a_for_update_at_repeatable_read_whose_row_commits_inside_its_window_is_refused_with_40001`; red again with the re-run taken out (both backends' g1 and g3: 38 run, 34 passed) and with the in-memory validation taken out (that backend's g1 and g2: 38 run, 36 passed). Measured before: `esker-coord/s1-oracle-2026-09-13/g/` (PostgreSQL 19), `esker-coord/s1-unit-l/l3-probe.log` (this node against three real stores, the probe's source beside it); `docs/plans/0114-implementation.md` §2 step 5 |
| 91 | ~~**At READ COMMITTED the eager row lock — `FOR UPDATE`'s `Check` — is validated at `start_ts` instead of the statement's read timestamp, so a row another transaction committed after `BEGIN` is always `40001`.**~~ Opened 2026-09-13 out of #90's second half, which is what run 127 attempt 6 actually hit: after ADR 0114 §1 the duel's second `INSERT` waits and is `23505`, and the rescue's `where(nick:).lock.find_by!` is refused `40001 could not serialize access due to concurrent update: a commit at 1014 beat this transaction at 1004`. **The mechanism:** ADR 0088's eager lock prewrites a key that is not in the write buffer as `TxnMutation::Check { key }` (`esker_client::Transaction::lock` → `mutations_for`); a `Check` carries no read timestamp, and the store gives it the request's `start_ts` (`crates/esker-store/src/txnkv.rs:623`), so `esker_txn`'s prewrite refuses the key for any commit after the transaction **began** rather than after the statement's snapshot. ADR 0057 §5's *"validates the key at that statement's `read_ts`"* was true of the lock before ADR 0088 moved it into the store. **PostgreSQL 19** locks the version the statement reads: a row updated after the locker's first statement answers `11` and a row inserted after it answers `20`; at REPEATABLE READ the first is `40001` and the second no row (`esker-coord/s1-oracle-2026-09-13/e/`). **Wider than #90:** every READ COMMITTED `lock!`, `with_lock` or `.lock` in a transaction that began before another transaction committed the row. `MemoryBackend` takes no Percolator lock, so no in-process test can see it. | **Paid 2026-09-13 — [ADR 0114](../adr/0114-a-unique-key-being-written-waits-at-read-committed.md) §2, built by s1-sql (unit K) after the user approved the format change and answered question 1 (a).** `TxnMutation::Check` and `TxnWrite::Check` carry `read_ts: Option<u64>` — tag and kind 5 without one, as every existing byte stays, and tag and kind 7 with one; the store validates a check at `read_ts.unwrap_or(start_ts)`, and the client stamps an eager lock with the statement's read timestamp before `pin_primary`, keeps the stamp only when the lock is taken, and drops it on `release`, so `commit` re-sends the lock at the snapshot it was validated at. Stores are upgraded before nodes: an old store refuses tag 7 as an invalid request, which the node reports as `08006`, and an old follower stops at kind 7 rather than skip it. *Site:* `crates/esker-store/src/txnkv.rs:623` (a `Check`'s read timestamp is `start_ts`); `crates/esker-client/src/txn.rs` (`Transaction::lock`, `mutations_for`) · *Size:* **Medium, and most of it is the ruling.** ADR 0114 §2 (a): a `Check` that carries the statement's read timestamp — an additive tag on the wire (`TxnMutation`) and an additive kind in the Raft log (`TxnWrite`), as ADR 0057 §4 did for `Put` and `Delete` with tags 3 and 4 — across `esker-proto`, `esker-txn`, `esker-store`, `esker-client` and their goldens; the read-set `Check` a SERIALIZABLE commit sends stays tag 5 at `start_ts` · *Owner:* s1-sql · *Evidence:* `concurrent_unique_insert.rs::a_for_update_of_a_row_committed_after_the_transaction_began_takes_the_lock` and the duel, green with their `#[ignore]` removed; the store test `a_lock_is_validated_at_its_statement.rs` and five client tests beside it; REPEATABLE READ's e3 and e4 guarded by `a_for_update_at_repeatable_read_keeps_the_transaction_s_snapshot`; red again with the store's decision alone taken back to `start_ts` (`a commit at 1009 beat this transaction at 1004`, the duel's `… 1015 …`, the store test `Conflict { commit_ts: 20 }`) while REPEATABLE READ's twin stays green. Before: `crates/esker-sql/tests/concurrent_unique_insert.rs::a_for_update_of_a_row_committed_after_the_transaction_began_takes_the_lock`, red (`a commit at 1009 beat this transaction at 1004`, and `… 1011 … 1006` for the inserted row) and `#[ignore]`d with ADR 0114 §2's reason until the ruling, `--run-ignored only` to run it; `relations_test_s_find_or_create_by_duel_commits_both_sessions` waits on it too |
| 90 | ~~**Two sessions inserting one unique key at READ COMMITTED: the second is refused `40001` where PostgreSQL makes it wait and answers `23505`.**~~ Attempt 6, file 369, `relations_test.rb` — the file's two errors and its only divergence from run 116, which had it clean: `CreateOrFindByWithinTransactions#test_multiple_find_or_create_by_within_transactions` and `…_bang_within_transactions`, both `ActiveRecord::SerializationFailure: PG::TRSerializationFailure: ERROR:  could not serialize access due to concurrent update: a commit at 469047227545812992 beat this transaction at …`. The tests race two threads' `Subscriber.transaction { find_or_create_by(nick: "bob") }` on purpose: `find_by`, then `create_or_find_by`'s `SAVEPOINT` and `INSERT`, and on `RecordNotUnique` a `ROLLBACK TO SAVEPOINT` and a `SELECT … FOR UPDATE`. `subscribers` has **no primary key** (`id: false`) and a unique index on `nick`. **PostgreSQL 19, two `psql` sessions interleaved by `pg_sleep`, 2026-09-13:** at READ COMMITTED the second `INSERT` waited 1.32 s — until the first transaction's `COMMIT` — and answered `23505 duplicate key value violates unique constraint`, and Rails' sequence then went through `ROLLBACK TO SAVEPOINT` and `FOR UPDATE` to `COMMIT` with one row; the file's two tests against PostgreSQL 19 itself are `2 runs, 4 assertions, 0 failures, 0 errors`. **The wait is every level's, and what follows it is not:** a holder that rolls back lets the insert through at all three; a holder that commits is `23505` at READ COMMITTED and REPEATABLE READ, and at SERIALIZABLE `40001 could not serialize access due to read/write dependencies among transactions` when the inserter had read the key first and `23505` when it had not. **The site is read, not yet proved by a red test:** `exec::dml::write_row` takes ADR 0057's row lock on the **row key**, and a unique index entry is read at the statement's snapshot and put with no lock — so where the row key is an internal row id, nothing makes the second writer wait. **Paid in part, 2026-09-13 — [ADR 0114](../adr/0114-a-unique-key-being-written-waits-at-read-committed.md) §1 and §3; §2 waits for the user.** Measured against three real stores (`crates/esker-sql/tests/concurrent_unique_insert.rs`), the duel had **two defects, one behind the other**. **(1) Built:** `write_row` locked the row key and not the unique entry, so the second writer of `bob` never waited — its `INSERT` answered at once and its `COMMIT` was refused `23505`, out of the rescue's reach. At READ COMMITTED the entry is now locked before it is read: the second insert waits, and is `23505` from the `INSERT` (PostgreSQL's cases 01–03) or goes through (02). **(2) Not built — a format change:** the rescue's `SELECT … FOR UPDATE` takes ADR 0088's eager lock as a `Check` mutation, which carries no read timestamp and is validated at the transaction's `start_ts` (`crates/esker-store/src/txnkv.rs`), so at READ COMMITTED it refuses any row committed after the transaction **began**. After (1) the duel fails with `40001 could not serialize access due to concurrent update: a commit at 1015 beat this transaction at 1004` — attempt 6's text — where PostgreSQL locks the version the statement reads (`11` and `20` in its cases e1, e2). The fix gives `Check` a statement timestamp on the wire and in the Raft log, which `CLAUDE.md` puts behind a yes, and it is **not #90's alone**: it is every `lock!` in a block that began before another transaction's update of the row. **(3) Paid, 2026-09-13 — ADR 0114 §3, (ii) with the arbiter rule**, ruled by the coordinator under the mandate to close the gaps (the user may overrule it): the unit was issued as *SERIALIZABLE keeps its `40001`*, and the node's answer there was always `23505` at `COMMIT`. A lost race on a unique key is now `40001` when SERIALIZABLE had read the key before writing it, or when an `ON CONFLICT` arbiter had read it at REPEATABLE READ or SERIALIZABLE, and `23505` otherwise — PostgreSQL's code in its cases 04, 07, 09, 10 and 13–16, at `COMMIT` rather than at the `INSERT`. (2) is now **#91**, which carries its own row, and its red tests stay `#[ignore]`d with ADR 0114 §2's reason until the format ruling — `--run-ignored only` runs them. | **Paid in full 2026-09-13** by [ADR 0114](../adr/0114-a-unique-key-being-written-waits-at-read-committed.md), all three parts built by s1-sql: (1) §1, the unique entry locked before it is read at READ COMMITTED; (3) §3, `40001` where SERIALIZABLE had read the key or an `ON CONFLICT` arbiter had at the two snapshot levels; and (2), which had become #91, §2 — the eager lock validated at the statement's snapshot, after the user approved the format change. `relations_test_s_find_or_create_by_duel_commits_both_sessions`, the file's two tests statement for statement, is green with its `#[ignore]` removed. *Site:* `crates/esker-sql/src/exec/dml.rs` (`write_row`), and for (2) `crates/esker-client/src/txn.rs` (`mutations_for`) with `crates/esker-store/src/txnkv.rs` (a `Check`'s read timestamp) · *Size:* **(1) Small, built.** (2) Now #91. **(3) Small, built.** · *Owner:* s1-sql · *Evidence:* `esker-rails-harness/results/run-127/attempt6.md` §5 for the field; `esker-coord/s1-oracle-2026-09-13/d/` (thirteen two-session cases), `e/` (`FOR UPDATE` after a commit) and `d-relations-pg19.out` (the file on PostgreSQL 19) for the oracle; `concurrent_unique_insert.rs`'s two waits, green after (1) and red with it taken out, for (1); the §3 cases in `concurrent_unique_insert.rs` and `serializable.rs`, red before (3) and under either of its counterfactuals, for (3) |
| 92 | ~~**A constraint's name was chosen, found and refused by rules PostgreSQL does not have — in `public` as much as in any schema.**~~ Found by B5's capture (`pg19_constraint_name_in_a_schema.txt`, where three of its rows were declared first) and issued as s2-plpgsql's unit D on 2026-09-13; registered in §1 when the unit began and moved here by the same unit. Four shapes, each measured against PostgreSQL 19beta1. **(1)** A `CHECK` with no name was `<table>_check` as a table constraint and `<table>_<column>_check` as a column constraint on that column, where PostgreSQL names it for **the one column its expression reads** and for none when it reads none or several — it cannot see where the constraint was written — and **numbers** a taken derived name past every constraint in the schema: `ck_a_check1`, and `nsq_x_check1` beside another table's `nsq_x_check`. **(2)** `ADD CONSTRAINT` checked a new name against some kinds of the table's constraints and not others, so a `CHECK` named like the primary key, a `UNIQUE`, a `NOT NULL` or a foreign key was added, where a table's constraints share one name space and the answer is `42710`; two given `CHECK`s of one name in one `CREATE TABLE` are `42710 check constraint "dup" already exists`, a sentence of their own. **(3)** A `UNIQUE` or `PRIMARY KEY` whose name a relation held was `42710` or went through, where PostgreSQL meets the relation first (`42P07`) and a constraint second. **(4)** `SET CONSTRAINTS <name>` took the first constraint of the name in any schema and read `s.x` as two names, where PostgreSQL finds a qualified name in its own schema (`3F000` for one that is not there) and a bare one in the first schema on the search path that has one — and every constraint of that name there. **The half the issue did not name:** a derived foreign-key, `UNIQUE` or primary-key name that was already taken was **refused**, where PostgreSQL numbers it — `t_a_fkey1`, `k_a_fkey1` beside a `CHECK` called `k_a_fkey`, `t_b_key1`, `q_pkey1` beside a table called `q_pkey` — and widening (2)'s check without it would have refused more of them, so it was measured in the same session and built with it. | **Closed by s2-plpgsql's unit D, 2026-09-13** (the commit that carries this row). A constraint's name is chosen where the catalog is, in `crate::exec::ddl`: a `CHECK`'s or a foreign key's by `constraint_name` — a given one refused when `pg_constraint`'s rows for the table already hold it, a derived one numbered past every constraint name in the schema — and a `UNIQUE`'s or a primary key's by `index_constraint_name`, a relation first; the column a `CHECK` is named for is `only_column_read`, which counts the expression's distinct columns. `SET CONSTRAINTS` resolves each name by `constraints_named`, and the deferred setting is kept by schema and bare name (`exec/deferred.rs`), so a name no longer reaches a schema it was not found in. Site `crates/esker-sql/src/exec/ddl.rs`; `crates/esker-sql/src/catalog/pg_constraint.rs` (`constraint_names`, `constraint_names_in_schema`, `deferrable_named`); `crates/esker-sql/src/exec/mod.rs` with `exec/deferred.rs`; `crates/esker-sql/src/parse/mod.rs` (`written_constraint_names`). Size Medium; owner s2-plpgsql. Evidence: `esker-coord/s2-d92a.out`, `s2-d92b.out` and `s2-d92d.out` for the oracle; `tests/corpus/pg19_constraint_names.txt` (139 statements, three sessions) for the replay; `tests/constraint_names.rs`, whose five rule tests were red before the change and red again under its counterfactual — five asserted mutations (a `CHECK` named for no column, no numbering, no single name space, no relation met first, a bare `SET CONSTRAINTS` name looked for in every schema), all five tests and the replay red, both files restored byte for byte, while the sixth — `set_constraints_reaches_every_constraint_of_its_name_in_the_schema` — is a guard that was green before and after; `pg19_constraint_name_in_a_schema.txt`'s four declarations retired with it; and ADR 0115's declaration that `regnamespace` is not an index key made true — a primary key or `UNIQUE` over a `regtype`, `regproc` or `regnamespace` column, whose key the row codec writes no bytes for, is refused by name (`tests/regnamespace.rs::a_regnamespace_column_is_not_an_index_key`). |
| 58 | ~~**The same work costs more the longer a node has run.** r1's isolation pinned the catalog size and found the work *emitted* identical to the digit — 33.5 reads, 27.3 scans, 61.4 round trips per statement across two arms — while a load-matched pair went 34.4 → 52.6 ms per statement. Not compaction debt and not manifest churn: arm A wrote **no SST at all** in four passes. **The read-path half is landed** (#137): a scan stepped one MVCC version at a time and the engine stepped every superseded entry of a decided key, so a scan cost `O(keys × versions)` — 15,380 entries at V=256 against 220 after, flat from V=16 up (`esker.entries-stepped`, the counter added to see it). **What is left is that each read is dearer as history grows, and the mechanism is not named.** r1's 127d closing measurement after the read-path fix: slope down only ~25%, µs/statement still monotone 39.6 → 47.6 → 56.0. Round 2 established it is **not** more reads — the count is fixed by the catalog size and did not move over ten rounds — so it is per-read cost, and the next probe needs *history* rather than rounds (a thousand in-process, then a real cluster carrying history). **The space half is a different debt and has its own page**: nothing is ever collected because no safepoint is ever published, which is ADR 0110, #60 is why collecting reclaims records rather than bytes, and **#62 is why collecting reclaimed nothing at all** — a compaction arriving at the bottom level moved its input instead of reading it, so no drop rule ever ran. **The records half of it is now measured flat and the bytes half is not, so this row stays open, 2026-09-11.** Four units closed the chain the sentence above names: a safepoint is published (ADR 0110), a compaction reads its input rather than moving it (#62), a rising safepoint makes one happen at all (#70), and a deleted key's versions go as one segment rather than leaving an immortal gravestone (ADR 0111). Measured on `esker-sql/tests/collection_keeps_up.rs`, twelve rounds of identical DDL at the default 64 MiB write buffer, in process: files on the books went from `{6: 1}` … `{6: 12}` — one more every round — to an oscillation between two and three; entries held went from 135 → 1,095 to a flat 4 … 13; the read cost's slope went from `+288.9` a round to `−1.4 … +2.6`, an interval that contains zero, against a control arm of `+5,928.6`. **What that measurement does not reach is the reason this row is not closed**, and it is two things. **(a) #60.** Every value in that workload is a catalog record short enough to inline, so none of it spills to `default` — and a spilled value is never collected, which ADR 0111 makes sharper rather than softer: dropping the `write` record that named it leaves the value an orphan with nothing pointing at it at all (ADR 0112). A workload whose values exceed `SHORT_VALUE_MAX_LEN` still grows without bound and this probe cannot see it. **(b) The topology.** The probe publishes its own safepoint and has no placement driver, so the window arithmetic PD performs — `now − retention` — is not exercised by it; r1's attempt 5 is the real-cluster arm and had not reported when this was written.~~ | **Closed by run 127 attempt 6, on the build of `98958255`** — ADR 0111, ADR 0112 (#60), #85, #86 and #87 together, four stores and a placement driver, `64 MiB · cluster start --retention-ms 60000` asserted on the running PD — **the first real-topology pass to finish the suite, 426 of 426 files in 10.29 h.** It answers both reasons this row gave for staying open: **(a)** #60 is closed by ADR 0112 and was in the build, and **(b)** the topology is the measurement, with the placement driver publishing the safepoint. Three fixed-work series, and all three flat. The **catalog canary** — the same query, 272–276 rows, after every file — met the bar `plan-attempt6.md` §2 registered before the GO, files 271–300 within 1.3× of files 1–30: **1.03×**, 190 ms against 184, where attempt 5 was 2.50× at the same segment and 2.74× where it stopped; every segment from 1–30 to 421–426 stayed within 180–210 ms, and none rose more than 8% over the one before. The **user-data probe** — three fixed questions on a table nothing writes to after seeding — held 358–383 ms from file 31 to file 426, 334 samples and every one green. **Per-file time** held 93–99 s where attempt 5 had reached 248 s by file 330. **What it does not reach, said here so that finding it is not a reason to re-open the row:** it cannot name the commit — *"the BUILD removes the growth, not which commit does"* (`attempt6.md` §1) — and it has **no bytes series**, only the end state, 294 MB and 13 SSTs after 10.29 h. **The tests this row names were re-run before it moved**, in the container on `sql-gaps` over `2177e244`'s Rust, 2026-09-13: `esker-store::scan_cost_per_version` 4 of 4 and `esker-sql::collection_keeps_up` 2 of 2. · **Site** `crates/esker-store/src/txnkv.rs`, `crates/esker-engine/src/db/iter.rs` · **Size as opened** Large · **Owner** h1 · **Verified how** `esker-rails-harness/results/run-127/attempt6.md` §1–§4 for the close; `crates/esker-store/tests/scan_cost_per_version.rs` for the landed half; `esker-rails-harness/results/run-127c/shape-diff.md` and `esker-coord/h1-58-round2.md` for the history of the half that stayed open |
| 87 | ~~**A self-join plans both sides as point reads, because predicate placement cannot tell two aliases of one table apart.**~~ Attempt 5, files 240–248: `counter_cache_test.rb` failed four times on one assertion — `reset_counters`' `SELECT COUNT(*)` answered **0 where 1 was expected** — and `has_many_associations_test.rb` answered *"Couldn't find Namespaced::Firm with 'id'=49"* for a row the stores held. No `ERROR`, no `WARN`, no sentinel; the catalog canary cannot see user data. The pass binary `29e66e60` carries #79 (the store's chunked resume and the client's empty-batch rule) and #78, and not ADR 0111. | **Closed by `2e5f3b89`, the morning it was opened.** A filter now travels with **the name the statement refers to that relation by** — `access_path` takes `Option<(&Expr, &str)>`, so handing a predicate in without saying which instance it was written against stopped being expressible. Five of its seven call sites pass `None` and did not change a character. `mentions_only` compares to that name instead of `table.name`, `collect_equalities` stops discarding the qualifier, and the join's outer name is swapped **beside** its table by `drive_from`'s own flag so the two cannot go out of step again. · **Site** Unknown. **What it is not, measured**: `crates/esker-store/tests/a_live_key_a_scan_does_not_answer_for.rs` puts #79's walk through the four ways its arithmetic could drop a key and all four are green — (1) a key whose versions straddle the chunk boundary, in **both** real key shapes, a row key and a two-field secondary index key, three versions each; (2) the `lock` walk's reach, which counts *entries* where the union is cut by *keys* — the gap is real on paper and **cannot be silent**, because `esker_txn::read` meets the first live lock in the chunk and `scan` returns it as an error, so a range with any live lock in it gives no answer rather than a short one (asserted with its denominator: a thousand live locks refuse, and the same range answers for all 2,049 keys once they are resolved); (3) the byte budget cutting a key's versions, which cannot happen — the budget is checked per *pair* and a key's versions are collapsed before a pair exists; (4) a limit landing exactly on the chunk boundary. **And the collector is not it either, under the two things #78's enumeration cannot reach.** `crates/esker-store/tests/a_collection_under_many_keys.rs` is a stress probe where that one is a proof: a hundred keys in **both** real shapes, each with a random history of `Put`, `Delete`, `Check` (which commits a `Kind::Lock` above the version it validated), a rolled-back transaction and a lock left standing across a round; half the values **spilled** past `SHORT_VALUE_MAX_LEN` into `default`, where they are keyed by `start_ts` while the record that points at them is keyed by `commit_ts` ([ADR 0112](../adr/0112-collecting-a-spilled-value.md)); the safepoint rising every round with the sweeper's own `flush` + `compact_range(write)`; and a second thread writing a disjoint range so the **picker's** compactions run beside the sweeper's. Every round every key is read above the safepoint and compared with the last committed write. Green, and **with a denominator**: 12 rounds, 12 collections, 100 keys, **148 `write` records standing** where the rounds committed some twelve hundred — so the collector really was deciding — 58 live, 3,900 filler rows. That covers the shared `keep_as_newest` slot across many keys in one compaction, and the interleaving with concurrent compaction, which are the two things the single-key enumeration in `a_lock_is_not_a_version` cannot reach. **And it is neither: r1's binary comparison took the regression story away and the tap named the mechanism.** `counter_cache_test.rb` on a fresh cluster fails **identically** on `29e66e60` (with #78 and #79) and on `091f07bb` (with neither) — 4 of 56 both ways, in 95 s, with no pass and no accumulation. It is not new code; it is newly *reached* code, and that file is number 234 where no earlier attempt got past 44. What names it is the tap's own read counters in the same file: `SELECT COUNT(*) FROM bulbs WHERE car_id = $1` is `pr=0 rs=2` and right, while `SELECT COUNT(*) … FROM topics INNER JOIN topics replies_topics ON replies_topics.parent_id = topics.id` is **`pr=1 rs=0`** — one point read, no scan — and answers 0. **Reproduced at the SQL layer in 0.07 s** (`crates/esker-sql/tests/a_self_join_counts_nothing.rs`), and it is not the aggregate at all: the *join* returns no rows. The differentials in that file put it precisely — a plain child count, a join of two **different** tables, and the same self-join **driven from the child side** are all right; only the self-join whose **unaliased** copy is filtered by its primary key is wrong. `EXPLAIN` says why: `Nested Loop / Inner: Point Get on topics / Point Get on topics` — **both sides are point reads** where the same join without the `WHERE` plans `Inner: Point Get / Seq Scan on topics` and is right. **The two functions are named.** `exec::query::mentions_only` decides whether the `WHERE` may be pushed into a join side by comparing a column's qualifier to **`table.name`**, which is the same string for both aliases; and `exec::query::collect_equalities` matches `Expr::Column { name, .. }`, **discarding the qualifier entirely** — under a field whose own documentation says it is carried *"rather than dropped, which it used to be … the argument is wrong, because it also throws away the case where it is **not** right"*. So `topics.id = 1` is read as a primary-key equality on whichever instance is being planned, `access_path` pins the key, and the side that had to be scanned becomes a point read of the wrong row. **Paid the same morning, and the fix is smaller than the row's own sizing said.** A filter now travels with **the name the statement refers to that relation by** — `access_path` takes `Option<(&Expr, &str)>` rather than `Option<&Expr>`, so handing a predicate in without saying which instance it was written against stopped being expressible. Five of the seven call sites pass `None` and did not change a character; the two that push a predicate name their instance, and the join's outer name is swapped **beside** its table by `drive_from`'s own `swapped` flag so the two cannot go out of step again — which is exactly how this happened, since `drive_from` may put the *aliased* copy on the outside. `mentions_only` compares to that name instead of `table.name`, `collect_equalities` stops discarding the qualifier, and `pinned_relation` and `answerable_in` take it the same way. **What must not move is pinned beside it**: an equality written *on* the alias still reaches the alias (`WHERE replies_topics.id = 2` answers the reply), and an unqualified predicate is unchanged — a fix that simply stopped trusting qualifiers would pass the acceptance and break both. `EXPLAIN` before and after, printed by the test: `Inner: Point Get / Point Get on topics` answering nothing becomes `Inner: Point Get / Seq Scan on topics` answering the row. **`esker-sql` 2,237 run, 2,237 passed** — every corpus, with the planner on the hot path. **The `firm49` half is recorded and NOT claimed.** `SELECT … FROM companies WHERE type = $1 AND id = 49` is right here in all three forms (no alias, two aliases of one table, two tables): the plan is `Filter((type = …) AND (id = 49)) / Point Get on companies` and it answers the row. So this row does **not** explain that failure. h1's reading of #86 does — a columnar copy cannot see an unresolved secondary lock, so a fragment comes back exactly one row short with no error — and `EXPLAIN ANALYZE` on both statements here says `Fragments: []`, which is the honest answer and not a clearing: **this harness has no columnar learner**, so a green row path cannot stand in for a fragment path nobody has covered. Whether run 128's `id=49` went to a fragment is #86's question and its fix is on h1's branch. **Where the walk and the collector were ruled out**: `txnkv::get` and `txnkv::scan` both go through `esker_txn::read`, so a point lookup by primary key and an index `COUNT` share exactly one thing, and it is not the walk — which is also the only thing the two failing shapes have in common. The other half of what `29e66e60` changed is the collector (#78), and the pass was collecting on a 60 s window · **Size as opened** Unknown until it reproduces; r1 is extracting the statement sequences (`esker-coord/r1-counter-cache-sql.txt`, `r1-firm49-sql.txt`) and running `counter_cache_test.rb` against `29e66e60` and `091f07bb` on separate ports · **Owner** unassigned · **Verified how** Attempt 5's files 240–248. Four probes green on a real store, each named above with what it excludes. **A negative result with a denominator**: the first version of probe (2) passed on the error branch without ever building its state, and the first version of its denominator was one key short because the orphan id sat *inside* the committed block — both fixed before the row was written · **Re-checked when it moved**: Re-run 2026-09-12 on `98958255`: `esker-sql::a_self_join_counts_nothing` **8 of 8**, and the two probes that excluded the store — `esker-store::a_live_key_a_scan_does_not_answer_for` 4 of 4 and `a_collection_under_many_keys` 1 of 1. `esker-sql` was 2,240 of 2,240 on the merged tree when the fix landed. |
| 86 | ~~**A columnar copy dropped one row that no store was missing, once in one gate run.** `esker-sql`'s `joint_gate::the_learner_answers_fragments_that_agree_with_a_row_scan` is a differential — the same question to the copy and to the row scan — and on the gate of `e8d31d68` they disagreed by exactly one row: the fragment answered ids **1, 2, 5, 6** and the row scan **1, 2, 4, 5, 6**. The missing one is `[Int8(4), Text("barbara"), Text("unknown")]`, and **all four stores hold it** — one `write` record, reading as 19 bytes, on the three row stores and on the columnar learner itself. So this is not replication and not collection: the data is under the copy and the copy does not have it. `ts` 469029702496419840, `min_apply_index` **56**, every store `applied` **58**, table 16384, projection `[0, 1, 2]`, region 1. **The tail cannot explain it**, which is the observation that narrows this: ids 5 and 6 carry **one** `write` record each, exactly as id 4 does, and **both are in the copy** — so a cutoff at the end of the log would have dropped them first, not 4. (Id 3 is the control and both sides get it right: two `write` records, reading as absent, in neither answer.) Two hypotheses, and the row leans away from the first for the reason just given: the `min_apply_index` for the read `ts` was computed low — id 4 committed at 57 or 58 while the `ts` makes it visible — or `RaftPeer::tee_columnar` dropped a row on the way to `ColumnarSlot::commit`. **Intermittent**: re-run 3 of 3 green on the same tree, so it is a correctness defect and not a timing assertion, and a single run cannot bisect it.~~ | **Closed by h1's `b858ad37`.** The fix and not the test is what closes it, and the distinction matters here: this row's own evidence says the failure was intermittent and the named test was **3 of 3 green on the same tree before any fix**, so a green run of it is necessary and not sufficient. What is re-run here is the test as a guard against a regression, not as the proof. · **Site** `crates/esker-store/src/peer.rs` (`tee_columnar`), `crates/esker-store/src/columnar.rs` (`ColumnarSlot::commit`, `saw`), and `Store::serve_fragment`'s `min_apply_index` — the same family as #74 and #77 · **Size as opened** Unknown until it is reproduced; the reproduction is the row's own first problem, and `joint_gate` running under a loaded box is the only thing that has produced it · **Owner** h1 (#74's family) · **Verified how** The gate of `e8d31d68`, 2026-09-12 02:57, one failure in 4,630; the dump the harness wrote is kept at `esker-coord/joint-gate-disagreement-e8d31d68.txt` with every store's record counts beside both answers. Re-run 3/3 green on the same tree · **Re-checked when it moved**: Re-run 2026-09-12 on `98958255`: `esker-sql::joint_gate::the_learner_answers_fragments_that_agree_with_a_row_scan` green, and the whole `joint_gate` binary 8 of 8. **What is still open is #88**, which came out of this fix: a fragment refuses a whole scan where the row path refuses one key. |
| 80 | ~~**A reverse `RawKv` scan still asks each region once, because which end of a piece a resume moves is undecided.** #79 gave every other scan the rule that only an **empty** batch ends a range — a batch is bounded by the caller's page *and* by a byte budget no caller can see, so a short one proves nothing — and `RawClient::scan_with` pages on that rule in the forward direction only. Reverse is left at one call per piece, and the reason is a **disagreement between two pieces of existing code, not laziness**: `RawClient::regions_of` hands out its pieces as `(low, high)` in key order whichever way the walk goes, while `rawkv::scan_bounds` reads a reverse `RawKvReq::Scan`'s **`start` field as the exclusive upper bound** and its `end` as the lower — so `scan_region(&from, &to, …, reverse)` sends the piece's low key as the store's upper bound. Which end a reverse resume should move is a question about that pairing, and the pairing looks wrong before any resume is added. Answering it by guess, in a unit that could not compile at the time, would have been worse than the one call it leaves.~~ | **Closed by `7d80244b`.** The pairing is settled in the one place that builds a request: the client's arguments are `(low, high)` in both directions and the wire's reverse fields are `(upper, lower)`, so `scan_region` swaps them and the store's convention is untouched. No wire change. Routing had the same fact and the same gap — a reverse `Scan` routes by its **lower** bound — and it pages on #79's rule, a reverse piece resuming by lowering its exclusive upper bound to the last key of a descending batch. · **Site** `crates/esker-client/src/raw.rs` — `scan_with`'s `reverse` arm (which carries this reasoning as a comment), `regions_of`, and `crates/esker-store/src/rawkv.rs::scan_bounds` opposite it · **Size as opened** **Small, and mostly reading.** The work is deciding what a reverse `RawKvReq::Scan`'s two bound fields mean across a multi-region walk, writing a test that crosses a boundary backwards, and then the resume is four lines. The bound question may turn out to be a defect of its own, which is why it is sized as a measurement rather than as an edit · **Owner** unassigned · **Verified how** **The risk is low and named.** `scan_reverse` has exactly **one** caller in the repository and it is a test — `crates/esker-client/tests/multi_region.rs:376`, `scan_reverse(b"", b"", 100)`, both bounds empty, which is the one shape where the pairing above cannot show itself. No SQL path reaches it: `Transaction::scan_page` is the only sender of `TxnKvReq::Scan` and hard-codes `reverse: false`. So what is open is a silent-subset hole on a path nothing in production walks — and a bound question underneath it that a test with empty bounds cannot see · **Re-checked when it moved**: Re-run 2026-09-12 on `98958255`: `esker-client::raw_scan_reverse` 3 of 3 — including the 1,500-key `a_reverse_scan_of_a_whole_region_comes_back_whole` — and `multi_region` 13 of 13, with `a_reverse_raw_scan_visits_the_regions_from_the_top` among them. |
| 60 | ~~**A spilled value is never collected.** `gc.rs`'s module doc said *"Everything older goes, and so does its `default` entry"*; nothing in that module writes to or deletes from the `default` family — every `cf::DEFAULT` in it reads retention configuration. A value longer than `SHORT_VALUE_MAX_LEN` (255) is stored at prewrite in `default`, keyed by `(user_key, start_ts)`, and it **stays after the `write` record naming it is collected**. Measured with `esker admin gc` (ADR 0110 step 1), eight keys, twelve versions, 512-byte values, at safepoint 0 against a safepoint above all of them: `write` **96 → 8**, `default` **96 → 96**. So collecting reclaims version *records* and not *bytes* — a version record is tens of bytes and a value is as large as the user made it — which is the number anyone measuring #58's space curve will quote. **Two routes out, neither free.** **(a) The `write` filter emits the deletion**: it already decodes the record it is dropping, so it knows the `start_ts` whose value dies with it — but a compaction filter runs inside one column family and cannot write to another, so this needs a channel out of the filter (a queue of `default` keys the store deletes afterwards) and that channel has to survive a crash mid-compaction or it leaks silently instead of loudly. **(b) A `default` filter that asks the `write` family**: keep `(user_key, start_ts)` if any surviving `write` record names that `start_ts`. Self-correcting and needs no channel, but it is a cross-family read *during* compaction, against a version of the `write` family that is itself being rewritten — and getting that snapshot wrong deletes a live value. (a) is smaller and leaks on a crash; (b) is safer to reason about and harder to make correct. **Written up as [ADR 0112](../adr/0112-collecting-a-spilled-value.md), proposed 2026-09-11, with a third option and a recommendation.** ADR 0111 changed the shape of this row rather than the size of it: a deleted key's `write` records now go entirely, so its `default` entries lose their last reference — which makes the orphan unreachable in principle and, at the same time, makes "unreferenced" unambiguous to decide. The ADR recommends the option that reads `write` to judge `default` on the sweeper's existing pass, because it is the only one with no durable side-channel and its failure mode is keeping too much.~~ | **Closed by `ccb78004` (ADR 0112).** `collect::Sweeper` runs a pass over `default` between the two compactions — after `write`'s, so it can see which records the collection left, and before `default`'s, so the deletions it writes are applied by the same sweep. · **Site** `crates/esker-store/src/gc.rs` · **Size as opened** Medium · **Owner** unassigned · **Verified how** `crates/esker-store/tests/safepoint_collects.rs::a_spilled_value_is_collected_with_the_version_that_names_it`, `#[ignore]`d and red on purpose — it is the acceptance · **Re-checked when it moved**: Re-run 2026-09-12 on `98958255`: `esker-store::safepoint_collects::a_spilled_value_is_collected_with_the_version_that_names_it` green — the `#[ignore]` this row was waiting on is gone, which is what its own text said closing it would mean — with `safepoint_collects` 4 of 4 and `a_spilled_value_is_collected` 4 of 4 beside it. |
| 85 | ~~**A columnar learner answers no fragment until something else commits.**~~ A fragment asks `Store::serve_fragment`, which calls `Store::catch_up` before it evaluates anything, and `catch_up`'s first act is `RaftPeer::read_index_as_learner().await` — a oneshot that resolves only when the core answers the ReadIndex and errors only when the peer **stops**. On a learner that has just been placed, with **nothing committed since the conf change that placed it**, it does not resolve, and there is no bound anywhere on the path: `catch_up` *refuses* on `min_apply_index` rather than waiting for it, so this is the only wait in `serve_fragment`, and it has no deadline. **Measured, not inferred**: the same construction hung there for 60 s on three separate runs (`timeout` killed the first two at 300 s and 200 s before a bound was added to name it), and committing **one unrelated raw `put`** between the placement and the fragment makes it answer immediately — that write is the only thing that changed. **Why it is a product row and not a test note**: a SQL node asking a newly placed columnar learner for a fragment has no reason to write something first, and the wait it inherits has no end — the statement does not fail slowly, it does not fail at all. The learner has the data (the region arrives by snapshot and the row store holds every version), so this is liveness and not correctness. It is invisible to the suite because every existing test commits a row after the placement before it asks, `a_placed_columnar_learner_answers_for_the_rows_that_predate_it` included — which is the *near miss* rather than coverage. | **Closed by (b), the user's ruling of 2026-09-12.** `Store::catch_up` bounds its `read_index_as_learner` round (`READ_INDEX_WITHIN`), and inside the bound a peer with nothing outstanding — `applied == commit == last_index` — is read as caught up and answered from what it has applied; a peer with work left is refused `TooFarBehind` with the three numbers. **What that claims and what it does not**, because the difference *is* the ruling: a learner that has applied everything its leader has told it was committed is current by definition, and that is what the predicate establishes. It does **not** establish that the leader has committed nothing this peer has not heard of — which is what the round was for, so this is the weaker claim, and the code says so where it takes it. **(a) is not closed with it and is not a debt of its own**: a leader appending a no-op after a conf change removes the condition instead of reading past it, which is the better shape and a bigger change — it waits for whoever writes **ADR 0113**, by the same ruling. **And the bound's first size was wrong, which is worth carrying.** It was ten seconds, sized the way this repository sizes a readiness wait: generously, for the worst machine. The test then took `10.71 s` — the full bound, then the fallback — which says the round is not *slow* in this state but *unanswerable*, so every second was latency on a freshly placed learner's first fragment and bought nothing. Two seconds is two orders of magnitude over a round that can be answered. **What decides a bound's size is the nature of its fallback, not the word "wait"**: a readiness wait's fallback is a failure and wants room, this one's is a weaker answer and wants none. Now in LANE-RULES. · **Site** `crates/esker-store/src/server.rs` (`Store::catch_up`), `crates/esker-store/src/peer.rs` (`RaftPeer::read_index_as_learner`, `read_index_inner`), and `esker-raft`'s ReadIndex forwarding · **Size** **Small to name, unknown to fix, and the two candidate repairs are different decisions.** (a) *The leader appends a no-op after a conf change*, which is the ordinary Raft answer to “a new leader or a new configuration has nothing committed in the current term” and would make the ReadIndex answerable at once — it changes the log, so it wants an ADR. (b) *`catch_up` bounds its round and reads “nothing has committed since I joined” as caught up*, which is local and cheap and is the weaker claim: a learner that has applied everything its leader has committed is by definition current. Which is right depends on whether the ReadIndex is unanswerable or merely unanswered, and this row does not settle that — **neither is implemented** · **Owner** h1 · **Verified how** Four runs of #77's construction, 2026-09-12: three hangs at the same call and one pass with a single extra committed entry. Full timeline and the exact construction in `esker-coord/h2-77-bisect.txt` and `esker-coord/h2-77-construction-instrumented.rs.txt`; the reproduction is a two-store topology, `Operator::AddLearner`, and a fragment asked before anything else commits · Closed by `8f7909b0`, whose red test is that measurement turned into one: `snapshot.rs::a_placed_columnar_learner_answers_before_anything_else_commits` is the sibling test with a single statement removed — the row committed *after* the placement, which was the only thing keeping every existing test off this wait. With the fix reverted by a hunk swap it fails at 60 s in `within`'s own words, *"a wait with no end rather than a slow one"*. Beside it, `a_fragment_asked_past_what_the_cluster_has_is_still_refused` guards what the fallback could break: asked for a floor no entry will reach, the answer must still be `TooFarBehind` — the two paths compose, the fallback answering the *round* and the `min_apply_index` check after it refusing |
| 50 | ~~**A catalog read waited behind a DDL's commit, and was refused `40001`.** Every statement that resolves a relation reads the catalog's version counter — one key per tenant, plus the cluster's and a layout stamp — and every DDL writes it. A write is a Percolator lock, and the SQL layer takes **no node-local lock for a catalog key** (`crate::backend::Reach` appears nowhere under `catalog/`), so an ordinary `SELECT` on a table no DDL ever named met that lock, spent the client's whole resolution budget and was refused. Measured at **4.28 s then `40001 … could not be cleared for a read`**, deterministic, against three real stores. PostgreSQL does neither half: under READ COMMITTED a plain `SELECT` never raises `40001`, and an uncommitted DDL is invisible to other sessions. **The row's own premise was wrong and the test says so**: an *open* DDL transaction blocks nobody, because a catalog write is `txn.put`, which buffers — the window is the DDL's **commit**, which is a window every DDL opens on a key every session reads. Rails' `setup` drops and creates two tables per test, which is why run 114 met it.~~ | **h1**, and it was already done when this row was last read. `catalog::view_at`'s two counter reads stopped waiting and `refuse_an_older_layout` with them; `crates/esker-sql/tests/catalog_contention.rs` is the pair that says so — the open DDL that blocks nobody, and the commit that blocked everybody, the second going **4.28 s → 0.088 s**. Both tests pass on `db02ec8f`. `catalog::view_at`'s two counter reads and `refuse_an_older_layout`, and `catalog/record.rs`'s comment, which described this contention before anything measured it · small once it was named — one read that never waits, no wire change and no format change — and it took ADR 0104 §4's producer diagnostic to name it at all · h1 · `crates/esker-sql/tests/catalog_contention.rs`: the open DDL that blocks nobody, and the commit that blocked everybody; the second went 4.28 s → 0.088 s |
| 51 | ~~**A SQL node kept its own clock, so two of them handed out the same timestamps.** `CLAUDE.md` invariant 6 is *"timestamps come only from PD's TSO. No node uses its wall clock for ordering."* `esker-sql.rs`'s `connect` built `CountingOracle::starting_at(1)` **whatever `--pd` said** and handed it to the `TxnClient` that drives Percolator, so `start_ts` and `commit_ts` came from a per-process counter. For one node that is a correct oracle — monotonic, and the only source. For two it is the ordering gone: both count from one, and every MVCC decision in this system is made against that number (visibility, first-committer-wins, lock ownership). Measured before the fix, deterministically: **the right node read at 1 while the left node had committed at 2, so a committed write was invisible to the other node.** Found while building the durability tool, which needed a PD oracle of its own and made the absence of one visible.~~ | **h1**, and it was already done when this row was last read. `PdConn` became the node's oracle as well as its connection, so `start_ts` and `commit_ts` come from the driver's TSO; `crates/esker-sql/tests/two_nodes_one_clock.rs` is two connections to one driver, with the counterfactual — a counter per connection — handing both `[1,2,3,4,5,6,7,8]`. Both tests pass on `db02ec8f`. `crates/esker-sql/src/bin/esker-sql.rs:360` built it; `esker-client/src/txn.rs:265` (`start_ts`) and `:1360` (`commit_ts`) spent it; `backend/store.rs:171,386,404` spent it again for READ COMMITTED statement snapshots · small, once named: `PdConn` is already the node's connection to the driver, so it becomes the oracle too — one connection, both questions. No wire change; `PdReq::Tso` has been on the wire since phase 2 · h1 · `crates/esker-sql/tests/two_nodes_one_clock.rs`, two connections to one driver; the counterfactual — a counter **per connection**, which is what two nodes each keeping their own clock means — hands both `[1,2,3,4,5,6,7,8]` |
| 54 | ~~**Two computed catalog views join by cross product, so a statement over both scales with the square of the catalog.** Found by #49 (b)'s wall-clock acceptance test, which stayed red after the reads were gone: the second run of `pk_and_sequence_for` at an unchanged version reads **4 keys at 20 relations and 4 at 100** — flat — and takes **1.96 ms and 14.38 ms**. The read count is flat and the clock is not. Where the milliseconds are, per statement, 20 → 100 relations: `SELECT count(*) FROM pg_class` 164 → 639 µs (3.9×), `pg_attribute` 316 → 737 µs (2.3×), `pg_class, pg_namespace` 371 µs → 1.355 ms (3.7×) — all fine, a view's own materialisation is linear and `pg_namespace` is a handful of rows — and then `SELECT count(*) FROM pg_class seq, pg_depend dep WHERE seq.oid = dep.objid` **1.55 → 24.6 ms, 15.9× for 5× the catalog**. Five times the catalog is twenty-five times the pairs. `ActiveRecord`'s reflection writes exactly this join — `pk_and_sequence_for` is five catalog relations in one `FROM` list — so a schema load pays it per model, and it is the residue [ADR 0106](../adr/0106-what-a-statement-reads-below-the-sql.md) predicted in the sentence it was written to be refuted by: *if the other three quarters are not reads, none of the three shapes touches them.* None of them does.~~ | **`4375e520`, 2026-09-10** — *a condition a join step can answer belongs on the join, not above it* — with `36000ce3` turning its acceptance into a **count and a plan** and leaving the clock to print, which is what a shared box makes of a wall-clock gate. `catalog_read_slope::a_repeated_statement_stops_tracking_the_catalog` asserts the read count at both catalog sizes and that `EXPLAIN` names `Join Filter: (oid = objid)`, with a control that an honest cross product has no join filter at all — so the string cannot pass by being in every plan. Measured **1.19 → 2.74 ms** for five times the catalog where it had been **4.09 → 25.33**. Green on `db02ec8f`. the planner's join over the computed `pg_*` views — a catalog view has no key range, so `exec::query::join_node` builds no probe over it and the inner side is materialised and walked per outer row · **unmeasured**, and deliberately not guessed: what it needs is a probe over a materialised inner side, which is a planner change and not a catalog one. Sizing it means asking whether a hash probe over a computed view is worth building for the four or five relations a reflection query names · unassigned · `crates/esker-sql/tests/catalog_read_slope.rs::a_repeated_statement_stops_tracking_the_catalog`, **`#[ignore]`d and red** — it was #49's acceptance test and is now this row's, which is the one place the in-process node measures better than the real topology: a KV read there is a `BTreeMap` lookup, so what the clock sees is the planner and nothing else |
| 68 | ~~**A leader-kill test's setup is not idempotent after a `40003`.** `esker-sql`'s `statement_across_a_leader_kill` runs every statement through a `run()` helper that panics on any error, and the statement that fails is the **setup's** `CREATE TABLE` — before the test kills anything, on a natural election during startup: *"`CREATE TABLE t (id int PRIMARY KEY, v text)` failed with SQLSTATE 40003: the transaction's outcome is unknown: the `TxnPrewrite` may or may not have been applied: connection closed: region 1 stopped leading with this proposal in its log; it may still commit"*. Seen on b4's gate (`gate-b4-types-7c774e1c.red1.test.out`) and once in six rounds here. **Not a mechanical retry**: `40003` says the write may or may not have landed, so re-sending a bare `CREATE TABLE` can meet `42P07` instead — the setup wants `IF NOT EXISTS` with a retry, or a wait until the cluster has stopped electing before the first statement, and which of those is right is a decision about what the test means to assert. The product is not implicated: an outcome-unknown on a leader change is the honest answer (`crates/esker-store` says so where it makes it), and the same helper would panic on it in any file that copied this one.~~ | **h1, `567e5e5a`.** The setup became idempotent *and* retried, because neither is enough alone: `40003` says the write may already have landed, so resending `CREATE TABLE` meets `42P07` and the `INSERT` meets `23505`, while `IF NOT EXISTS` still has to be **sent again** after the connection that carried the first one closed. The retry admits four states and raises anything else. The discriminating half is **calling the fixture twice** — what a retry after an outcome-unknown does, and red at `42P07` without the fix; taking the leader away first does *not* discriminate, and the test says so rather than implying it. `crates/esker-sql/tests/statement_across_a_leader_kill.rs` (the `run` helper, and its callers in the setup) · Small · unassigned · The two failures above; single runs of the binary are green 6 of 6, so the reproduction wants the whole cluster set in parallel |
| 83 | ~~**A session's ending never ran on the ordinary endings, and a caught panic was all that said so.**~~ `Executor`'s destructor did the session's cleanup — roll the open block back, take the temporary schema with it ([ADR 0054](../adr/0054-a-temporary-table-is-a-relation-in-a-schema-that-belongs-to-one-session.md)) — and `Connection::run` leaves its loop **seven** ways, of which only three moved the bundle onto the blocking pool before dropping it: the two `terminated()` arms and the idle-in-transaction timeout. The other four are the *ordinary* endings — the client leaving, a `Terminate`, a message that will not decode, and every `?` — and they dropped it on the runtime thread, where every part of that cleanup is illegal: a rollback is a Percolator rollback and dropping a schema is a transaction of its own, so all three reach `BlockingTransport::call`, which **refuses on a thread driving a runtime**. **The panic in the log is a caught one.** That guard runs `block_on` inside `catch_unwind` and turns `tokio`'s refusal into `ProtoError::internal`; its own documentation says the message is printed by the default hook on the way past. So the node was never in danger — and the destructor read the refusal as `let Ok(mut txn) = self.begin_txn() else { return; }`, which made the whole ending a **silent no-op**. ADR 0054 does sanction leaving the schema behind, but for *"a backend that will not answer"*; here the backend answers perfectly and only the thread is wrong, so the degraded path had quietly become the normal one. **The trigger is not load.** The destructor returns at once unless `temp_schema` is `Some`, so only a session that made a temporary relation reaches any of it: run 128's 234,000-line node log holds **exactly one** `CREATE TEMPORARY TABLE` (`things`, 07:40:54.72Z) and **exactly one** such panic, 3.6 s later on the same connection, after its `DROP TABLE IF EXISTS "things"` — one temporary table in 178,138 statements, and it leaked. Load decides only *when* a connection ends. **A second, smaller instance came with it**: `Execute::release_advisory_locks`' own documentation said *"Every path out of `Connection::run` calls it"*, and that was false for two of the seven — a message that will not decode, and every `?` — which is an in-process lock held for the life of the process. | **Closed by the one ending.** `Execute` gained a **required** `close`, for the reason `terminated` is required and not defaulted: a default that does nothing reads as "this executor holds nothing" and is indistinguishable from an implementor that forgot. `Connection::run` now holds the bundle itself and the loop only borrows it, so **every** way out of the loop — including a `?` nobody has written yet — reaches one `spawn_blocking(\|\| executor.close())`; the three arms that did it for themselves lost their copies. Written as one exit for the reason `exec::wait_for_row` gives for its own: *"there are five of those and the two that were missed are the two nobody thinks of as a wait ending."* The destructor is now the **fallback** — for an executor that never belonged to a connection (a test, the CLI) and for the one connection case that cannot reach the ending, a statement's blocking task panicking, which drops the bundle on the blocking thread where this works. It **attempts the work and reports what came back** rather than testing the thread first: the obvious guard is `Handle::try_current().is_ok()`, and this repository has already measured that it cannot tell a driving thread from a `spawn_blocking` one — `BlockingTransport::call` used to be exactly that test and *"refused every caller that was doing the right thing"* — so skipping on it would skip the cleanup on the blocking pool, the one place the fallback still has to work. A failure is now a `tracing::warn!` naming the schema instead of silence. · **Site** `crates/esker-sql/src/pgwire/server.rs` (`Connection::run`), `src/exec/mod.rs` (`Executor::give_back`, `Drop`), `src/pgwire/session.rs` (`Execute::close`) · **Size** Small · **Owner** h2 · **Verified how** `crates/esker-sql/tests/a_session_that_ends_gives_back_what_it_held.rs`: three real stores, a real `Connection::run` over a duplex, `CREATE TEMPORARY TABLE`, then the client simply goes away — with a **denominator** beside it, because a session that made no schema would pass the assertion for free. No format and no wire message moved. |
| 82 | ~~**Nothing tested that a lower level still holding an older version stops a segment being dropped.** [ADR 0111](../adr/0111-a-deleted-keys-versions-are-dropped-as-one-segment.md) drops every version of a key whose newest version is a `Delete`, and condition (2) is what keeps that from **resurrecting** the key: if a level below the compaction's output still holds an older `Put`, dropping the delete above it makes a deleted key answer with a value again. The condition is implemented — `Picker::nothing_below`, the range form of `is_bottom_level_for_key`, asked over `key::version_range` because the timestamp suffix is complemented and an older version sorts *after* the delete that hides it — and its correctness is held up by **construction rather than by a test**: back the range query down to the point query and every test still passes. The two conditions that *are* pinned are (1), the newest version being a delete, and (3), a live reader below it.~~ | **h1**, the same day it opened, in two halves with a counterfactual each. **The predicate**: `compaction::picker::tests::a_range_sees_what_a_point_query_under_it_cannot` asserts that the point form answers `true` for a delete whose older version is directly beneath it and the range form answers `false` — back `nothing_below` down to the point query and it reddens. **The wiring**: `esker-engine/tests/a_segment_is_kept_when_something_is_below.rs` runs a real compaction with a filter carrying ADR 0111's segment rule and nothing else, over a database whose older version was pushed to the bottom first — back the closure in `db::compact` down to the point form and it reddens with the resurrection named. The compaction is the **background pool's**, because `compact_range` walks every level for its range and so always ends where nothing is below it: the condition cannot arise under it, which is why an L0 trigger of one is what makes this testable at all. Site: `crates/esker-engine/src/compaction/picker.rs`'s `nothing_below`, and `crates/esker-engine/tests/compaction_drops_the_dead.rs`, which is where #62's own drop rules are tested and where this belongs · Size: **Small, and it is a fixture rather than an idea.** The test needs a database with the delete's segment at one level and an older `Put` at a lower one, and a compaction that touches only the upper — which `compact_range` cannot express, since it walks every level. #62's file already builds levelled state by hand, so the shape is there · Owner: unassigned · Originally verified: The counterfactual has not been run: the claim is that the point query answers "nothing below" for a delete whose older version is directly beneath it, which is why the range form exists, and nothing today would notice if it were wrong |
| 73 | ~~**An unsuffixed integer literal is a `bigint` here and an `integer` there, and one message says so.** `INSERT INTO t (m) VALUES (1)` into an `enum` column is `42804 column "m" is of type mood but expression is of type **bigint**` on this node and `… of type **integer**` on 19beta1, measured 2026-09-11. It is not the enum's half — the *comparison* path already says `integer`, because `expr_type` reads the width off the value the literal holds, while the write path names `Datum::Int8`'s own type. The rule underneath is crate-wide and deliberate: `plan::Literal::Integer` is an `i64` and `Literal::assign` narrows it to whatever column it meets (`ARRAY[1,2,3]` is `bigint[]` here and `integer[]` there for the same reason), so this is one visible edge of a choice, not a local bug. **Not fixed**: changing an integer literal's default width is a decision about every assignment, every array constructor and every `RowDescription` in the crate, and it belongs in its own unit with a census of what moves. | `exec::assign::into_enum`'s `42804`, and `plan::Literal::Integer` underneath it · **Size**: unknown until the census — the message is one line, the width is a crate-wide rule · **Owner**: type surface · **Verified how**: measured on 19beta1 in a `BEGIN … ROLLBACK` beside the enum sentences it was found next to, and pinned as this node answers it in `tests/enum_type_mismatch.rs::the_other_refusals_still_name_what_they_always_named` — so the day it changes, a test says so.~~ | **Closed 2026-09-11, and the row's own sizing was the thing that was wrong.** It read the defect as a crate-wide rule — *changing an integer literal's default width is a decision about every assignment, every array constructor and every `RowDescription`* — and the census says otherwise: the width was already right everywhere that asked the **literal**, and wrong in the one place that asked the **datum**. `Literal::type_name` has carried the ladder since it was written (a bare constant takes the width its value needs, a written cast the width it was declared) and the comparison path has always used it; `enum_literal` turned every bare integer into a `Datum::Int8` — its own comment says so, ADR 0087 is why — and `into_enum` built the sentence out of that. **One fact, three readers, and the write path had the third.** The fix is that the literal's name travels with its value, and the message prefers a user-defined type, then the type the expression was *written* as, then the datum — the last still right for an evaluated expression, where the value's width **is** its type. **Site**: `exec::assign::enum_literal` / `into_enum`, with the name threaded through `into_column` and `assigned_value`. **Nothing persistent moved**: no `Datum`, no row encoding, no `ColumnType`. **Tests**: `tests/enum_type_mismatch.rs::an_integer_literal_is_named_by_the_width_it_was_written_as`, ten shapes measured on 19beta1 2026-09-11 and asserted verbatim, both through `INSERT` and through `UPDATE` — which reach the sentence by two different functions. **The `bigint` rows are the control**: renaming every integer `integer` passes the first shape and fails `1::bigint`; reading the stored width passes `3000000000` and fails `1::bigint`. Only asking the literal passes all ten. **Two corpus rows declared as divergences now agree and the harness said so** — it refuses to pass while a row is declared and matches — so `enum_value.rs`'s `VALUES (1)` and `SET current_mood = 1` entries are deleted. Capture: `tests/captures/pg19_integer_literal_width.txt`. |
| 79 | ~~**A scan answered with a prefix of its range and nothing said so.**~~ `txnkv::user_keys_in` collected at most `MAX_SCAN_LIMIT` **distinct user keys, live or dead**, and `scan` then read them at `ts` and threw most away — so the ceiling was spent on work rather than on the answer, and the two count different things by exactly the keys whose newest version is a `Delete`. A range accumulates those for ever (the newest version below the safepoint survives, which is what [ADR 0111](../adr/0111-a-deleted-keys-versions-are-dropped-as-one-segment.md) is about), so the gap only grows. Run 127 attempt 4: tenant 1 held **8,342** catalog table-record keys, **278** of them live, the 8,192nd at table id **34617** — and the five files that stopped the pass broke on tables at positions 8255, 8271, 8289, 8307 and 8325, every one past the ceiling, with `a name points at table 34755, which is not there` about a record that was on disk and readable. **Every number the caller could see said the answer was complete**: 278 pairs against a limit of 1,024 and 4 MiB of budget untouched. Two more holes were in the same shape — `scan` read a `limit` of **zero** as a literal zero and answered with nothing, where the wire has always meant "as many as the server will give" and `rawkv::scan` has always done that; and `esker_client::Transaction::scan` asked each region **once**, so a batch cut short by the byte budget ended the range. | **Found and closed 2026-09-11**, in the unit that disproved the collector hypothesis it was opened under (#78). **Fix (a), the coordinator's ruling of the two written up in the handover**: the ceiling counts **pairs returned**; a dead key costs an iteration step and no slot; `user_keys_in` became a `KEY_CHUNK` the walk asks for again, resuming strictly past the last key it consumed, in either direction. **Plus the client contract that closes what (a) alone cannot** — the caller's own limit, and a byte budget no caller can see: **a scan ends on an empty batch and never on a short one**. `Transaction::scan` and `RawClient::scan` page from the immediate successor of the last key they were given, across a region boundary as readily as inside one, and `limit = 0` means every pair in the range. The store's half of that contract is that it checks its byte budget **after** pushing a pair, so one live key always produces one pair. A scan that cannot finish — routing that does not advance, more than `MAX_SCAN_REGIONS` regions, more than `MAX_SCAN_CALLS` calls — is now an **error** where it used to be a short answer. **No wire change**; `docs/DESIGN.md` §9 carries the promise and records the alternative not taken (a resumption cursor, which would buy back the one empty round trip per scan that this spends). **Tests**: `esker-store/tests/a_scan_answers_with_a_subset.rs` (8,342 keys, 8 live above the old ceiling — the acceptance, `#[ignore]`d when it was opened and un-ignored by the fix) and `esker-client/tests/scan_pages_the_whole_range.rs` (9,000 live keys through a real store and a real wire; and forty 128 KiB values, five megabytes against a four-megabyte budget, so the batch is cut for a reason the caller cannot see), plus `esker-client/tests/retry.rs::a_scan_pages_past_a_short_batch_and_stops_only_on_an_empty_one`. **The census is in the handover**: every `.scan(` caller with its old and new termination rule. `for_each_page`, `cursor`, `verbs`, `job`, `flashback` and `bench_remote` already stopped only on an empty batch and each says why — the rule this ruling generalises was already the repo's, everywhere except the client itself. · **The alternative the row named is not closed with it: a resumption cursor on the wire, so a store says where it stopped instead of spending an empty round trip per scan. The user took it as option (b) and scheduled it for v1.2, 2026-09-12** — it is a wire change, and the rule this row bought (only an empty batch ends a range) is what makes the cheaper version safe to add later rather than necessary now. |
| 76 | ~~**A set operation one clause down never reaches the pass that resolves its arms' casts.** `Executor::resolve_user_cast` builds its list of projections from the *statement's own* select plus its `set_arms` (`exec::mod`, the loop #57 (a) widened), so a set operation inside a **derived table** or a **`WITH`** is not in it: the arm's `'sad'::mood` is never folded against the catalog, the column it produces carries no `user_type`, and the set is declared `smallint` and sends the **ordinals** where 19beta1 sends the labels. Measured 2026-09-11, and the contrast is what names the mechanism: `SELECT m FROM t UNION SELECT 'sad'::mood` answers `sad, ok` and `SELECT v FROM (SELECT m AS v FROM t UNION SELECT 'sad'::mood) s` answers `1, 2` — same arms, one clause apart. A `WITH` is the same. It is **#57 (a)'s walker shape one level further out**: that unit taught four walkers in `exec::bind` to enter a set operation's arms, and this is the one pass that has to enter a *derived table's select* as well. **It also blocks half of #57's own last reader**: while an arm's `user_type` can be absent because nobody resolved it, an absent one cannot be read as "not an enum", so `SELECT m FROM t UNION SELECT 1::smallint` still answers rows where 19beta1 says `42804 UNION types mood and smallint cannot be matched`. Refusing on that asymmetry before this row is paid turns statements that answer into `42804`s — tried, and the suite caught it. | `Executor::resolve_user_cast`'s projection list (`crates/esker-sql/src/exec/mod.rs`), which needs to reach `select.from[..].derived.select` and the joins' the way `expand_views` already recurses · **Size**: small-to-medium — one recursion, and the open question is whether a CTE's body is reachable at that point or has already been substituted by `plan::cte` · **Owner**: type surface · **Verified how**: measured on 19beta1 in a `BEGIN … ROLLBACK` (both nested forms answer the labels and `pg_typeof(v)` is `mood`) and on this tree the same hour; the red test is `tests/set_operation_enum.rs::a_nested_set_operation_loses_its_arms_user_type`, `#[ignore]`d with the control assertion beside it — removing the attribute **is** the acceptance, for this row and for #57's remaining half.~~ | **Closed 2026-09-11 by the recursion the row asked for.** `Executor::resolve_user_cast` now visits **every set operation in the statement**, wherever it is written — in `FROM`, in a join, in a `WITH` item, in the recursive term of one, inside an expression subquery, or inside another arm — each resolving its own arms under its own `printed` rule. It is a `dyn` closure because it recurses. **A nested select with no arms is descended into and not visited**: this row is about the arms, and the projection rule for a plain sub-select is a different question with no measurement behind it. An arm is descended into but visited only when it has arms of its own — resolving its projection twice would meet an already-folded expression and record no type for it, which is the defect rather than the fix. **Site**: `crates/esker-sql/src/exec/mod.rs`. **Size**: small, as sized — one recursion and two helpers; the CTE question the row left open answered itself, because a `WITH` item is already a derived table in `Select::ctes` by the time this runs. **Re-measured on 19beta1 2026-09-11** in one `BEGIN … ROLLBACK` with a savepoint per statement so that no refusal swallowed the rest: both nested forms answer `sad` then `ok` and `pg_typeof(v)` is `mood`. **Tests**: `tests/set_operation_enum.rs::a_nested_set_operation_loses_its_arms_user_type` lost its `#[ignore]` and gained the two `pg_typeof` assertions — the half the rows alone do not prove, because a node printing a label it does not know the type of passes on rows — and `::a_set_operation_reached_through_two_clauses_keeps_its_type` is new and is why the walk recurses rather than stepping one level. Counterfactual: the recursion taken back out is red on both at `1 ; 2`. **No format change**; `user_type` is a planning-time slot. **What it unblocked, built in the unit after this one**: #57's remaining half — with an arm's absent user type no longer ambiguous, an enum beside another category is refused as 19beta1 refuses it, and two of the four shapes had no sentence at all before (they answered the ordinals). See #57's own row. |
| 75 | ~~**An unknown literal in a set operation's arm never takes the other arm's type — three of the four rules are not built, and the capture that measured them was never pinned against the node.** `tests/captures/pg19_set_operations.txt`'s header lists what 19beta1 does with an arm that has no type of its own: `SELECT 'lit' UNION ALL SELECT t FROM …` is `text`, `SELECT NULL UNION ALL SELECT 1` is `integer`, `SELECT NULL UNION ALL SELECT NULL` is `text`, and a literal that cannot be read as the resolved type fails **as that type** — `SELECT 1 UNION ALL SELECT 'abc'` is `22P02 invalid input syntax for type integer: "abc"`, not a mismatch. Measured on this tree 2026-09-11: **only the first works**, and it works by accident because both arms are `text` anyway. The other three are all `42804 UNION types … cannot be matched` — `integer and text` for `SELECT 1 UNION ALL SELECT 'abc'` and for `SELECT 1 UNION ALL SELECT NULL`, `text and integer` for `SELECT NULL UNION ALL SELECT 1`. **A capture is not a test**: that header has sat beside `tests/set_operation.rs` since 2026-09-05 asserting four rules the file never checks, and the hand-written tests beside it pin only the pairs where both arms already have a type. Found while measuring #57's last reader, because an enum makes it visible — `SELECT m FROM t UNION SELECT 'sad'` answers `sad, ok` there and is refused here — but **nothing about it is the enum's**: every shape above is two built-in types. | `exec::query::append` (an arm arrives as a planned `OutputColumn` with a settled `ty`, and an unknown literal's is already `text` by then) and whatever would have to retype the arm's projection before it — `coerce_arm` is where the cast per arm is already built · **Size**: medium — the rule is one sentence but the seam is not there: an arm is planned independently of its neighbours, so `unknown` has to survive planning as something other than `text`, which is the same vocabulary question `Literal::String` carries everywhere else · **Owner**: type surface · **Verified how**: measured on 19beta1 in a `BEGIN … ROLLBACK` and on this tree the same hour; the three divergences are pinned as this node answers them in `tests/set_operation_enum.rs` (`one_enum_across_the_arms_still_answers` and `the_ordinary_set_operation_refusals_are_what_they_were`), with 19beta1's answer written beside each — so the day one closes, a test says so.~~ | **Closed 2026-09-11, the built-in half, and the enum half is named rather than left implied.** `OutputColumn` gains one `bool` — whether the column is a literal PostgreSQL would call `unknown`, the one thing about it that cannot be read back off its type, since this node has made it a `text` by then. It is read in exactly one place: `exec::query::append`. An unknown arm **does not vote** in the unification, is **exempt from the implicit-cast check** — a literal is *parsed* as the settled type, never converted to it — and is then coerced by the cast `coerce_arm` already inserts, which is what turns `'abc'` reaching an `integer` into the `22P02` about the **value** rather than a `42804` about the pair. Every arm unknown leaves the column `text`, which is 19beta1's answer and was already this node's. **Site**: `crates/esker-sql/src/exec/query.rs`. **Size**: the row said medium and the seam turned out to be one field and two branches — the vocabulary question it feared is answered by carrying the fact rather than by making `unknown` a type. **Re-measured on 19beta1 2026-09-11**, savepoint per statement: `'lit' ∪ t` is `text`; `NULL ∪ 1` is `integer`; `NULL ∪ NULL` is `text`; `1 ∪ NULL` answers both rows; `1 ∪ 'abc'` is `22P02 invalid input syntax for type integer: "abc"`. **Tests**: `tests/set_operation.rs::an_unknown_arm_takes_the_other_arms_type` (rows and `pg_typeof` for each rule) and `::a_real_mismatch_is_still_a_mismatch`, which is the other side — a cast is not unknown and keeps its vote. The divergence pinned in `set_operation_enum.rs::the_ordinary_set_operation_refusals_are_what_they_were` **closed and the test that pinned it said so**: it was written as this node's `42804` with 19beta1's sentence beside it, and the fix made the two the same sentence. Counterfactual: the flag always `false` is red on both. **The enum half is not built and stays pinned**: reading `'sad'` as a `mood` means looking a label up in the catalog and `append` has no view — the arms are planned by the time they reach it — so `SELECT m FROM t UNION SELECT 'sad'` keeps this node's `42804` where 19beta1 answers the labels, with that answer beside it and the guard saying so where it is made. The same capture found one more of the family that no row had: `SELECT m FROM t UNION SELECT 'nope'` is `22P02 invalid input value for enum mood: "nope"` there. |
| 78 | ~~**The collector let a `Kind::Lock` record stand in for the version underneath it, and collected the version.**~~ `MvccCollector::filter` special-cased exactly one non-version, `Kind::Rollback`, and sent everything else through `keep_as_newest` — which answers *"is this what a read at the safepoint returns"*. `Kind::Lock` stopped being reserved at [ADR 0088](../adr/0088-a-row-lock-across-nodes.md): a committed `Op::Check` writes one, which is what a SERIALIZABLE transaction leaves on its validated read set ([ADR 0062](../adr/0062-serializable-is-snapshot-isolation-plus-a-validated-read-set.md), [ADR 0067](../adr/0067-the-check-mutation-and-the-latest-commit-question.md)) and what `SELECT … FOR UPDATE` leaves on every row it locks. So a key could carry `Put@c1` with `Lock@cL` above it; below the safepoint the filter met the lock first (versions arrive newest first), kept **it** as the newest, and the `Put` underneath was then older than what had been kept and was **removed**. The read side steps past a lock looking for a version (`esker_txn::percolator::newest_version_at`), found nothing, and the key read as absent. **A key nobody validated kept its value; a key some transaction only *read* lost it.** | **Found and closed 2026-09-11 in one unit, by a hypothesis the coordinator wrote from reading `gc.rs` against `percolator.rs`.** The fix: no record with `!kind.is_a_version()` enters `keep_as_newest` — a rollback marker keeps its `start_ts` rule (below it an in-flight `Prewrite` can still arrive with nothing else to stop it), and a lock record goes on its own `commit_ts` at the same boundary, because nothing reads it and keeping one record more is the side that costs space rather than rows. No format change, no wire change. **Site**: `crates/esker-store/src/gc.rs`'s `filter`. **Size**: small — six lines and the module doc. **Tests**: `crates/esker-store/tests/a_lock_is_not_a_version.rs`, two, both red on the commit before the fix — `a_key_held_by_a_read_keeps_its_value_through_a_collection` drives a real store through prewrite/commit, `Op::Check`, a safepoint above both and `collect::Sweeper`'s own flush + `compact_range(write)`, and read back `None`; `no_read_above_the_safepoint_changes_its_answer_when_the_collector_runs` enumerates **every** history of one to four records over all four kinds — 340 of them — against seven safepoints and asserts the property decision 5 makes the right one (*every read at or above the safepoint gives the answer it gave before*), failing first on `[(20, Lock), (10, Put)]` at safepoint 25. Exhaustive rather than sampled, so there is no seed under which it passes. [ADR 0111](../adr/0111-a-deleted-keys-versions-are-dropped-as-one-segment.md) gained a section for the same confusion one design over: its condition (1), *"the newest record in the segment is a `Delete`"*, is about the newest **version** and its streaming shortcut has to step past the non-versions at the head of a segment. **It was opened as the diagnosis of run 127 attempt 4's P0 and it is not that P0**: the preserved data directory has no `Kind::Lock` record anywhere — node-1's `write` family is 34,440 records, 3,021 `Put` and 31,419 `Delete`, and nothing else — and table 34755's record is present and readable. The defect above is real, reachable from SQL, and was fixed on its own evidence; what broke run 127 is the scan ceiling recorded beside this row. |
| 57 | ~~**A cast to a user-defined type is resolved before planning, and two things about that are wrong.** `'sad'::mood` is carried as a `CatalogFunc::UserCast` with the placeholder type `ColumnType::Int2` (`plan::expr`) until `Executor::bound` folds it against the catalog (ADR 0053) — the row evaluator has no catalog, so the pass is right to exist. What is wrong is where it reaches and what it leaves behind. **(a) The walkers it runs through never entered a set operation's arms**, so every pass in `bound` was blind to every arm but the first and each said so from the row evaluator: `XX000` for `::regclass`, for `current_schema()`, for `current_database()`, for a cast to a user type and for a `regtype` over one — five, measured, for statements whose *first* arm answers the same expression perfectly — plus a parameter in an arm that was neither typed nor substituted, and a relation only an arm names being invisible to both. **(a) is paid, `9f6774af`**, in four walkers written as two pairs the way `exec::bind`'s own comments require, with a red test each. **(b) is the row.** A resolved enum cast is a bare `Literal::Typed(Datum::Int2(ordinal))`: the type's *name* survives in exactly one place, `plan::SelectItem::user_type`, which is a slot the **projection** has and no other position does. So an enum cast outside a target list has no identity, and everything that types two operands against each other refuses it. Measured on this tree, `mood = ('sad','ok')`: `WHERE m = 'sad'::mood`, `HAVING`, `IN (…)` and `>` are each `42883 operator does not exist: mood = smallint`; **`INSERT INTO t VALUES ('sad'::mood)` and `UPDATE t SET m = 'sad'::mood` are `42804 column "m" is of type mood but expression is of type smallint`** — a *write* refused, which is the half a reader does not predict from the comparison. `SELECT 'sad'::mood` and `WHERE m = 'sad'` (unquoted, coerced by `reconcile_enum`) are right, which is why no corpus caught it. And in a set operation the same gap answers the *values* correctly while declaring `smallint`, because `query::append` unifies a `user_type` neither arm carries.~~ | **Paid in two units, 2026-09-11** — the notes above are the payment itself, written as they were measured. **Site**: `exec::mod::resolve_user_cast` and `Executor::user_cast` (the fold), `exec::query::reconcile_enum`'s `enum_of`, which recognises an enum **only** as `Expr::Ordinal` through `Scope::enum_at`, and `plan::SelectItem::user_type`, the one slot a user type can live in · **Size as opened**: **medium, and it is ADR 0050's unfinished half rather than a walker gap** — measured by trying the three cheap routes and rejecting each: (1) fold to `Literal::String(label)` outside a projection, which makes `mood = 'sad'::text` answer where a real server says `42883` — the distinction `reconcile_enum`'s doc comment is written around; (2) let `reconcile_enum` accept any `int2` literal against an enum column, which makes `m = 2::int2` answer, the same trade one step worse; (3) carry the type on the node — the only correct one, and it needs a vocabulary the plan does not have: `ColumnType` is a closed enum of *storage* types and cannot spell `mood`, so it is a new `Expr`/`Literal` shape carrying a `catalog::TypeDef` (or its oid) plus the readers that ask an expression its type. `Datum::Geometry` and `Datum::Bit` are the precedent for a value that carries its own type (ADR 0050); a `Datum` variant is the expensive version of that and would reach `esker-keys` and the row codec, which this row does **not** propose · **Owner**: type surface · **Verified how**: measured shape by shape on this tree, 2026-09-11, with the five `XX000`s and the six `42883`/`42804` statements written out above; the set-arm half is pinned by `tests/set_operation.rs::a_set_arm_gets_the_same_resolution_the_first_one_does` and `::a_parameter_in_a_set_arm_is_typed_by_its_own_arm`, each red under the removal of the walker it names. The enum half has **no test yet**: a red one would be the first thing the fix needs **(b) is paid too, 2026-09-11, and by the route this row named.** `plan::Literal::Typed` became `{ value, user: Option<u64> }` — the datum it always carried plus **which user-defined type it is a value of**, because `ColumnType` is a closed enum of storage types and cannot spell `mood`. Making the tuple variant a struct one is the point: the **compiler named all 91 sites** rather than leaving the ones that pattern-match `Literal::Typed(..)` to miss a new variant in silence — the hazard this row's own sizing described, and the shape h1's `Option<Hydrated>` used one debt over. Three readers take the fact: `reconcile_enum` lets a cast to *this* enum through (the ordinary path then compares two ordinals); `assign::enum_literal` carries it to `into_enum`, which accepts an `Int2` **only** when the oids match, so `VALUES (1)` is still the `42804` the measurement named; and `resolve_user_cast`'s projection loop reaches a set operation's **arms**, resolving them to the ordinal rather than the printed label so `query::append` unifies `mood` with `mood` — the union sends the enum's oid and the labels, read off the `RowDescription`. **Neither `ColumnType` nor any stored byte moved.** Evidence: `tests/enum_cast_identity.rs`, three tests and a counterfactual each (the carried oid reddens the comparison and the write, the arms loop reddens the set operation), with what must not move pinned beside them: `m = 'sad'::text` is still `42883 … mood = text`. **What is left is the bind half and it is a different mechanism**: `Executor::described_in` never runs `resolve_user_cast`, so at `Describe` a `$1::mood` is an unresolved call declaring the `int2` placeholder and the comparison is refused before a value is bound — wire v3 family F11's shape one pass over, `#[ignore]`d in the same file. Calling the pass there does not fix it: it would meet `substitute_placeholders`' empty `Datum::Text` where the label goes and answer `22P02` for a statement that runs. **A third gap found beside it, not this row's**: a derived table loses a column's user type — `SELECT pg_typeof(v) FROM (SELECT m AS v FROM t) s` is `smallint`, with no set operation in sight. **And the bind half is paid the same day, `f0d67650`, with the third and fourth shapes beside it — the row is closed.** `Executor::described_in` never ran `resolve_user_cast`, so at `Describe` a `$1::mood` was an unresolved call declaring the `int2` placeholder and the comparison was refused before a value was bound. It runs there now, **after `bind::infer` has counted the parameters** (replacing one earlier would make a statement with a `$1` describe none) and **before the placeholders go in** — and a `$n` under an enum cast becomes a stand-in *of that enum* (`describing_stand_in`) rather than of the representation, which is the trap the earlier note named: resolving the placeholder itself reads an empty `Datum::Text` as a label and answers `22P02` for a statement that runs. **The parameter is declared as the enum too**: `user_typed_parameters` reads the statement as written and overrides the frame's oid, because `bind::infer` speaks in `ColumnType`s and none of them spells `mood` — 19beta1's `pg_prepared_statements.parameter_types` is `{mood}` for all three shapes and this node sent `text`'s 25. And `DEFAULT 'sad'::mood`, which looked like the shape the literal fix could not reach — a stored default is a value by the time `exec::ddl` sees it — turned out to be covered, because a default is evaluated through the same assignment path an `INSERT` takes; measured rather than assumed, and pinned. Six tests, four counterfactuals, and **the derived-table gap stays open and is not this row's**: `SELECT pg_typeof(v) FROM (SELECT m AS v FROM t) s` is `smallint`. **Both of those are paid the next day, `9c699b8d` and the sentence beside it.** The derived-table gap was one builder: `exec::subquery::plan_derived` writes the synthetic `TableDef` a sub-select looks like from above, and it set `user_type: None` on every column and left the `enums` map empty — so a `WITH`, a view and a `FROM (SELECT …)` all lost the type, one fix for all three because `plan::cte` and `expand_views` rewrite to the same node. And the **sentence**: `Literal::Typed::user` became the `catalog::TypeDef` rather than its oid, because an oid settles identity and two readers need a *name* — `42883 operator does not exist: mood = other_mood` and `42804 … but expression is of type other_mood`, both measured, both answered `smallint` here. `SqlError::DatatypeMismatchInColumn::expression_type` is a `String` for the same reason `column_type` already was. **A wrong answer fell out of measuring the sentence**: `reconcile_enum` let *two* enum columns through to the ordinary path — both are `int2` ordinals in the row — so `SELECT m = n FROM t` over two different enums answered `f` where a real server refuses. Two enums of the same oid still compare; that is what the ordinal storage is for. Evidence: `tests/derived_user_type.rs` (three wrappers x two modes, two counterfactuals) and `tests/enum_type_mismatch.rs` (four tests, three counterfactuals). **And the last shape landed 2026-09-11, once #76 made it writable.** An enum beside another category is two categories, so a real server refuses with `42804` and names the **enum** — and two of the four shapes had no sentence here at all, because an enum is an `int2` in the row and a node unifying representations answered the ordinals: `SELECT m FROM t UNION SELECT 1::integer` was `integer  1 ; 2`. What had stopped it was that an absent `user_type` was ambiguous while `resolve_user_cast` reached only the top-level arms; #76 removed the ambiguity and this became the one line `unify_user_type`'s own comment had promised. The rule is **enum-only** and two tests say so — a domain is named by its base and still unifies with it, a range is already its own `ColumnType` — and a third covers an enum arriving through `max()`. Evidence: `tests/set_operation_enum.rs::an_enum_beside_another_category_is_refused_and_the_enum_is_named`, `::a_domain_and_a_range_keep_the_behaviour_they_had`, `::an_aggregate_of_an_enum_is_still_that_enum`, all four sentences re-measured on 19beta1 2026-09-11. **What is still open beside it is #81**, not this row: an unknown literal beside an enum is refused here and answers there. **One divergence recorded rather than fixed**: `INSERT INTO t VALUES (1)` into an enum column says `expression is of type bigint` where 19beta1 says `integer` — an unsuffixed integer literal is an `int4` there and an `int8` here until a column says otherwise, and the *comparison* path already says `integer`; it is the literal's width and not the enum's identity. |
| 62 | ~~**A full compaction dropped nothing, because it never read anything.**~~ `esker admin gc --safepoint <max>` followed by a compaction of every column family left the `lock` family at **3,546 entries out of 3,546** while the `write` family went 1,773 → 604, and a thousand-round in-process probe walked +22 more stored entries per round for ever. Neither of ADR 0110's two suspects was the cause: the tombstones were not held down by a snapshot (none was open), and they *had* reached the bottom level — they got there without being read. `Compaction::is_trivial_move()` re-labels a single non-overlapping input as belonging to the next level with one manifest edit and no bytes, which skips `compaction::job` and therefore all three of its drop rules at once. The shortcut already stood down for a compaction filter and for range tombstones; point tombstones were the third reason nobody had named. The two families are the proof: `write` carries the MVCC collector, so the shortcut was never available to it, and it collected identically before and after the fix. | **Closed by `ff1fd2eb`**: a compaction whose output is the last level rewrites instead of moving, which is the standard rule — the move stays free through every level above the bottom, and the file is read once on arrival, where a point tombstone finally has nothing beneath it. The last level rather than "nothing overlaps below", which would also fix it but rewrites at *every* level of an otherwise empty family; tightening further wants a tombstone count in `FileMeta`, and that is an on-disk format change. A/B on one command, `ROUNDS=200 CHECKPOINT=25 COLLECT_AT=100`: identical to the digit through round 75 (851 / 1,400 / 1,950 / 2,500 steps), then at the collection `lock` **3,746 → 0** against 3,746 → 3,746, steps **388** against 3,050, and 36.2 ms against 44.2 ms at round 200 — with `write` unmoved in both arms as the control. What the probe still does *not* do is go flat between collections, and it should not: it publishes no safepoint by construction, which is ADR 0110 step 2. Site `crates/esker-engine/src/db/compact.rs`; size Small; found by #58 round 3 (`esker-coord/h1-58-round3.md`), answered in `esker-coord/h1-62.md`. Tests: `crates/esker-engine/tests/compaction_drops_the_dead.rs` (three, one of them the counterfactual that a snapshot still sees what it could see), `tests/model.rs` already covering it as the property kind, and `tests/crash_compaction.rs` — the crash kind §11 asks for, which this path had none of while it was a manifest edit. |
| 65 | ~~**The public sweep reclaimed no objects at all, including the one at open.**~~ `Db::purge_obsolete_files` listed the directory and deleted the local files no live version named, and stopped there — so the *object* of every file it deleted stayed in the bucket for good. A listing cannot drive object reclamation in the first place, which is [ADR 0024](../adr/0024-tiering-failure-semantics.md) decision 5's whole point: a file the tier has evicted is not in the listing at all. `Db::open` calls this sweep, and it is the **only** sweep that runs before any flush or compaction can, so the garbage the last process abandoned was exactly the garbage that never went. Invisible until #62, because a bottom-level compaction used to move its input rather than obsolete it; the first run after the fix left objects **{4, 6, 8, 10, 12, 14}** in a store whose version named **{15}**. | **Closed by `6ac90a59`**: the public entry point calls `purge_and_evict`, the same sweep the flush, compaction, ingest and drop-cf paths run, with its `writable` guard still in front so a read-only handle refuses as before. Found while fixing #62, and numbered 65 because r1 holds 63 and 64. Site `crates/esker-engine/src/db/open.rs`; size Small; test `crates/esker-engine/tests/tier.rs::the_public_sweep_reclaims_objects_and_not_only_files`, which pins an open iterator across the compaction so the compaction's own sweep correctly leaves its inputs alone and the public sweep is provably the next one to run. |
| 66 | ~~**A compaction's own sweep could not reclaim the compaction's own inputs.**~~ `run_compaction` swept before returning — from inside a call whose caller still held the `Arc<Version>` the compaction had been computed from. A pinned version's files are live by definition, so that sweep looked at the inputs of the compaction that had just finished, found every one of them named, and reclaimed nothing; the files went one sweep late, whenever the next flush or compaction happened to come along. Invisible while arriving at the bottom level was a trivial move, because a move obsoletes nothing. The symptom after #62: `esker admin gc` compacts, reports 201 entries, and leaves 202 on disk — an operator who runs the verb and then measures the directory is the one who sees it. | **Closed by `5bbfc8ac`**: the sweep moves out to both callers — `Db::compact_range`'s loop and the background `maybe_compact` — and runs after `drop(version)`, in the same order in both: release the reservation, let the version go, sweep. Site `crates/esker-engine/src/db/compact.rs`; size Small; test `crates/esker-cli/tests/admin_storage.rs::a_safepoint_an_operator_handed_over_comes_back_with_a_receipt`, which already asserted that the receipt's total is the total `sst-dump` reads back and was the thing that went red. Found while fixing #62, alongside #65. |
| 67 | ~~**Twelve real-process test files picked their ports from a fixed band, and collided with each other.**~~ Each scanned a band of its own — 30,100, 31,100, 32,100, 33,100, 34,100, and 41,000 for two files at once — by binding every port of a run, checking they all bound, **dropping the listeners** and returning the base. The comment each carried said a private band made the release-then-bind race harmless *between test binaries*, which is true and is not the case that bites: `nextest` gives every **test** its own process, so two tests of one file are two processes walking the same band from the same end, and both start at its first port. One of them loses: `esker server: listening on 127.0.0.1:34100: io error: Address already in use (os error 98)`. Four gates in `esker-coord/`'s archive died on that line — two of this lane's and two of b4's — always in a band's first ports, always looking like a load flake. | **Closed by the allocator two of these files already had.** `cluster_chaos.rs` and `tier_acceptance.rs` had found it, fixed it and written the test that names it; the other twelve never adopted it, which is the shape a measured rule reaching only its first caller always has. Their reasoning is now `crates/esker-cli/tests/port_band/mod.rs` and all fourteen use it: the run is **held** in the returned value and released on the line before the spawn, the scan **starts at a slot the clock and the pid pick** rather than at the bottom, and the range sits **below 32,768** so no outgoing connection of the suite's thousands can take a port while nothing holds it — an intermediate fix that bound `127.0.0.1:0` and released passed two rounds and failed the third for exactly that reason. Test: `crates/esker-cli/tests/ports.rs`, whose first assertion prints the gate's own failure against the old allocator — *"two reservations held at the same moment overlap: 34100..34104 and 34100..34104"*. **A second face was underneath**: with the ports fixed, the same test failed on `removing member 4 would leave 2 live of the 3 a quorum needs` — the liveness check in `pd/mod.rs` whose own comment says *"the operator retries a second later"*, and the test was the operator that did not. It retries now. **`esker-sql` and `esker-store` are deliberately left alone**: their real-process tests bind `127.0.0.1:0` rather than a band, so they have the narrow release-to-bind window and not the deterministic collision — closing it would mean sharing `port_band` across crates, and the ruling (2026-09-11) is to move them only if a gate goes red the same way. Size Small; found by the gate on `042a3036`. |
| 61 | ~~**`DROP TABLE` reads the whole catalog.**~~ Dropping one table costs one `KIND_TABLE` read and one `KIND_SEQUENCE` scan **per relation in the database**, because it materialises the whole relations view: `Catalog::relations()` memoises `pg_relations::Relations::read`, and `DROP TABLE` needs it. Measured on a real in-process store with 150 background relations: `reads 160 = catalog 't' x152, …` and `scans 158 = catalog 'q' x152, …` — 152 of 160 and 152 of 158 are the per-relation walk. An empty schema measures **10** reads, which is how the dependence on catalog size was found. This is r1's 277 reads a statement (run-127c shape diff): **not a property of the statement, a property of the catalog**, and at 294 relations it is the right order. It matters because `DROP TABLE IF EXISTS` is **73% of a Rails file's statement time and 87% of the file's growth** — the most expensive shape in the suite, by a distance, and the amplification is a constant factor of ~150× that no read-path fix reaches.  Note it is memoised **per catalog view**, so it is once per statement and not once per dropped table; a multi-table `DROP` pays it once. | **Closed by `21654f97`**, the narrowed view — the third of the three options, and the only one that changes no on-disk format: a dependency index and a reverse-reference record are both new persisted kinds. `Catalog::matviews()` reads the same table records as **one scan** of the range `table_key` has always written, decodes each without hydrating it, and keeps the matviews; `record::table_range` is read-side only, so `CATALOG_FORMAT_VERSION` does not move. At 150 relations: **reads 160 → 9, scans 158 → 5, 81 ms → 12 ms**, and what is left is one scan and one point read of the table being dropped. Semantics unchanged and the corpora are the judge — matview, view, view_debts, ddl, ddl_cascade_fk_trigger, drop_column, drop_sequence, drop_constraint, foreign_key_drop_cycle, updatable_view, pg_class, pg_catalog, plus the crate's whole suite at 2191. Acceptance: `crates/esker-sql/tests/ddl_cost.rs::dropping_one_table_does_not_read_every_relation` — under 20 reads at 300 relations, **with a denominator assertion beside it**, because the first version passed against the unfixed code with every counter switched off. Site was `catalog/mod.rs` (`relations`) and `catalog/pg_relations.rs` (`Relations::read`); size Medium; found by #58 round 2 (`esker-coord/h1-58-round2.md`). |
| 59 | ~~**A store's open treated a placement driver that could not answer as fatal.**~~ | **Two faces, both mine, both fixed.** *First* (`191f07c9`): a three-member driver group has no leader for the first second or two of its life and answers `PdNotLeader` to everything, and `Store::open` exited on it — so a one-second election became a dead node, but only on a machine slow enough to lose the race. Four ADR 0108 tests failed on a loaded gate and were green on a quiet one. The fix is a **startup budget that nests inside** `esker_proto`'s redirect budget (`StoreOptions::pd_leader_wait`, 30 s): three seconds is right for *finding* a leader and wrong for *waiting for the first one*, and raising the shared budget would slow every later redirect on every client. *Second* (`f1f8b52f`): that fix waited on an unreachable member only **after** some other member had answered, on the reasoning that a refused connection must be a mistyped `--pd`. It is also what a store sees when a driver's process is up and its **port is not listening yet** — the same wire error, nothing inside the window separating them — so the narrow reading was taken whenever the store won the race, and a race is decided by load. Two of three gates went red at 2.4 s. Both refusals now wait under the one budget; the cost is stated rather than hidden (a genuinely wrong `--pd` fails after the budget, and the message names the endpoint and distinguishes *no driver answered* from *none led the group*). Closing measurement: the four ADR 0108 tests under a fourteen-loop arm at **load 9.4 → 12.1**, the band the reds were seen in, 2–15 s each against 22.4 s timeouts. Two of those tests now **keep their children's output** — `exit status: 1` does not say why, and discarding it cost a night of guessing. `crates/esker-store/tests/store_open_waits_for_a_pd_leader.rs` (5) and `crates/esker-cli/tests/store_starts_before_its_driver.rs`, whose **first version passed against the broken rule** because it held the driver back by a clock instead of waiting for the store to say it was waiting. ADR 0108 carries both rules and what the narrow one cost. |
| 53 | ~~**A peer that was promoted came back believing it is a learner, and never campaigned again.**~~ run 123's census, four peers of one region at the same `epoch_conf=7`: stores 1, 2 and 4 all read `core_voters=[2, 9, 10, 13] core_learners=[]`, and store 3 read `core_voters=[2, 9, 13] core_learners=[10]` — it called **itself** a learner. Its own census dates the flip to the line: seventy-three rounds of the right answer, `node 3 exited with signal: 9`, `node 3 restarted`, and the first census sixteen milliseconds after `store opened` is the wrong one, for the remaining hundred and thirty-one rounds. A voter that reads itself out of the voters does not campaign (`is_voter(self)`), so a group of four silently has three candidates — and nobody is told, because every *other* peer's view is right. | **Opened and closed on 2026-09-10 by h1 (`4bc66122` on `txn-conflict`), and it never sat in §1** — the second row this week to be paid the day it was written. **The mechanism is one record answering the wrong question.** `applied_conf` is documented as the membership *as of the apply index*: the value a compaction records as the log's new anchor and the value a snapshot names. `RaftPeer::start` rebuilt it from the Raft state record's `conf_state`, which is the membership as of the **truncated** index — a different index, and deliberately so, because the core replays the log's conf-change entries on top of it. So every restart dropped from `applied_conf` every conf change applied since the last truncation. The **core** stayed right, which is why the first two restarts of store 3 looked clean; the stale value was written back to disk by the next compaction, and from then on the entries that said otherwise were gone and nothing could correct it. · **The arithmetic is the proof**: store 3 was killed three times, peer 10 was promoted at 22:16:11 and peer 13 at 22:18:11, and a stale `[2, 9] + learner 10` with 13's promotion applied on top of it is `voters=[2, 9, 13] learners=[10]` — the field's line to the digit. · **The fix that was recorded first would have been a regression**, and saying so is half the row: persisting the membership *in force* into `PersistedState.conf_state` leaves those changes folded into the core's base, so a leader truncating them away can no longer revert them — the two-leaders-in-one-term the simulator found on `ESKER_SIM_SEED=42705`, which is why that field is specified as it is. The record that answers *the membership as of the apply index* is the **region record**, written by the very batch that carries that apply index, and it was already in the peer's hand: `host_region` computes it and passes it in. `crate::region::membership` is now its one reader, for the core's bootstrap configuration and for `applied_conf` both, so the two cannot answer one question differently. · **Site**: `crates/esker-store/src/peer.rs` (`RaftPeer::start`), `crates/esker-store/src/region.rs`, `crates/esker-store/src/server.rs` (`start_peer`) · **Size**: small once named — one line moved and one reader made shared · **Owner**: h1 · **Verified**: red first, on a real store and its own database, in `crates/esker-store/tests/membership_across_restart.rs`. Two tests, and both failed at their assertion rather than at their harness: the membership the peer *names* after a restart (`[1]` against the record's `[1] + learner 2`), and the field's own chain — restart, ordinary writes, a compaction past the change, restart — which leaves the **core** at `[1] + no learners` against a region record that says otherwise, with nothing left in the log to correct it. Counterfactual: restore the one line that read `storage.state().conf_state` and both go back to red. The whole `esker-store` suite is 353 passed. **Not accepted on the real topology yet**: run 125 (run 123's recipe) is where the census has to show four peers agreeing across the kills. · **What this row is *not*, checked rather than assumed**: it is not run 123's 2,501 ms gap. That gap is kill 5 — the only one of the eight that took the region's **leader**, where the other seven took a follower and cost 4–83 ms — and it is a leader election at the configured `election_tick` of 10–20 ticks × 100 ms, plus the client finding the new leader. Through the whole window 22:18:23–22:18:31 all four peers read `core_voters=[2, 9, 10, 13]`; store 3's flip is at 22:19:26, kill 8. **That election had its full candidate pool.** · **But the row is a tail-latency risk and not only a correctness one**, and that is the part worth keeping: a peer that reads itself out of the voters does not campaign, so a group of four silently has three candidates and a group of three has two — every leader-kill budget after it is measured against a smaller field than the one it was computed for. The leader-kill criterion this produced is in `esker-coord/h1-run124-design.md` §2, and its precondition is this row: the budget applies to a round only if every peer's `core_voters` agrees at the same `epoch_conf`. |
| 52 | ~~**Writes were unavailable for more than 111 seconds after four `kill -9`s.**~~ run 120 (main `4dd723cc`, 4 stores + driver, `chaos --every 20s` beside `record --clients 4`): no acknowledged write lost — 3,526 read back at their own commit timestamps — and after the fourth kill the cluster acknowledged nothing for the last 111 s, 6,332,735 attempts refused. | **Closed 2026-09-10 by [`aa90d7cd`](#122): the client never redialled a store that came back.** `TcpStores` built one connection per store at construction, never built another, and **kept no addresses**, so it could not have. A killed store closes its connection; every later call answers `NotSent { "the connection is closed" }`, which is deliberately *not* retryable — the class that provably never left the client — so nothing above retried and nothing re-routed. The store never came back **for that client**, however many times it restarted. A load generator was the mild case: every `esker-sql` node holds one of these for the life of the process. · **How it was found**: not by reading. run 121 could not reproduce it (four kills, 0.80 s worst gap) because it had no `record` arm — 12 attempts a second against run 120's 31,700, and rounds that never enter the region where a failure lives are not evidence about it, which is [#9's lesson](debts-v1.md) exactly. run 122 ran run 120's recipe unchanged **plus the region census** (#118), and the census is what closed the question: through the whole eighty seconds every peer sat at `term=2`, `answered_by == handle_peer` without exception, and `applied`/`commit`/`last_index` were frozen at 5,640 on all three. **The Raft group was healthy and idle — no write ever arrived.** Candidates C (the term ladder) and D ([ADR 0099](../adr/0099-one-core-per-region-per-store.md)'s displaced core) were ruled out by that line; what remained was above Raft. · **Site**: `crates/esker-client/src/tcp.rs` · **Size**: small, once named — the entry keeps its address, a call that finds its connection closed dials again outside the lock, no sooner than 100 ms after a failed dial (run 120's 31,700 attempts a second would otherwise be spent on `connect(2)`), and the handshake is re-checked because an address can be reused · **Owner**: h1 · **Verified**: red first with the field error — a real `esker server`, a client held across a `kill -9`, the store restarted on the same address (`crates/esker-cli/tests/client_reconnect.rs`): 30 s of *"request not sent: the connection is closed"* before, 0.1 s after, and the key written before the kill still readable — so a reconnection and not a client talking somewhere else. Counterfactual: never redial, and the 30 s failure returns. **Accepted on the real topology by run 123**: 8 kills, **0 withheld** against run 122's 3 kills and 5 withheld; 43,883 refusals against 7,353,948, a factor of **168**; zero lost; and the long-lived client's recoveries measured from `record`'s own write timestamps at **9, 34, 16, 83, 7, 4, 10 and 60 ms** — every one inside the 100 ms redial cadence. The poller could not have measured this: it spawned a process per probe and never went down the path that was fixed. · **Left open, and written where it lives**: a store that is *down when the client connects* has no entry at all, because the book is keyed by the store id a handshake reports — `TODO(debt-c6 #4)`, PD's store list is what closes it |
| 19 | ~~**No collation derivation**, and the family is now measured rather than named. PostgreSQL refuses a *stored* expression whose collation it cannot derive from a **column** — `42P22`, naming the operation — and the family has two independent halves. **Which operations need one**: `upper`/`lower` (case mapping), `<`/`=`/`replace` (string comparison) and `LIKE` do; `||`, `length`, `octet_length`, `md5`, `substr`, `btrim`, `CASE`, `COALESCE`, a cast and a bare literal do not — `replace` compares and `substr` does not, which is what says this is not a rule about names. ~~**Which contexts ask**: a generated column, an index key and a `CHECK` do; a **`DEFAULT` does not**, and neither does a query, so `upper('a')` is refused in three places and answered in two.~~ **Wrong, corrected 2026-09-10 against the corpus's own rows, which said so from the start.** `CREATE INDEX ON g ((upper('a')))` and `CHECK (upper('a') = 'A')` are both **accepted** by 19beta1 — re-measured, the index and the constraint were built — and so is an index *predicate*. **One context asks, and it is the generated column**: refused in one place and answered in four. This sizing came from the corpus **header**, which carried a table its own rows contradict, and the reading that came with it — *"values that get compared"* — is refuted by it, an index key and a `CHECK` being compared and not asking. **And there are two mechanisms rather than one**, which every probe running against an *empty* table could not separate: a conflict between two **implicit** collations (`u < v`, `C` against `POSIX`) is an **evaluation-time** error, in a query and in a generated column alike, while the generated column's need for a collation derivable from a column is a **DDL** check that fires with or without rows. And only a column settles it: an explicit `COLLATE` on a literal does not, in any placement. Two sqlstates to implement, not one — `42P22` for the indeterminate case and `42P21 collation mismatch` for two explicit collations that disagree. This node also **drops an explicit `COLLATE` from a stored expression's printed form** (`upper(t COLLATE "C")` prints `upper(t)`), which any derivation would have to carry through.~~ | **Paid on three of ADR 0096's four rules, 2026-09-10, in three commits — and the ADR corrected what sized this row twice before any of it was built.** (1) **The corpus header was wrong about two of its three contexts and its own rows were right**: `CREATE INDEX ON g ((upper('a')))`, an index *predicate* and `CHECK (upper('a') = 'A')` are all **accepted** by 19beta1 — re-measured, the index and the constraint were built — so `upper('a')` is refused in **one** place and answered in four, not the other way round. The reading that came with the wrong table, *"values that get compared"*, is refuted by it: an index key and a `CHECK` are compared and do not ask. (2) **There are two mechanisms, and every probe running against an *empty* table could not separate them**: a conflict between two **implicit** collations (`u < v`, `C` against `POSIX`) is an **evaluation-time** error, in a query and in a generated column alike; the generated column's need for a collation derivable from a **column** is a **DDL** check that fires with or without rows. **What is built**: `plan::Expr::Collate` keeps the clause, so `upper(t COLLATE "C")` deparses as `upper((t COLLATE "C"))` (`e1a19b63`); `42P21 collation mismatch` at lowering, wherever two explicit clauses **merge** — `||`, `COALESCE` and `CASE` included, propagating up through a function, with a nested clause's **outer** one winning (`3da1735b`); and `42P22` naming the operation, in a generated column and nowhere else (`938d9162`). **The census is the part that will outlive the row**: 43 shapes asked in one session (`tests/captures/pg19_collation_operations.txt`) found **six** operation names where the placement-by-placement corpus had three — `initcap() function`, `ILIKE` beside `LIKE` and `regular expression` are the three nobody would guess — and put `greatest`, `least`, `nullif`, `strpos`, `split_part`, `string_to_array`, `array_position` and `replace` in the `string comparison` bucket while leaving `substr`, `btrim`, `||` and `COALESCE` out of it; **the type decides, not the operator**, `(1 = 2)` being accepted and `('a' = 'b')` not. **And the predicate is "is a column under it", not "is the derivation stronger than none"** — an explicit clause *names* an ordering and does not make one derivable, `upper(t COLLATE "C")` building where `upper('a' COLLATE "C")` is `42P22` with both being explicit; four declared divergences came off the moment that was corrected. **Sixteen declared divergences are two**: `md5`, a function this node does not have (#26), and `upper('a'::text COLLATE "C")`, which turned out not to be a collation divergence at all but a **parser** one — the same expression parenthesised gets the right `42P22`, and without parentheses `sqlparser` has no infix production for the clause after a cast. A thirteenth came off a corpus that is not this one: `generated_parens.rs` declared a collation statement in a file about parentheses, and its list is now empty. **What is left, and it is pinned rather than written in a sentence** (`two_disagreeing_implicit_collations_are_answered_here_and_refused_there`): rule 3, the evaluation-time conflict between two implicit collations. It is not built because the plan has nowhere to carry it — the refusal is per row and `plan::Expr::Binary` holds an operator and two operands and no seam — where the three that are built need none. The value is right either way, `C` and `POSIX` both ordering by byte (ADR 0076), so what differs is that a statement a real server refuses is answered; and no captured suite compares two differently collated columns to each other. **Site**: `plan::collation` (new), `parse::lower`'s `Collate` arm, `exec::ddl`'s `Stored::GeneratedColumn`, `exec::query`, `exec::cursor`, `exec::bind`, `exec::fragment`, `exec::subquery`, `plan::cte`, `plan::query`, `error`, `sqlstate` · **Size as opened**: ~~medium, a collation flowing through every expression node~~ — **smaller, and the measurement is why**: three call sites ask, each holds the whole expression, so a recursion answering `(collation, derivation, from_column)` did it with **one** new `plan::Expr` variant and no field on every node — the shape ADR 0103 refused as its "B", refused here for the same reason · **Owner**: type surface, by the user's ruling of 2026-09-10 (the row said `unassigned` until then) · **Verified how**: the whole family measured on 19beta1 one placement at a time and then 43 shapes at once; the self-check list computed from the diff — 161 binaries, 913 tests — which is what found `generated_parens`. [ADR 0096](../adr/0096-a-collation-is-derived-from-a-column-or-from-nothing.md) is accepted and carries the decision, the corrections and what the building measured. |
| 37 | ~~**A domain is not a type a client can be sent.** `information_schema.tables.table_name` is the domain `sql_identifier` on a real server — `typtype = 'd'`, base `name`, with an array type of its own — so `pg_typeof` of the column is `information_schema.sql_identifier` and `array_agg` of it is `_sql_identifier` (13360). This node answers the *base* type and its array, `name` and `_name` (1003). **The values are identical**: a domain adds a constraint, not a representation, so every row this node returns is the row a real server returns and what differs is the name a `RowDescription` carries. The reason it cannot be an arm somewhere is structural: `ColumnType` is a closed enum of storage types, a domain is carried beside a column (`catalog::ColumnDef`) rather than as one, and the oid sent to a client comes from the enum — so a domain has nowhere to live on the wire. `CREATE DOMAIN` already works and its constraint is already enforced; what is missing is the *type identity*. Group 10 of r1's wire-108 baseline is this row and nothing else, and it is deliberately not in the sweep: it needs an ADR and a milestone the user sets, not a wire fix — [ADR 0103](../adr/0103-a-domain-is-a-type-a-client-can-be-sent.md) is that ADR, proposed rather than decided, with the three shapes and what each costs.~~ | **Paid as shape A, 2026-09-10, in three families and three commits, `pg_type` rows before the wire in both order and argument.** The constraint decided the order and not a preference: `ActiveRecord` loads its type map with `WHERE t.typtype IN ('r','e','d')` on **every** connection — 712 occurrences across 164 captured files — so **an oid this node sends that does not come back from that query has no decoder**, and a correct value would have arrived as a string. Family one gave `typinput` its kind's function (`domain_in`, not the base's); family two gave the five `information_schema` domains real `pg_type` rows and an array type each; family three put their oids on the wire. **Three things the measurement had that this row did not.** (1) **A type has three answers and only one is the wire** — corrected 2026-09-10, and the correction is the more useful half: `\gdesc` is the client's rendering, `pg_prepared_statements.result_types` is the *plan's* type, and the `RowDescription` bytes are neither. For `information_schema.tables.table_name` they are `name`, `information_schema.sql_identifier` and **19** — the wire sending the **base**, because `printtup.c` replaces a domain on the way out. The third family shipped the plan's answer on the wire and r1's baseline caught it; `exec::mod`'s `field_of` now sends the base type with the domain's typmod, and the `pg_type` rows stay. Every corpus in this repository holds `\gdesc`: `pg_prepared_statements.result_types` says `{information_schema.sql_identifier, information_schema.yes_or_no, information_schema.cardinal_number}` for three columns whose `\gdesc` prints `name`, `character varying(3)`, `integer` — the `RowDescription` carries the domain and `psql` prints the base, so a node that read the corpora as the wire would have answered the base and been wrong in the one place `ActiveRecord` looks. The parity harness now renders a domain oid the way `psql` does, which is what makes the two agree. (2) **The width is the type's**: `pg_type.typtypmod` is 7 for `yes_or_no` and `atttypmod` is -1 on every one of the 122 `information_schema` columns; this node had the 7 on the six columns instead, one fact in two places. (3) **The lookup this row named was already fixed and a different one was not** — `exec::assign::user_type_of` answers for any kind, so a `CREATE DOMAIN` column already declared itself; what was missing was the *views'* own columns, and then `pg_attribute`'s catalog rows, which were built from the `const` column list rather than from the `TableDef` every other reader uses, so with the wire fixed the catalog still said `name`. **A wrong value found on the way**: `pg_typeof` of a type in a schema printed the stored name raw — `s\0dom_probe`, a NUL on the wire, where a real server says `s.dom_probe` — while `format_type` had it right one screen away. **What is not built, deliberately**: the array. `pg_typeof(array_agg(table_name))` is `name[]` here and `information_schema.sql_identifier[]` there; that is shape **B**, r1 accepted the divergence in `results/run-107.md:43`, and it is now pinned by a test so closing it is a decision rather than a surprise. Evidence: `tests/domain.rs`'s five pins (the extended path included, because a corpus replays only the simple one), `tests/captures/pg19_domain_type.txt`, [ADR 0103](../adr/0103-a-domain-is-a-type-a-client-can-be-sent.md) — accepted, with what the building measured written under the decision. **Site as opened**: `value::ColumnType` is the closed enum; `catalog::pg_catalog::typname`/`typtype` have no domain row to emit; the four declared rows are in `tests/aggregate_groups.rs` · **Size as opened**: medium — a type-system unit with an ADR in it, and the `information_schema` columns are five domains rather than one — it was both, and the ADR is what made the second family's oids safe to write down · **Owner**: type surface, the row having said `unassigned` until the user's ruling of 2026-09-10 · **Verified how**: `crates/esker-sql/tests/captures/pg19_aggregate_groups.txt`, the `sql_identifier` block |
| 48 | ~~**An idle node grows `pg_stat_activity` by one row per re-drive pass, for ever.**~~ r1 measured it on a cluster with nothing but a listening socket — no client, no runner — 2–4 sessions a minute, 326 → 330 in thirty seconds with two of those the probe's own, and about 160 in forty minutes. **Not #47**: no client is involved at all, which is why closing that one did not touch this. | **Paid the day it was opened, 2026-09-10, and it was findable by census rather than by instrumentation.** `crate::session::register` has three production callers and `crate::session::deregister` has **one** — a connection's destructor. The third caller is `exec::redrive::ReDriver::executor`, which built a fresh session per executor, twice a pass, and nothing ever took those rows out again. One grep for the pair named it; no timer, no health check and no PD heartbeat is involved, and the view counts exactly what it is given. **PostgreSQL's own shape is the fix.** Measured on 19beta1, 2026-09-10: an idle server has eight rows and seven are background — `autovacuum launcher`, `background writer`, `checkpointer`, `io worker` twice, `logical replication launcher`, `walwriter` — and **each is one row for the life of the process**, named for what it does, with a NULL `client_addr` and a set `backend_start`. So the re-driver holds one identity for its lifetime, clones it per executor, and deregisters in its destructor. **And it is named**: `backend_type` was hard-coded `client backend` for every row, so this node's one worker read as a client with no address — a row an operator would go hunting a client for. It is `schema re-driver` now, which is the column PostgreSQL uses to tell the two apart. `#47`'s attribution columns are what made the diagnosis legible: a session with a NULL `client_addr` is an internal object, which is the test the user proposed and it was right. **The pin passed with the defect reinstated on its first draft**, which is worth more than the fix: it counted only rows whose `backend_type` was `schema re-driver`, and the leaked ones carried the *default* type, so the filter could not see them. Counting **every** session makes the counterfactual say what r1 saw — ten passes take the table from 2 to 12. Evidence: `tests/redrive.rs::a_re_driver_is_one_session_however_many_passes_it_takes`, and r1's `results/run-113.md`. **Site**: `exec::redrive::ReDriver`, `crate::session::Client::backend_type`, `catalog::pg_catalog::stat_activity_row`. **Size as opened**: unknown — the report named a rate and not a cause, and the cause was one grep away. **Owner**: b4-types. **Verified how**: measured on the real cluster by r1, reproduced as a unit counterfactual here, and the fix's shape taken from the oracle rather than chosen. |
| 44 | ~~**The cast matrix's groups 4 and 5: seven renderings and ten refusal orderings.** (Opened as eight and seven; the matrix then reached `pg_cast`'s integer rows and each family grew, and then five renderings were paid by #43's own fix — the count has moved twice in a day and is counted from the declarations each time, never from this sentence.) Eight pairs where both servers answer and the *values* differ — this node prints an `inet` without its prefix length (`10.0.0.1` where PostgreSQL keeps `10.0.0.1/32`), renders a `boolean` into any character type as `t` where PostgreSQL writes `true`, and gives `uuid -> bytea` the uuid's **text** where PostgreSQL gives its sixteen bytes (`\x0000…0001`) — and seven where both refuse with different sentences: a `jsonb` object cast to a number is `cannot cast jsonb object to type numeric` there and `22P02 invalid input syntax` here, because this node routes the cast through the target's *input function* rather than refusing the **shape** first.~~ | **Paid, 2026-09-10, and it opened as eighteen rows holding seven mechanisms.** Counted from the declarations each time, never from the row: it was written as *eight renderings and seven orderings* and was **seven and ten** by the time anyone read the file. **Group 4 held four mechanisms and only two were renderings.** A `boolean` prints `t` and *casts* to `true` — PostgreSQL has a separate `booltext` — and `ToText` and `||` both knew that while the evaluator's `Expr::Cast` arm fell through to the output function; the `::text` spelling was right all along, which is why nobody saw it. An address keeps the prefix its output function hides, and `parse::lower` had had that rule for a *literal* since the address unit while every cast over a **column** did not. `crate::value::cast_text_under` is the one reader for both now. `bpchar -> xml` was not a rendering at all: a cast out of `bpchar` strips the padding, the rule had been read off the five targets it was measured on — all of them character types — and `xml` is not one. `uuid -> bytea` had already left with #43. **Group 5 held three, and only one was an ordering.** The `jsonb` family was: this node handed the whole document to the target's input function where PostgreSQL asks the *kind* first, and `value::json::cast_to_scalar` is now asked by the evaluator's cast and by the literal fold — the literal spelling had a declared divergence of its own in `pg19_json.txt` and went with them. `bytea -> uuid` was a length, not a syntax: `22P03` with a `DETAIL` naming the bytes. And `text/varchar -> regclass` **were never divergences**: the node already answered `42P01 relation "42" does not exist`, and the capture simply had not recorded the SQLSTATE. **One row is left declared and unfixed by ruling**, and it is neither #44's nor #45's: `oid -> regclass` answers `16384` there and `castprobe` here because oid 16384 names a relation in one catalog and not the other. Both behaviours are right; the capture has an oid *literal* in it, so the row compares two catalogs rather than a cast. **The capture cost more rounds than the code.** `pg19_cast_matrix.txt` carried two generations of format in one file — older rows without a SQLSTATE, one row in psql's raw `\nDETAIL:  ` shape and with its refusal in the *rows* field, so no answer could ever have matched it — and it existed **twice**, byte-identical, under `tests/corpus/` and `tests/captures/`, with the harness replaying the first. The duplicate is gone, the file says at its head what it is, and the provenance checker now looks in both directories. Evidence: `tests/cast_matrix.rs` and `tests/corpus/pg19_cast_matrix.txt`. **Site**: `crates/esker-sql/tests/captures/pg19_cast_matrix.txt` carries all seventeen with PostgreSQL's answer beside this node's; `tests/cast_matrix.rs` declares them in two groups, each with one reason attached to every member. The renderings are output functions (`value::inet`, the boolean and uuid renderings); the orderings are `exec::cursor`'s `Expr::Cast` arm reaching the input function before it checks the source's shape. **Size as opened**: medium, and **deliberately not part of #43**: fixing the evaluator's missing *conversions* will not move either group. `boolean::text` giving `t` is the one an application sees — a client that writes a boolean into a `varchar` column gets `t` here and `true` there — so this is not only a catalog-shaped difference. **Owner**: unassigned **Verified how**: **measured as a matrix rather than as instances**: 94 `pg_cast` pairs, each probed with a *column* operand so the answer comes from the evaluator rather than from the parse-time fold. 63 of the 157 disagree; these are the 22 that are not missing conversions. **It was 15 of 94** — the probe table declared 34 types and the integers were not among them; extending it to `castprobe2` added four renderings (`int2`/`int4`/`int8` to `bytea` give the ASCII of the digits where a real server gives the number's bytes, and `int4::"char"` gives `6` where a real server gives `A`) and three orderings (a `jsonb` object to each integer width). Both additions were the families this row already names. **Then #43's own fix paid five of the renderings** — `int2`/`int4`/`int8 -> bytea`, `int4 -> "char"` and `uuid -> bytea`, the row this family was named after — because they are not renderings at all: they are the same text round trip `bytea -> int4` was, seen from the other side, and one table fixed both directions. What is left here is genuinely output-shaped: `inet` without its prefix length, a `boolean` written as `t`. **A family named from its symptom held two mechanisms**, and the half that shared a mechanism with #43 left with it. |
| 47 | ~~**A client that dies mid-statement leaks its session and its socket until the statement ends on its own.** Measured by r1 on the real cluster, run 112b: a client killed while **idle** is reaped within 8 seconds and its socket goes with it — five sessions, clean — while a client killed **during** a statement leaves the session and a `CLOSE_WAIT` socket in place until that statement finishes naturally: `psql -c "SELECT pg_sleep(60)"` killed at once still holds both for the full sixty seconds. The reader only notices the peer is gone when it next goes to read, which is after the executor has returned. In the 112 series the runner's statements were killed by an 1800 s watchdog and **never** ended, so ~3,000 sessions accumulated permanently — `pg_stat_activity` went 2,983 → 3,006 and never came down. **And the instrument is blind too**: `client_addr`, `backend_start` and `application_name` are all empty in that view, so the leak cannot be attributed to a client or aged from the view that reports it. The two halves are one row because they are one file and one fix session: a lifecycle that does not notice the peer, and a view that cannot say whose sessions these are.~~ | **Paid, both halves, 2026-09-10.** The leak first: the socket is read **while the statement runs**, and a read that returns zero — the only way a socket says its peer is gone — sets the cancellation flag. **It was not the cancellation feature the size cell opposite calls it**, and that is worth reading twice: the node already had the flag (`pg_cancel_backend` and the protocol's `CancelRequest` both set it) and `exec::cancel` already checked it in the scan walk, the row wait and `pg_sleep`, which steps every 10 ms. What was missing was the *noticing*, because between statements the loop is blocked in a read and during one nobody reads at all. The size was written from the symptom before the code was opened. **One race, found by reading and not by running**: `exec::cancel::with_session` clears the flag at the start of every statement — deliberately, so a `CancelRequest` arriving while a session is idle cannot kill the *next* one — and it does that on the blocking thread, after the watcher has started. A client already gone would have its one store wiped. Harmless for a `SELECT`; the whole bug here, where the statement is blocked on a lock held by another dead session and never ends. The watcher insists every 50 ms for as long as the statement runs. **The counterfactual was run**: watcher removed, the pin fails in r1's own words — *the session outlived its client: still 3 sessions after 8 s* — and takes 60.0 s, the `pg_sleep` it waits out; watcher in, 0.06 s. The idle arm passes either way, which is what makes it a control. **Then the instrument**: `usename`, `application_name`, `client_addr`, `client_port` and `backend_start` are filled, measured against 19beta1 — and `client_addr` is an `inet` where this column had said `text`, which nothing could catch while it only ever held NULL. `backend_start` reads the **wall clock**, ruled acceptable because the constitution forbids it for *ordering* and this orders nothing; the measurement asked for with that ruling contradicted the prediction behind it — the node's own clock and the wall clock came out ten microseconds apart on this backend, not the days expected — and is printed rather than pinned, because one sample of one configuration is not a rule. **What is left is not this row**: `xact_start`, `query_start` and `state_change` are still NULL, and they are properties of a *statement*, which does have a transaction to read invariant 6's clock from — a unit of its own and not a wall-clock question. Worth a row when somebody wants those three. Evidence: `tests/client_leaves_mid_statement.rs`, `tests/session_identity.rs`, r1's `results/run-112b/session-leak.md`. **Site**: `esker-sql`'s pgwire session lifecycle — the read side that owns the socket, and the executor loop it hands a statement to; `pg_stat_activity`'s row builder for the second half. r1's repro and evidence: `esker-rails-harness/results/run-112b/session-leak.md`, four lines: `psql -c "SELECT pg_sleep(60)" &` · `kill -KILL %1` · `lsof -nP -iTCP:<port>` shows `CLOSE_WAIT` that does not go · `SELECT count(*) FROM pg_stat_activity` is +1 and does not come down until the sixtieth second. **Size as opened**: medium, and it is a **cancellation** feature rather than a reaping one: the read side has to watch the socket for EOF/RST *while* the statement runs, set a cancel flag, and the executor has to check it at row and operation boundaries and abort — a wait like `pg_sleep` has to be interruptible too, which is the part a row-boundary check alone does not reach. **Ranked above everything after #42** by the user, 2026-09-10: it is the hardest systems evidence the 112 series produced, and it is what makes a killed harness poison the cluster for every later run. **Owner**: b4-types **Verified how**: **measured on the real topology, both arms, with the control**: idle-kill reaps in 8 s, mid-statement kill does not until the statement ends — one number apart, which is what makes it a lifecycle finding and not a load one. The pin to write is r1's four lines as a test (kill mid-statement, session count back within 8 s, no `CLOSE_WAIT` left) beside the idle-kill control, because a test with only the failing arm cannot tell a fixed lifecycle from a reaper that runs on a timer. |
| 42 | ~~**A fold at plan time destroys a node PostgreSQL keeps and prints**, and the deparse census is where it surfaced. `lower_cast` folds a literal under a cast into a `Literal::Typed` datum, and `ToText` over a constant folds too, so `((1)::bigint)::text` is one `Datum::Text` before any reader can print it: the `bigint` is not recoverable, the `DEFAULT` falls back to the text the user wrote, and that text is `sqlparser`'s `Display` — which is where the upper-cased `::TEXT` in this node's answers comes from. A real server keeps the node.~~ | **Paid in two halves, 2026-09-09 and 2026-09-10, and the deparse census's group D is empty.** The `text` half first: `lower_cast` stopped discarding a node whose target is `text`, because `ToText` is the type's own output function and deferring it to evaluation costs nothing. Then the rest of `lower_cast`, once its recorded blocker was **re-measured** and turned out to be one red rather than three (the other two had been paid by #43's first mechanism a week later, and nobody had gone back to say so). Then the last entry, which was a different site: `lower_array_constructor` folding a constant constructor into a `Literal::Typed(Datum::Array)`, after which `(ARRAY[1, 2])::text` and `'{1,2}'::integer[]::text` are the same value and nothing can tell them apart. **What the fold was holding up was not only printing**, which is the row's real lesson: `lower_cast`'s half closed a declared divergence in `assignment_cast_date.rs` (`'…'::timestamptz::date` rendered without a session, a day out), and the array half exposed three rules leaning on the fold — `exec::query::resolve` re-settling an element type lowering had already decided, `= ANY`'s reader expecting a folded value, and an `unknown` element widening the array to `text` where `select_common_type` reads it as the typed elements' type. The last of those was a **wrong type on an `INSERT`** and had been invisible for months because the fold produced a value `Literal::assign` could re-read. Evidence: `tests/deparse_census.rs` group D (now a note and no entries), `tests/array_constructor.rs`'s measured element-type rule, `tests/array_of_array.rs`. **One divergence stays and is named**: `ARRAY['a'::name, 'b'::text]` is `name[]` there and `text[]` here — `select_common_type`'s preferred-type rule between two *typed* elements — which was true before this row closed too and is in no corpus. **Site**: `parse::lower::lower_cast`'s fold, `exec::query`'s `ToText` arm, and `exec::ddl::reprinted_by_pg_get_expr`, which refuses the `Typed(Text)` top node the fold leaves. Four rows in `crates/esker-sql/tests/deparse_census.rs`, group D-fold, carry the citations. **Size as opened**: **The scalar fold is closed; one row is left and it is a different site.** The `text` half landed first: `lower_cast` no longer discards a node when the target is `text`, because `ToText` is the type's own output function and deferring it to evaluation costs nothing. The rest landed on 2026-09-10 — the guard is now `from != ty` for every target, so a cast whose operand is not already of the target type keeps its node and the constant under it is the one that was written. Same ground as [ADR 0086](../adr/0086-a-folded-cast-keeps-the-type-it-named.md), which fixed the *type* half of this and left the *printing* half. **What is left is the array constructor's own fold**, one site down in `lower_array_constructor`: `(ARRAY[1, 2])::text` prints `('{1,2}'::integer[])::text` where a real server prints the constructor it kept, and it is the single remaining entry in the deparse census's group D-fold. **Owner**: type surface **Verified how**: **measured with a one-off build that bypassed `reads_back`**, because the four rows do not all fail for the same reason and the register should say which. Three are *unchanged* by the bypass — they never reach the fixpoint guard at all. The fourth does change, to `('{1,2}'::integer[])::text`: an array constant printed as a literal where a real server prints the constructor it kept, and the guard was right to refuse it. **And the blocker was re-measured rather than read.** This row and #43 both said the widening turns *three* green tests red; widening it again on 2026-09-10 turned **one** red. Two of the three had been paid by #43's first mechanism — the evaluator has every `pg_cast` conversion now — and nobody had gone back to say the blocker had shrunk. The three statements were written into `lower_cast`'s own comment when they were measured, which is what made re-running them a one-line experiment instead of a rediscovery; a recorded blocker is a hypothesis with a date on it. |
| 43 | ~~**The constant fold is load-bearing beyond printing, and #42 is what proved it.** Keeping the `Cast` node for a non-`text` target — the obvious way to finish #42 — turns three green tests red, and each names a real gap the fold has been covering at parse time: `567.89::numeric::money` becomes `42846 cannot cast type numeric to money` and `1::money UNION ALL SELECT 2::numeric` changes its refusal sentence, because the evaluator's `Cast` arm knows fewer conversions than the fold does; and `1::oid = 1::int8` becomes `42883 operator does not exist: oid = bigint`, because a comparison retypes a **literal** against the other side and a `Cast` node is not one.~~ | **Paid, both mechanisms, 2026-09-08 to 2026-09-10.** The first was the evaluator's cast coverage and its real size was 38 missing conversions, all 38 paid. The second was `reconcile` retyping only literals, and it closed by measurement rather than by reading: there are **three** ways two types can meet in a comparison — two columns, two literals, and a column against a typed literal — and each takes different arms. All three were put to the oracle as a full matrix, 8,321 rows, and all three agree. The last pairing alone held 86 disagreements in four mechanisms, each visible only once the one in front of it had gone: a range value that `fits` every column there is (41), `comparable_with` ending in the **assignment** rule (31), `retype` narrowing where it should compare (11), and three hand-written type lists inside `comparable_with` of which the integer one stopped at `oid` (9). The fix is one sentence — ask `same_family`, the measured table, not `Datum::fits` — and it deleted three lists rather than adding a fourth. **It also closed a declared divergence**: `'…'::timestamptz::date` was folded at lowering where there is no session, and #42's remaining half stopped the fold from swallowing the node. Evidence: `tests/comparison_matrix.rs` and its two captures, `tests/oid_type.rs` for the `pg_cmp` arms, `tests/cast_matrix.rs` for the first mechanism. **A wrong value came out of it too**, which no answers-versus-refuses matrix could see: every oid-ish value ranked *as* an `Oid` in `pg_cmp`'s total order and every integer ranked with it, so a pair with no arm of its own answered `Equal` — `'pg_class'::regclass = 1::int2` was `t`. **Site**: `exec::cursor`'s `Expr::Cast` arm (the conversions), `exec::query::reconcile` and `retype` (the literal-only retyping). The three statements are in `parse::lower::lower_cast`'s comment, beside the fold, so the next attempt starts from them rather than rediscovering them. **Size as opened**: medium, and it is two mechanisms rather than one — the evaluator's cast coverage and the resolver's literal-only path — either of which can be done alone. **It was the precondition for #42's remaining half**, and both are paid: #42 closed on 2026-09-10 on top of this row. **What is left is one unmeasured pairing.** The matrix put every ordered pair of the 52 scalars to the oracle twice — over two columns and over two literals — and the node now agrees on both, 2,809 rows each. A **column against a typed literal** is the third pairing and has never been measured: it takes `reconcile`'s `Ordinal` arms, which reach `Literal::comparable_with`'s fallback to `Datum::fits` — the *assignment* rule — exactly as the cast arms did before this row's fix. 2,704 statements to capture, one probe table away, and it is the shape to measure before touching that fallback. **Owner**: type surface **Verified how**: **measured by trying it**: the wider change was written, run, and the three reds read one by one before it was narrowed to `text`. Each is reproducible by widening the guard in `lower_cast` to any target and running `binary(money)`, `binary(set_operation)` and `binary(oid_type)`. **Progress, 2026-09-09.** The first mechanism was measured as a matrix before any of it was fixed: 94 `pg_cast` pairs, each probed with a *column* operand so the answer comes from the evaluator and not from the parse-time fold (`tests/cast_matrix.rs`, `tests/captures/pg19_cast_matrix.txt`). 33 of the 94 disagree, 18 of them missing conversions, and **no pair at all where this node answers and PostgreSQL refuses** — nothing here is too permissive. The geometric family, fourteen of the eighteen, is paid, and paying it found the fold answering four of them through the text with two of those wrong. `regclass -> oid` turned out not to be a conversion gap at all (#45: ADR 0097's boundary). `numeric <-> money` and `interval -> time` are what is left of the first mechanism; the second, `reconcile`'s literal-only retyping, is untouched. The other fifteen disagreements are #44 and are deliberately not part of this row. **The fold's `casts_to` gate stopped being a list**, which was the cheapest thing in this row. It began as the bit strings, gained `"char"`, `money`, `regproc` and `regtype` in two days — three families in a row, each added because a probe found this node **folding** a pair a real server refuses (`int2 -> "char"`, `2::int2::money`, `'int4in'::regproc::int2`), each invisible until the fold was compared to the oracle — and it now asks for every pair, as `exec::query` already did for every cast over an expression. **Verified by enumeration rather than by sampling**, which is what made it possible under a single-binary rule: every folded cast in every corpus and every test body is a (source, target) pair — 160 of them, taken by adjacency so `a::A::B::C` counts as two — and asking `casts_to` for each named the **30** the gate would newly refuse. Twenty-nine are already refusals the corpora assert, with the same `CannotCast` sentence `refused_cast` gives them one screen earlier; the thirtieth, `int4 -> regtype`, is answered in four corpus rows and turned out never to reach the gate at all, because a cast *to* a `reg*` type is lowered by an arm of its own. Fifteen binaries hold all thirty and all fifteen are green. The scan's boundary, stated: it reads corpora and test bodies, not statements a test builds at run time. **The 94 was a floor and the rest is now measured**: `castprobe` declared 34 types and the integers were not among them, so `pg_cast`'s largest family had never been probed. `castprobe2` adds `int2`, `int4`, `int8` and the 66 pairs they unlock, and the matrix is now every `pg_cast` row between two of the **41** scalars this node and the oracle both have — `citext`, `hstore`, `ltree` and `lquery` have no extension on the oracle and are the whole of what is unmeasured. **157 pairs, 63 disagree**, and still **no pair where this node answers and PostgreSQL refuses**. The first mechanism's real size is **38 missing conversions, and 37 are paid**: the geometric 14, the eleven the text round trip could not perform (`bool -> int4`, `int4 -> bool`, `bytea` and both floats to all three integer widths), the nine `pg_cast` rows `CASTS` did not have (both integers to `money`, all three to `regproc`, `regproc` and `regtype` back to an integer), `numeric <-> money` and `interval -> time`. **All 38 are paid.** The last was `"char" -> int4` over a column, and it is the shape worth keeping: a `"char"` and a `text` are the same `Datum::Text`, so the *value* could not say which cast it was — where `text -> int4` really is the I/O conversion it looks like — and the same cast over a *literal* had been right all along because the fold knows the type that was written. The **plan** knows too: a column reference carries its column's type (`plan::Expr::Ordinal`'s `ty`), a cast carries the type it named, and a folded literal carries its own. `exec::cursor::declared_type_of` asks it, for this one pair. **The general fix is still right and still unwritten**: a `Cast` node carrying the type it casts *from*, or a `"char"` carrying its own type the way `Datum::Geometry` and `Datum::Bit` carry theirs (ADR 0050). Nothing needs it today; the next type that shares a `Datum` with another and has a cast of its own will. Three further rows are declared and are not this mechanism's: `int2`/`int4`/`int8 -> regproc` convert correctly and *name* differently, because oid 65 is `int4eq` in every PostgreSQL and this node's `pg_proc` holds the fifteen functions `BUILTIN_FUNCTIONS` lists. **The second mechanism — `reconcile` retypes only literals — is untouched and is now the whole of this row.** The matrix stands at 157 pairs, **23 disagreeing**, and none of the 23 is a missing conversion: three are the two catalogs differing, seven and ten are #44's two families, three are #45's oid space. **The second mechanism was measured as a matrix too, and closed, 2026-09-09 to 2026-09-10.** Its named symptom — `1::oid = 1::int8` — did not reproduce, because with the `text`-only fold both operands still folded to literals; measuring the row's own example would have measured nothing. So the surface it lives in was measured instead: every ordered pair of the 52 scalars both sides have, **2,704 pairs put to the oracle twice**, once over two columns and once over two literals, and the two forms agree with each other there — which is itself the finding, since the node's two paths are different code and this row is about exactly that difference. The node disagreed on 59, and they closed in four steps, each a different reader of one fact: a flat family tag that could not express `oid`'s partial overlap with the numbers (47, the larger and worse direction — this node **answered** where a real server refuses); `one_representation` asked in the direction it is actually asked, `citext` *held* against a text-shaped target (12); a cast beside a typed literal that `reconcile` retyped instead of family-checking (26); and `'[1,3)'::int8range` refusing *itself*, because `Datum::Range`'s `column_type` answers a representative and the kept cast then looked like one between two types. `void` and `lquery` joined the no-equality list on the way. **The matrix is `tests/comparison_matrix.rs` and it is green.** |
| 45 | ~~`regclass` cast to `oid` overflows for a real relation, so `min`/`max` over one cannot answer.~~ | **Not a defect — the boundary of [ADR 0097](../adr/0097-an-oid-is-four-bytes-and-a-derived-one-is-not.md), and recorded here because the register is where a reader looks for it.** Relation ids are `u64` and an `oid` is four bytes, so `'pg_class'::regclass::oid` on a live relation is `value 9223372036854774786 is out of range for type oid`; the aggregates reach it because their argument decays through the implicit cast to `oid`. Narrowing the id is the allocation change ADR 0097 already weighed and declined, and answering a truncated oid would be a wrong value wearing a right type — the failure this whole register exists to refuse. Ruled 2026-09-09. Measured by r1's wire census, run 111: `results/run-111/wire-baseline-54-before.txt` and the baseline that replaced it (two NEW rows there, 54 to 49 overall). Owner b4. **A second surface, 2026-09-11, and the same ruling:** the cast matrix put every ordered pair of the wire v3 probe list's types to both servers, and `regclass -> integer|oid` and their arrays were the last four rows where this node refuses and 19beta1 answers. The user ruled on the ADR's own text rather than re-deciding — a truncated oid is the wrong value under a right type — so they are **declared**, pinned in `tests/user_decided_divergences.rs` beside the two spellings that are not a narrowing (`::bigint` is the id, `::text` is the name, both answer). That column of `tests/captures/pg19_cast_matrix.txt` is now empty. |
| 41 | ~~A relation name does not reach a `regclass` wherever the value does.~~ | **Paid in two commits, and the split was the measurement's rather than a preference.** PostgreSQL is not uniform here: a *comparison* reads a bare literal as an **oid** — `WHERE r = 'ra'` is `22P02`, because `=` is `oideq` — and an *assignment* resolves the name through `regclassin`. The first half needed no catalog and is a two-line arm beside the one ADR 0098 wrote for `regproc` (`bf0d1d91`). The second is the phase-ordering half: the rules for resolving a name are all `Executor`'s (`stored_name_written` rewrites a `pg_temp` prefix, then walks the resolved `search_path` against the catalog view), and `Env` carries enough to write a *nearly* faithful copy — which would have been a second reader of one name grammar, the mistake this crate has already paid 4,873 tests for. So the executor builds the rule and hands it down, exactly as #35 hands the *oid to name* direction into `decode_row`: `Settings` carries a `&dyn Fn(&str) -> Result<i64>` (there, not `Env`, because `Settings` is already threaded through every recursive `Cursor::open` — two construction sites instead of a new parameter on all of them), and each shape becomes one arm. **Three shapes, and the third was r1's rather than this row's**: `SELECT c::regclass FROM (VALUES ('pg_class')) s(c)` — a cast whose operand only exists per row — which the run-110 wire gate found and which `CatalogFunc::RegClassName` now decides by the *datum*, the way `RegTypeName` already did. **`IN` is the fourth and it needed a plan change**: `x IN ('ra')` resolves the name where `x = ANY('{ra}')` is `22P02`, and this crate folds `= ANY` into the same `InList` node so an index seek can use it — so the node carries an `any` flag now, because the two are one rule everywhere *but* a bare literal in the list. It also had to run **before** the common-type coercion, which was giving every unknown the list's type and so applying the comparison's rule one step early. Sites: `exec::cursor::Settings`, `Executor::name_rule`, `exec::dml`, `exec::query::resolve_in_list`, `plan::Expr::InList`. Tests: `tests/regclass_literal.rs` and `tests/regclass_name_at_runtime.rs`, both written red first. Owner b4. |
| 39 | ~~No secondary index on a `regclass` column.~~ | **Paid, and the asymmetry in the row was the diagnosis.** A *primary key* over the column already worked, because the row key is `encode_row`-shaped and took #35's eight-byte form for free; only the secondary index has its own encoding and its own list. So it was two entries removed from `is_index_key` and `decode_key_column`, and one arm each side encoding the oid exactly as an `int8` — which is what `pg_cmp` compares two of them by, so the key order and the comparison cannot disagree. The DDL refusal a user saw was already derived (`exec::ddl` asks `is_index_key`), so nothing had to be removed there. **The measured surprise is the ordering**: the key is the oid, so `ORDER BY r` over relations created `ra` then `rb`, with `ra` renamed to `rz`, is `rz` before `rb` — a key over the printed name would sort them the other way *and* would have to be rebuilt on every rename, which is the same argument that kept the name out of the row. A rename does not move the key and cannot make a duplicate acceptable under a unique index; both measured. Sites: `esker_keys::row`'s `is_index_key`, `encode_key_column`, `decode_key_column`. Test: `tests/regclass_index.rs`, written red first. Owner b4. |
| 38 | ~~A `regclass[]` cannot be a stored column.~~ | **Paid, and it was the other half of #35's own design rather than a separate problem.** The refusal was written from a failure whose real cause was a *fold* — `ARRAY['t'::regclass]` reaching `Datum::from_text`, which has no catalog — and closing that inside #35 left the write path already correct: `esker_keys::row` encodes an array **element by element**, so a `Datum::RegClass` element writes its eight bytes exactly as a scalar does and a `regclass[]` column already held nothing but numbers. What was missing was the resolution on the way *out*: `crate::row::decode_row` put names back into scalars and did not walk into an array. One function, one arm, one level deep — this crate has no array of an array. Measured element-wise and it is the scalar's story throughout: a rename reaches inside (`{ra,rb}` becomes `{rz,rb}` with no rewrite), a dropped relation leaves its number beside a live name, a NULL element survives both, and a name that answers to nothing fails at the cast with the *element's* `42P01`. The helper that refused a `regclass[]` column, written for the gap, is deleted with it. **The lesson is the sizing**: this row was opened from a symptom rather than from a cause, and the cause had already been fixed by the commit that opened it — worth one grep before a unit, not after. Site: `crate::row::resolve`. Test: `tests/stored_regclass_array.rs`, written red first, with the three pins the ruling asked for. Owner b4. |
| 36 | ~~A bit column's length is not enforced on write.~~ | **Paid, and the row was half the defect.** Its diagnosis was right — `value::fit_to_typmod` serves both the cast and the row write, and what it held was the *cast's* answer, so five bits went into a `bit varying(3)` column and came back three — and its sizing said "the string types are the worked example". **They were not.** The two halves are exactly swapped, and each type was wired to whichever caller it was written for: `'abcdef'::varchar(3)` was `22001` where a real server truncates to `abc`, because `value::truncate_to_typmod` (the cast's rule) had **one** caller — the runtime cast in `exec::cursor` — while the *folded literal* cast in `parse::lower` and the *domain* cast in `exec` both asked the write's. So the fix is one seam and three callers: `truncate_to_typmod` gains the bit arm that pads and truncates, `fit_to_typmod`'s bit arm calls the `bit::fit_to_column` that was written with the right rule and had no caller, and the two casts ask the cast's side. Measured, one rule over four types and both directions: a cast truncates (`abc`, `abc`, `101`, `101`, and `'1'::bit(3)` pads to `100`) and an assignment refuses — `22001` for the types whose modifier is a **maximum** and `22026` for `bit(n)`, whose modifier is an **exact width** and which therefore refuses a value that is too *short* as well. A cast to a domain takes its base type's rule, measured. **Five declared entries across four files came off under rule 2** — three in `tests/varbit.rs` and one each in `date`, `time`, `interval` and `uuid`, the last four all the same `::char(n)` cast — and every one of them had named `fit_to_typmod` as the cause, which is what made them closable: an entry that names its site is an entry somebody can close. Sites: `value::truncate_to_typmod`, `value::fit_to_typmod`, `parse::lower`, `exec::mod`. Test: `tests/typmod_seam.rs`, written red first. Owner b4. |
| 35 | ~~A `regclass` cannot be a stored column.~~ | **Paid, and the row's own diagnosis was the design.** The blocker was never the declaration — it was that this node's `Datum::RegClass` carries the printed name beside the oid, so storing one wrote the name into the row and a name in a row goes stale. So the row holds **eight bytes and no name** (`esker_keys::row`), and the name is put back on the way out by a real `crate::row::decode_row` that takes the rule as an argument. **Every one of its callers passes one explicitly** — twenty-six sites, not the eleven this row estimated — and the ones that pass `None` are the paths where no value is printed: a foreign-key check compares oids, a table rewrite re-encodes what it decoded, a differential diff wants the stable form, and `tests/insert.rs` asserts what is *on disk* and must not resolve anything. The raw codec is no longer re-exported from that module, so the only way to decode a row in `esker-sql` is to say which of the two you meant. **The unresolved form is not a placeholder**: an oid naming nothing prints its digits on a real server, measured, so a reader with no catalog degrades to a real answer rather than to nonsense — which is what makes `None` a safe default rather than a trap. Verified against the oracle with **no declared divergence**: a stored value follows an `ALTER TABLE … RENAME` with no rewrite, a dropped relation leaves the number (`IS NULL` is `f` and `r::text = r::oid::text` is `t`), `'nosuchrel'::regclass` fails at the cast before a row is written, and a `regclass` is a primary key. **Two shapes are named rather than approximated and would each be a row if wanted**: a `regclass[]` *column* (the array write path rebuilds elements through the element type's input function, and `regclassin` needs a catalog it cannot reach there — the *expression* type is untouched and still answers 2210) and a **secondary index** on such a column, which a real server allows where a primary key already works here. One regression was caught and fixed inside the unit, the shape being its own lesson: making `regclass` a column type let `ARRAY['t'::regclass]` start folding through `Datum::from_text`, which has no catalog — fixing one face promotes the next, and the runtime constructor already had the seam. Sites: `esker_keys::row`, `crate::row`, `parse::lower`. Test: `tests/stored_regclass.rs` and `tests/captures/pg19_stored_regclass.txt`. Owner b4. |
| 33 | ~~`pg_typeof` and the `RowDescription` disagree about `GREATEST` over a `character(n)`.~~ | **Paid, and it was one name missing from one list — the fourth instance of this shape and the fourth to be one line.** `exec::query::resolve` reads a `bpchar` argument as `text` on the way into a catalog function, blanks stripped, which is what makes `length(c)` answer 1 and not 4; the functions excluded from that are the ones that do not consume the value as text. `pg_typeof` does not consume it at all — it reports the argument's *type* — so reading it as `text` first made it report the coercion this crate had just inserted rather than the expression the user wrote. Adding `CatalogFunc::PgTypeof` to that list is the whole fix. **The row named `GREATEST` and the defect was never only `GREATEST`**: `LEAST`, `COALESCE` and `greatest(c, c)` — no literal anywhere in it — all answered `text` too, which is what says it was in the argument and not in the common-type ladder the row found it beside. The row's own sizing was right ("a seam question, and the three earlier instances each turned out to be one line") and its *diagnosis* was not: the two readers do not differ by **when** they ask, they differ by **what they are asked about** — one sees the expression and the other saw the expression plus a cast of its own. Site: `crates/esker-sql/src/exec/query.rs`'s `resolve`. Test: `tests/expression_typmod.rs`'s `pg_typeof_and_the_row_description_agree_about_a_bpchar`, which asks both readers about six expressions in one session and was verified red before the fix — no corpus row could have caught it, because a corpus compares one reader at a time. Owner b4. |
| 30 | ~~A folded cast is lossy in two ways.~~ | **Paid, and both halves were smaller than the row's sizing: one line each, in two different files.** (a) **The spelling.** `-1::bigint` is an operator over a cast and `(-1)::bigint` a cast over a constant — and the *plan already held the difference*: `lower_expr` builds a `Negate` over the cast for the first and folds the sign into the constant for the second. What printed the two alike was `exec::ddl::reprinted_by_pg_get_expr`, where `Negate` sat on the "nothing to reprint" list with the reason that a folded negative literal is stored as a value — true of `DEFAULT - 1`, which keeps no node at all, and false of this one. Moving it in with the operators printed `(- (1)::bigint)` immediately, because the operand — an `int8` holding a value that fits an `int4`, so it can only have come from a cast — already printed `(1)::bigint` through #24's `numeric_constant`. **Exactly the correction `Literal` had received one revision earlier, for the same reason**, which is the second time that list's "nothing to reprint" reasoning was true of the value and false of the text. (b) **The two losses.** A cast whose fold does not keep the number that was written now keeps its `Cast` node over the literal **as written**, and converts per row — which is where a real server does it, since it never folds one at all: every form in `tests/captures/pg19_negative_constant.txt` holds two nodes. The rule is narrow on purpose. Rounding to an integer is always lossy; a float target is asked whether the digits survive; **no other type is asked, deliberately** — `'2020-1-1'::date` folds to a `date` that prints `2020-01-01` and a real server normalises it the same way, so keeping a node there would print a constant nobody wrote. A spelling difference is not a loss. **Keeping the node found a live gap**: `2.5::numeric::int` became `22P02 invalid input syntax for type integer` because the per-row cast was a text round trip and `int4in` cannot read `2.5`. The rounding rule moved out of `parse::lower` and in beside every other datum-level cast (`value::numeric_to_integer`), where both callers reach it — the fold for its range error at parse time, the evaluator for the value at run time. `(2147483647::numeric + 1)::int4` closed with it. Five declared divergences deleted across three files, and `negative_constant_deparse.rs` declares nothing at all now. |
| 14 | ~~Four type families this node does not have.~~ | **Paid, all four, and the row's own count was the measurement each time.** `regtype` closed first ([ADR 0077](../adr/0077-regtype-is-an-oid-that-prints-as-a-name.md)); then `"char"` ([ADR 0095](../adr/0095-char-is-one-byte-and-the-quotes-are-part-of-the-name.md)), `oid` ([ADR 0097](../adr/0097-an-oid-is-four-bytes-and-a-derived-one-is-not.md)) and `regproc` ([ADR 0098](../adr/0098-regproc-is-an-oid-that-prints-as-a-function.md)). **The census was the unit each time, and each census found more than the report that started it**: r1's wire sweep filed four `"char"` columns and a query against `pg_attribute` found eighteen; the same query with `atttypid = 26` found 46 `oid` columns and with 24 found 40 `regproc` ones, of which this node has one. Two rules came out that reasoning gets backwards — a `"char"` compares with `text` and shares no common type with it, and a `regproc` reads a bare literal in a comparison as an `oid` while an assignment resolves it as a name — and the second of those found `tests/array_delimiter.rs` writing `WHERE typinput = 'array_in'`, a statement a real server **refuses**, which passed only because the column was `text`. `oid` did not close whole: a column that names a *relation* stays a `bigint`, because a primary key's index and a catalog view take ids from bases at bits 56–62, and the ruling of 2026-09-09 is to keep `u64` relation ids (ADR 0097's Options). |
| 12 | ~~`int4` is measured and not implemented.~~ | **Paid by b4's literal ladder ([ADR 0087](../adr/0087-an-integer-literal-is-the-narrowest-type-that-holds-it.md)), and this row outlived it by a week.** `pg_typeof(1)` is `integer` here now, in both signs and at both boundaries. Found by the §1 reconciliation: the row's own evidence was a section of `tests/integer_literal_type.rs` titled "the `int4` rung is measured and **not** implemented, deliberately" — still there, still saying `bigint`, and **the test below it started at `2147483648`**, so the one rung the section was about was the one rung nothing asserted. Both are fixed: the prose says what happened and the table now carries `1`, `2147483647`, `-2147483648` and `-2147483649`. |
| 13 | ~~No `void` type.~~ | **Paid by `ea5e72e5`, "void is a type, and it is this vocabulary's first pseudo-type" ([ADR 0092](../adr/0092-void-is-a-type-and-it-is-the-first-pseudo-type.md)).** `ColumnType::Void` exists and `tests/advisory_lock.rs` — the row's cited evidence — declares **nothing**: both its `types` and `answers` lists are empty. |
| 32 | ~~**The `QueryCanceled` coin** — `transaction_test.rb`'s *raises QueryCanceled when canceling statement due to user request* read 12, 11 and 10 FAIL of 12 across three builds and was carried as a race.~~ | **Closed.** It was a defect, not a coin: r1's tap of both servers on one seed had identical SQL and binds at **513.3 ms here against 513.2 ms on PG19**, one row against `57014` — the cancel arrived and ended the wait at the same instant, and only the raise was missing. `ddc8b891` (with `6cc1f160` for the readiness check) put `cancel::check` at the top of `wait_for_row`'s loop and in front of the statement restart; **run 103 is its last sighting and run 104's sweep read 0 FAIL of 12**, silent through run 108. Re-measured at this HEAD 2026-09-09: cancellation is checked in **six** places — the row wait, the statement restart, the advisory wait, the scan's page boundary, the cursor's three loops and DDL's two — and there is no gap. The **second half of the pair has no discriminating test and cannot have one**: its own case is the flag set after the last loop-top check and before the re-run, a window of microseconds inside one blocking call that no test can schedule from outside. The counterfactual table is in `crates/esker-sql/tests/cross_session_cancel.rs`'s `a_cancel_survives_the_lock_coming_free_underneath_it`, where someone about to delete one of the two as redundant will read it |
| 17 | **`pg_get_expr` parenthesised only the outermost operator**, and a default's literal showed no coercion inside a call. | **This unit.** The rule was not missing, its caller was: `exec::ddl::deparse` already printed every operator node in its own pair and was reachable from the index key list alone. Routing `index_expression` through it unconditionally, normalising `ALTER TABLE ... ADD COLUMN ... GENERATED` the way `CREATE TABLE` already did, and giving a `DEFAULT` the same treatment at all three statements that store one closed the six declared divergences in `tests/generated_parens.rs` to zero. Three shapes needed the tree and could never have come from parenthesising text — `LIKE` prints as `~~`, `IN` as `= ANY (ARRAY[…])`, `BETWEEN` as two comparisons — and are measured in `tests/corpus/pg19_deparse_parens.txt`, 86 statements, 84 agreeing. |
| 18 | **`yes_or_no`'s length.** `information_schema.columns.is_nullable` is a domain over `character varying(3)` and this node declared `character varying` — a catalog column list carried a type and no length. | this file's own batch: the list carries a length now, and the measurement behind it is that **no `pg_catalog` column has a typmod at all** on a real server, and of `information_schema`'s five domains only `yes_or_no` and `time_stamp` (which no view here serves) carry one |
| 21 | ~~No `ALL` quantifier, so `NOT IN` cannot be printed the way a real server prints it.~~ | **Paid, and the register's own sentence was the thing that was wrong.** The node had the whole three-valued rule and had had it for as long as subqueries compared — `SubqueryKind::Quantified { op, all }` over all six operators, decided by `exec::subquery::quantified`, whose doc already recorded the four rules this unit went and measured. What was missing was the *array* right-hand side for every spelling but `= ANY`. So the fix is one node carrying `op` and `all`, one lowering that prefers the `IN` form for the two spellings that have one, and one delegation — 22 sites, no second copy of the rule. Measuring it first found **two live defects in the `= ANY` that already existed**: `NULL = ANY (ARRAY[]::integer[])` answered NULL where a real server answers `f` (the operand was checked before the array's emptiness, and an empty array settles a quantifier with no comparison at all), and an empty array taken through the `IN` shortcut hit an evaluator written for a grammar that has no empty list. |
| 22 | ~~A deparsed numeric literal never shows its type.~~ | **Paid, and the measurement was the work exactly as the row said.** `pg_get_expr` prints a numeric constant in one of **three** forms and which one is a property of the node: bare for the literal form's own type, `(N)::type` for a cast over one, `'N'::type` for a bare constant whose type is not that form's. The node has no cast node — it folds them — and it does not need one, because which node PostgreSQL had is decidable from the constant: an `int8` that fits `int4` can only have come from a cast, one that does not is what the scanner produces; `smallint`, `real` and `double precision` have no literal syntax at all; a negative constant is a folded unary minus. 46 of 49 statements agree, across all four readers (generated column, `DEFAULT`, index key, `CHECK`). **And building the corpus found a live `22003`**: `i4 > 9223372036854775807` was refused where a real server answers, because a comparison's literal was narrowed to the column's type — fixed by the same sentence `retype`'s `numeric` arm already carried. What remains is #23, #24 and #25, each a different thing and none of them about the form a constant prints in. |
| 23 | ~~A comparison's literal is narrowed to the column's type.~~ | **Paid, and the measurement was two lines of the corpus this row cited.** `exec::query::retype` leaves an integer literal alone against an integer column now, which is the sentence its own `numeric` arm already carried — narrowing is what an *assignment* does, and `22003` is the right answer to an `INSERT` and the wrong one to a `WHERE`. `i2 > 1` prints `(i2 > 1)` on both. **`int4` and `int8` were right by accident**: a literal's datum stays an `i64` whatever width it is *declared* ([ADR 0087](../adr/0087-an-integer-literal-is-the-narrowest-type-that-holds-it.md)), so narrowing to either was a no-op and `smallint` was the only width where the rule was visible — written as the rule rather than as that width, because a fix aimed at `smallint` would have been a fix to the symptom. The wrong refusal this row also named (`i4 > 9223372036854775807` → `22003`) was closed by #22's batch. `tests/numeric_literal_deparse.rs`'s `a_comparisons_literal_keeps_its_own_type` asserts the printed text and the values beside it, so the half that was already right cannot quietly move; the row's declared divergence went from three differing columns to two. |
| 24 | ~~A folded cast loses the node PostgreSQL prints from.~~ | **Paid, and the corpus said the tree is recoverable — the cast node is not needed after all.** The row was sized as "keeping the cast node changes what every reader of a stored expression prints"; `tests/corpus/pg19_negative_constant.txt` measured both signs against every target type in all six readers and found one regular rule instead. A negative constant has **no literal form** — the scanner reads the digits and folds the unary minus in — so PostgreSQL holds a quoted `Const` of the type the *digits* would have had, and that type is a function of the value: `integer` if they fit one, `bigint` if they fit that, `numeric` otherwise. Print the constant under that **natural** type and wrap it in a cast when the wanted type is not the natural one, and every one of the 26 measured forms falls out: `(-1)::bigint` is `('-1'::integer)::bigint`, `(-1)::integer` is `'-1'::integer` with nothing wrapped, `(-1.5)::double precision` is `('-1.5'::numeric)::double precision`, `(-9223372036854775807)::numeric` is `('-9223372036854775807'::bigint)::numeric`. `numeric_constant`'s three enumerated forms became two functions and the enumeration went away. **And the sign turned out not to be part of it**: the same shape appears with a positive constant whose natural type is not `integer`, where the old printer was also wrong and unmeasured — `(9223372036854775807)::numeric` is `('9223372036854775807'::bigint)::numeric`. Two more closures came with it: `exec::query::retype` was normalising `Literal::Typed(Int8)` back to `Literal::Integer`, which is where `c_i8_cast`'s declared width went (`i8 > 1::bigint` printed `(i8 > 1)`), and a bare numeric constant was on `reprinted_by_pg_get_expr`'s `false` list on the reasoning that a literal is stored as a value — true of the value and false of the text, since `parse::fold_column_default` keeps the unfolded expression beside the folded datum. `numeric_literal_deparse.rs` lost two of its three declared divergences to this, including the 32-column row. What is left is #30, and it is a different debt: the fold is *lossy* where the target type cannot hold the value. |
| 25 | ~~The deparser's `CatalogFunc` arm has no arguments.~~ | **Paid, and the corpus was the work exactly as the row said: 29 declared divergences down to 3.** The arm now prints a call's arguments, each under its *parameter's* type so a literal shows the coercion `pg_get_expr` shows — `btrim(t, 'x'::text)`, `split_part(t, ','::text, 1)` where the integer stays bare — from a measured table (`exec::ddl::catalog_parameter_types`) whose `None` keeps the old placeholder, because printing an invented coercion is worse than keeping written text. The four **grammar productions** print upper-cased and coerce to the call's common type, taken from **their own arguments** rather than from the type the parent threaded down — the bug that found that is `CHECK (greatest(b, 1) > 0)`, where the comparison threaded the literal's width and the `bigint` column's never arrived. `NULLIF` is the production that coerces *nothing*, which is the same fact as its result type being the comparison's left input; it did not exist here at all and is `ac8ac0c7`. Two more shapes fell out: a catalog function spelled as an **operator** now takes its operand types per *variant* (`(j \|\| '{}'::jsonb)` where the symbol alone said `text`), and a **cast over a quoted string constant** prints as one node, `'{}'::jsonb` rather than `('{}'::text)::jsonb`, because that is the one node a real server's parser builds. A `CHECK` became the **fifth reader** routed through the deparser (`normalise_checks`), which also closed `tests/check_constraint.rs`'s last `answers` divergence and its one `UNMEASURED` provenance, and `42P17` in a generated column now says PostgreSQL's own sentence — `generation expression is not immutable` — instead of the index one. The three left name a type this node lacks (`regconfig`, `"char"`) or a spelling it does not keep (`substring`'s call form); the sixth reader is #29. |
| 26 | ~~`md5('a')` in a generated column is refused, and the message may be naming the wrong thing.~~ | **Paid, and the distinction the prior decision waited for turned out not to be needed.** `tests/index_expression_volatility.rs` had recorded this mislabelling on purpose, with its reason: lowering carries an unresolved name as `CatalogFunc::UserFunc` so the *executor* can look it up, while `refuse_unless_immutable` runs earlier with a `TableDef` and no catalog, so telling "absent" from "present and volatile" there was a seam change. But the two cases a `UserFunc` can be — a name nobody declared, and a user function the catalog does hold — get the **same** answer from this node, because it cannot evaluate a user function in a stored expression either way. So the guard names the function without knowing which case it is, and gives the message the query path already gives: `0A000 the function md5 is not supported`. The guard still answers `42P17` where volatility *is* the reason (`date_trunc`, which this node has and PostgreSQL calls `STABLE`) — one refusal per cause. `md5` itself is not implemented: the suite sends it **0 times**, censused, so building it would be building what nothing asks for. Still open and named in the test: a user function declared `IMMUTABLE` should eventually be *accepted* by inlining, which a refusal that names the function is easier to change into than one that names the wrong cause. **And the audit this row started was finished a unit later**, because #25's corpus was refused `replace(t, 'a', 'b')` in a generated column for a call whose `provolatile` is `i`: `is_immutable` had grown one family per defect four times over, so `tests/captures/pg19_provolatile_census.txt` is now `pg_proc.provolatile` for **all 79 names** this node resolves, in one query. Twenty-two came back `i` and were refused here — 22 of 22 in both readers, index key and generated column — and all twenty-two now compute the oracle's own values and types. `date_trunc` stays refused and stays documented: it is `i` or `s` by *argument type*, which the volatility question cannot see from where it is asked. |
| 27 | ~~`information_schema.columns` has no `collation_name` column.~~ | **Paid, and it is three columns rather than one.** Measured on 19beta1 over five columns of one table: `collation_catalog`, `collation_schema` and `collation_name` are all `name`, and all three are **NULL together** — non-NULL exactly when the column asked for a collation, which is the same condition `pg_attribute.attcollation <> typcollation` names and the condition `catalog::ColumnDef::collation` already records as `Some`. `collation_schema` is `pg_catalog`, where `C` and `POSIX` live (ADR 0076), and `collation_catalog` is `crate::parse::DATABASE_NAME` — the same string `pg_database.datname` carries, so the two agree whatever a client connected as. Appended at the end of the column list, which is the rule that list already follows: `SELECT *` expands in declared order, so a column inserted anywhere else moves every one after it. |
| 20 | ~~A capture cannot hold a `\|` or a newline in a value, and both failures are silent.~~ | **The reader's half is paid, the writer's is r1's, and the row was three defects rather than two.** (1) **A type name with a comma** — `numeric(10,2)` — was split into two types, so a row declaring three was compared as five and could not agree whatever this node answered; six statements in `tests/numeric.rs` were declared for it. Decidable without any escape, because a comma separating two types is never inside parentheses: `declared_types` splits at parenthesis depth zero, one of the six agreed immediately, and the other five turned out to be **#28** (a cast's result carries no typmod) wearing the format's coat. (2) **A `\|` inside a value** is not decidable and needs an escape, so there is now a contract — a value's backslash, pipe, semicolon, newline, carriage return and tab are written escaped and everything else is kept as written — **opt-in per file** via a `#!escaped` header line. Opt-in is measured rather than cautious: 19 rows across `tests/corpus/` already hold a doubled backslash or a backslash-n inside a value, and un-escaping unconditionally would silently change what they assert, which is this row's own defect. (3) **Silence**, which was the worst of it: a row whose cell count contradicts its declared type count now *fails* with the line, the counts and what to do, instead of reading as a disagreement between two lines of text that look identical. That check found the three live rows — `pg19_to_text.txt`, `pg19_typmod.txt`, `pg19_tsvector.txt` — which were **three declared divergences, two of them `UNMEASURED`**, and all three are gone. Correcting `typmod`'s opened **#31**: its reason said `length` and `\|\|` were operations this node does not have, it has both, and what actually differs is a `character(n)`'s trailing blanks. `tests/corpus_format.rs` asserts the contract through the node and is **the specification the capture tool has to hit**: `sesscap.py` writes no escapes and still drops every output line beginning with a space, so a multi-line value cannot be captured at all. That half is r1's, and the corpus beside the test is the red sample. |
| 28 | ~~An expression carries no typmod.~~ | **Paid, and the row's sizing was wrong the same way #24's was.** It said "medium — giving expressions a typmod is a type-surface change, the same shape as #19, and touches every reader of a `RowDescription`". `OutputColumn` already carried a `typmod` and `plan::Expr::Cast` already held the one it was written with; what was missing was the rule saying which expressions pass one along. Measured over every shape in `tests/corpus/pg19_expression_typmod.txt`: a **cast** names its own, **`NULLIF`** takes its left argument's unless the comparison changed the type, **`COALESCE`/`GREATEST`/`LEAST`** keep one only when every input has the same, **`CASE`** has none, and everything else has none. One function (`exec::query::typmod_of`) and one gate. The second half is one clause in the cast fold, which already kept a `Cast` node "when the value cannot speak for itself" and had to keep one when the value cannot speak for its *modifier* either — a `numeric` datum can say it is numeric and never that it is `numeric(10,2)`. **Nineteen declared divergences deleted across five files**: 6 in `numeric.rs`, 7 in `bit_string.rs`, 3 in `time.rs`, 2 in `interval.rs`, 1 in `nullif.rs`. Keeping the node exposed one live defect it had been hiding — `'101'::bit(3) = '101'::text` answered `f` where a real server says `42883 operator does not exist: bit = text`, because the pair matched no arm in `reconcile` once neither side was a bare literal; a cast now types the other operand exactly as a column does. **Two oracles disagree about `CASE`** and the corpus follows the one a client reads: `CREATE TABLE AS` gives the created column the first branch's modifier, `\gdesc` gives none. |
| 29 | ~~A partial index's predicate is the sixth reader of the deparser and the one not routed through it.~~ | **Paid, and both obstacles the row named had an answer that made something else better.** (a) The reader re-parenthesises a top-level `AND`/`OR` chain's operands itself, so deparsing the chain doubled every pair. The answer is to deparse the **operands** and re-join them — and to do that with the reader's *own* scanner rather than a second copy of "find a top-level AND", so `catalog::parenthesised_operands` was split into `catalog::boolean_chain` (the split) and the wrapping, and the two callers now cannot disagree about where an operand ends. (b) The `ON CONFLICT` arbiter matches a partial index by its predicate **text**, so a deparsed predicate stopped matching the `WHERE "b" IS NOT NULL` `ActiveRecord` writes. The answer is that the arbiter puts what the statement wrote through the **same printer** before comparing — and still tries the raw text, so a predicate the printer has nothing to say about, or one stored before it existed, keeps matching: two chances to agree and none to disagree, which is the safe direction for an inference rule. Closed the `indpred` divergence in *both* `pg19_catalog_func_deparse.txt` and `pg19_negative_constant.txt`. `tests/index_deparse.rs::a_deparsed_predicate_is_still_the_one_on_conflict_names` holds both halves in one test, because the `ON CONFLICT` half also passes when nothing is deparsed at all — and removing the arbiter's new branch turns both it and `tests/on_conflict.rs` back into `42P10`, which is how it is known to be load-bearing. |
| 31 | ~~A `character(n)`'s trailing blanks do not vanish where PostgreSQL makes them vanish.~~ | **Paid, and the rule is sharper than the row's name.** It is not "a `bpchar` read as `text` is right-trimmed" everywhere: **a function reads a `bpchar` argument as `text` exactly when its result *is* `text`**, which is the same fact as #28's typmod surviving, seen from the value side — `upper(c)` is `X` and `greatest(d, d)` comes back `y ` **padded**, because a production that keeps its argument's type never coerced it. Three readers keep the padding and are in the corpus for exactly that reason: `octet_length` reports the storage (`4`) where `length` reports the value (`1`), `concat` takes `"any"` and goes through the output function, and **`LIKE` matches the padded value** — `c LIKE 'x'` is `false` while `c = 'x'` is `true`, which is the sharpest pair in the file. A cast is the other half: leaving `bpchar` strips (`c::varchar`, `c::name`, `c::text` are 1 byte) and arriving at it does not (`c::char(6)` is 6, `c::bpchar` is 4) — measured with `octet_length`, the only reader that can tell. The node already had the node that strips (`Expr::ToText`); what was missing was inserting it where PostgreSQL inserts a coercion. **And `NULLIF` was the fifth reader**: it is a comparison, so it now goes through the same `reconcile` a `=` does, which is where the blank-padding of the other side lives. Closed the last four divergences in `tests/expression_typmod.rs` and the last one in `tests/typmod.rs` — a row that took three units to empty, because #20 had to escape the value before the difference could be read at all. |
| 10 | ~~The mpp differential's time-sensitive assertion.~~ | **Closed 2026-09-09, and not by the lane it was assigned to.** The row asked for *"wait on the artefact, not the clock"*; `37514f28` made the differential wait for the state each query is about and record the ones it could not test, and `fab5405e` made it take that reading **once** — a learner that has caught up can fall behind, so planning the query again after the wait is a second sample of a state that is not monotone. What is left is structurally unable to fail on time: `columnar_plan_within` returns `Some` **only** when the plan says `Engine: columnar`, so the assertion it feeds is true by construction, and the clock can only *defer* a query — with `guarded \|\| on_the_columns > 0` after the loop refusing the degenerate round where everything deferred and the row engine was compared with itself. **Site:** `crates/esker-cli/tests/multi_region_differential.rs` — **and the row said `crates/esker-sql/tests`, which was also stale** · **Owner:** closed by `37514f28` + `fab5405e` · **Sized:** — · **Verified:** read at HEAD, and the evidence this row cited (`gate-h1-c31da348.log`) **predates both commits** |
| 11 | ~~A cursor's continued scan routes to the wrong region.~~ | **Closed before this file was committed**, and the row is kept because it shows what the register is for: the gate's `08006 … key is not in region 0` was not a routing defect but a **bound** — `Router::route` raises it itself when the placement driver has not yet learned of a split, and the wait for that was fixed at five asks (~310 ms), which is fifteen heartbeats on an idle box and less than one effective cycle on a loaded one. `75ab41a9` gives the lookup **the caller's own deadline** instead of a number invented in the router. **Site:** closed by `75ab41a9`, with the test at `ab130d48` · **Owner:** h1 · **Sized:** — · **Verified:** I wrote this row from `$S/gate-h1-ab130d48.log` as "h1 diagnosing, batch held"; both had landed by the time the file was committed, so the row was re-read against `git log` before it was written down. **A register written from a gate log is one landing behind by construction** |
| 16 | ~~`DROP INDEX CONCURRENTLY` is asynchronous.~~ | **裁决：保持，理由 ADR 0083** — ruled by the user on 2026-09-09 and therefore **not an open debt**; the row stays because a reader who meets the asynchronous drop needs to find the decision, and a deleted row sends them to read the code and rediscover the question. The original entry follows, unchanged: **`DROP INDEX CONCURRENTLY` is asynchronous.** `CREATE INDEX CONCURRENTLY` answers when the build is done ([ADR 0083](../adr/0083-a-concurrent-build-answers-when-it-is-built.md)); the removal direction still returns as soon as the job exists. Deliberate: its last step waits the **retention** window rather than the step interval, because what it must outlast is a reader, and a statement that held a client for an hour would be worse than one that returns. **Site:** `crates/esker-sql/src/exec/ddl.rs`'s concurrent drop · **Owner:** g1 · **Sized:** medium, and it needs a decision before it needs code · **Verified:** ADR 0083 records it as a decision, not an oversight; nothing in the suite executes the concurrent drop — `active_schema_test`'s two `remove_index … concurrently` cases assert generated SQL only |
| 15 | ~~`name[]` has no array type.~~ | **Paid, and it was paid by a different unit — nobody moved the row.** `ColumnType::NameArray` landed with [ADR 0084](../adr/0084-name-is-a-stored-type-and-its-tag-is-additive.md) and took six of the seven corpus entries with it; the seventh, `array_agg(enum.enumlabel ORDER BY enum.enumsortorder)` over `pg_enum`, went out with `a62d26e5` — the `regclass`/`oid` unit — because that is what gave the catalog's identifier columns their real types, and the aggregate then derived `name[]` from a `name` argument instead of `text` from a `text` one. Re-measured at this HEAD against 19beta1, all four shapes agree: `array_agg` over a `name` column, over `pg_enum.enumlabel`, and `current_schemas(true)` are all **1003**, and `min(name)` is **25** — the rule that runs the other way, because a real server has no `min(name)` and coerces. All four are pinned in `tests/name_array.rs`, which is why this could be confirmed rather than assumed. **The lesson is the row, not the fix**: a debt closed as a side effect of another unit stays open on the register until someone re-measures it, and this one had been closed for a day. Owner b4. |
| 4 | ~~`SELECT … FOR UPDATE` is not a cluster-wide lock.~~ **Closed 2026-09-09 by [ADR 0088](../adr/0088-a-row-lock-across-nodes.md) (a′), in two units.** The register called this *cross-node deadlock detection*, and the measurement says the debt is larger than its name: a row lock lives in one `esker-sql` process's table, so a second node does not see it, does not wait for it, and cannot deadlock on it. What is missing is not a graph — it is the lock. | **Site**: measured on a real cluster in `crates/esker-sql/tests/cross_node_deadlock.rs`: one node answers `40P01` and kills one of the two, two nodes commit both (rows end `(1,9)` and `(2,9)`) and `pg_locks` is empty · **Size**: medium — the lock becomes a Percolator lock ([ADR 0088](../adr/0088-a-row-lock-across-nodes.md) (a'), accepted 2026-09-09), which needs **no new wire tag**: `Check` (tag 5) and `Kind::Lock` already exist and the acquisition is an ordinary prewrite sent early · **Owner**: **h1**, not PD — the row said "needs a PD-held graph, owner pdha" until 2026-09-09, which sized option (b), the one the ADR refuses · **Verified**: the ADR's measurement, against the single-node control in the same file; the earlier half of #4 — a cross-node cycle is unreachable on the **write** path — is `28ec2474` and `crates/esker-client/tests/prewrite_ordering.rs`, a different mechanism reaching the same conclusion · **Unit 1** (`4eb3767a`): the lock is acquired as a Percolator lock on the row's key when the statement runs — an ordinary `Prewrite` of a `Check` mutation sent early, so **no new wire tag and no new method**; the victim of a cycle is chosen by wound-wait on `start_ts` (the TSO's, invariant 6), so every node breaks the tie the same way with no graph and no round trip. Two nodes given the crossed sequence now answer `40P01` with one victim and one commit, where they both committed before. **Unit 2** (`2d708e46`): the heartbeat sender, because a lock held for the length of a transaction outlives a three-second lease and `TxnKv::Heartbeat` had a handler and no caller — a client now renews at a third of the lease and stops at every ending, `Drop` included. **The five rules the build produced are in the ADR**, each found by a test going red rather than by reading: a transaction upgrades its own lock to the write it took it for; a reader never blocks on a lock-only lock; a reader never wounds; a losing transaction takes its locks with it; and a lock-only record is not a conflict — that last reversing a documented rule, with the argument and both faces pinned. · closed by `4eb3767a` + `2d708e46`, and the split face `a_row_lock_survives_the_split_that_moves_its_row` is what makes reusing the Percolator lock the right half of the choice |
| 9 | ~~**A displaced core and an orphaned handle: a store refuses to serve a region its own peer is leading.**~~ Filed as *`esker-raft` pre-vote term climb*, closed on ten green rounds, **reopened five rounds later**, and then found not to be a term climb at all. **Closed 2026-09-09 by [ADR 0099](../adr/0099-one-core-per-region-per-store.md).** The lesson stays first: those ten rounds each finished in 2.5–13 s and the failing one took 181 s — they were not in the same state at all, so "it did not appear" was never the same claim as "it cannot". | **Site**: `crates/esker-store/tests/promotion.rs`'s `a_learner_on_a_fresh_store_becomes_a_voter_under_load`, which prints the counters on the way out as well as down — that print is what caught it · **Size**: closed · **Owner**: h1 · **Verified**: **one line, and no single core can produce it**: store 1 at `term=7 is_leader=false believes_leader=None` beside `raft_role=Leader`, `campaigns_pre=1108` `campaigns_real=91`; stores 2 and 3 at **term 263**, followers, both believing 54 leads. `become_leader` sets `role` and `leader` two lines apart and `publish_leader` runs after every message, so the pair cannot disagree — and 91 adopted terms cannot end at 7. Read as **two cores under one region id** every field fits: 2/3 are right, 54 leads at 263, and the handle the request path holds belongs to a peer whose core was displaced. Membership agreed on all three, so ADR 0085's guard was never what was at work · **the fix is [ADR 0099](../adr/0099-one-core-per-region-per-store.md)**: registering claims a place instead of overwriting it, `RaftPeer::start` returns a reservation the caller commits after the region map takes it, retiring names the registration rather than the region, and ADR 0094's campaign runs after the commit. Red first across all five refusal branches in `every_refusal_leaves_nothing_driving_the_region_it_refused` . **Measured after the fix, in the coordinator's window of 2026-09-09: 32 rounds of that test (12 quiet, 20 under a six-thread arm at container load 4-20), all green, and the end-of-run sweep found no region whose handle was answered by another peer's core — 0 orphans in 32.** What those rounds do **not** show is the state region #9 was in: every one of them ran with `campaigns_pre` at 1 or 2 against that round's 1108, and the longest took 72 s against 181 s. They are evidence of no regression, not of a reproduction that came back clean |
| 40 | ~~**A pre-vote livelock when ticks arrive in batches**: three voters all `PreCandidate` at once, no leader, and the term climbing.~~ **Closed 2026-09-09 as in-process-harness capacity**, by the measurement the row's own verdict rule was written for. | **Nine rounds on four real `esker server` processes**, three quiet and six under load, every one of them valid under the discard rule — 605 to 696 regions, and `load1` measured inside the container at 0.99–3.02 (control), 4.65–7.60 (six spinners) and **9.96–17.54** (twelve). **Not one wait over thirty seconds, and not one `not the leader` wait at all.** The load reached the cluster — the same test takes 18 s quiet and 61 s at twelve spinners — so the zero is a zero and not an arm that missed. By the rule written before the numbers, that closes this as the harness's capacity rather than a system debt, and **ADR 0104 is not needed**. What stays is [ADR 0101](../adr/0101-a-batch-of-ticks-never-carries-a-whole-election.md)'s rule and `a_batch_of_ticks_flattens_the_randomised_timeout`, which pins the mechanism in the core where it is real. **the mechanism, and it is a property of the core rather than of the load**: an election timeout is randomised per peer, and [ADR 0081](../adr/0081-the-tick-driver-catches-up.md) has a starved driver deliver the ticks it slept through *as a batch*. A batch as wide as the randomisation's own span covers every draw in it, so every peer crosses its threshold inside one batch — **the randomisation is flattened**, all three campaign together, the vote splits, and the next pre-vote round does it again. The stall's own snapshot is in #34's row. The red test is `esker-raft`'s to write and is deterministic: the core is a pure state machine, so a batch is just `tick()` called n times before `step` — written, and it reads *one at a time: one leader / batched: none, term 6, pre 6 real 6*. **[ADR 0101](../adr/0101-a-batch-of-ticks-never-carries-a-whole-election.md) removed that flattening and did NOT close this row**, and the run that says so is the one that was supposed to verify it. Twice, at 5 ms and 4 threads: `how_the_leaderless_window_moves_with_the_drivers` saw **0 sightings** at 332 and 300 regions, while `how_long_a_writer_waits_for_a_region_between_leaders` — the **same** harness, threshold, tick, thread count and row count, in the next process — stalled **3 of 3** both times, at 321 and 235 regions, 30–32 s each. Same parameters, opposite answers, twice: **the sweep is a detector that does not fire, so its zero is not evidence and it cannot verify anything.** What the stall looks like now: always **region 1**, the term climbing between sightings (57 → 105 → 167, then 111 → 199 → 270 — fifty to ninety terms per two hundred and fifty rows), every handle still answered by its own core, and **once a store was a `Follower` with `leader Some(10)`** while the other two pre-campaigned — so a leader is sometimes elected and does not survive, which is churn rather than a group that cannot elect. Next: find why the sweep never fires before trusting either number — the item-by-item comparison is in the sweep's own doc comment, and the one experiment that decides it is to run the two in the other order. **And one candidate mechanism is now tested and refused**: a batch that puts a follower's ticks in front of the leader's heartbeat *does* depose a live leader — `a_batch_that_puts_ticks_before_a_heartbeat_deposes_a_live_leader` shows it at the pre-0101 width of 21 — but **not at ADR 0101's capped width of 9**, where every drive delivers a heartbeat that resets `election_elapsed` before nine ticks can reach a draw of ten. So the field's *elected but does not survive* is not this, and 0101 turns out to guard the leader as well as the election. **And the flip settled the sweep question: the answer follows the position, not the test.** Run first, the sibling saw **0**; run third, the same test with the same parameters stalled **3 of 3**, twice. The sweep fired in none of **six** opportunities across three positions, so it is not a detector and neither of its numbers says anything about the tick or the thread count. One confounder is left and is named rather than hidden: the third position in the flipped round held the 20 ms / 8 configuration, which may be immune on its own. The experiment that separates them — the same test three times in a row — **produced no data**: both runs hit a 400 s timeout with nothing printed, where the same test finished in 214 s an hour earlier on the same box at the same load. That doubling is **unattributed**, and three hypotheses have been refused with evidence rather than argument. **A gate**: the chain log says the box was idle 17:30:24–17:59:25. **Another lane**: re-checked with the *dates* on, not just the clock times — the three host compiles that looked like the answer were from the day the disk filled, and nobody ran anything heavy in the window. **The deadline swallowing a compile**: my own log says `Finished \`test\` profile … in 0.09s` on its first line, so both runs spent the whole 400 s inside the test. What is left is the box itself, and one candidate is now testable for free: at the time this lane's build volume held **331 GB** on a disk **86% full**, and it now holds **1 GB** — so the next control round of the same test is itself the experiment. Back near 214 s is evidence; still past 400 s says the disk was not it either. Three more candidates were ruled out on the way and each is worth keeping: leftover processes and data directories are impossible by construction (`in.sh` is `docker run --rm` and every store's data directory is `tempfile::tempdir()` **inside** the container, while the mounts are the repo, the build target and the cargo registry), and memory pressure is ruled out by measurement (swap 0 of 0, no swapins or swapouts on a 128 GB box — a small `free` figure on macOS is caching, not pressure). **And the first attribution was wrong**: I read a gate off a twelve-second sample extrapolated backwards, and the chain log says the box was idle then. **The separation this row asked for is finished** — see #40 in §2: nine rounds on four real store processes, six of them loaded to `load1` 17.5, not one wait over thirty seconds. This row's own question is answered and only its closure is left to rule on. A container's `/proc` is the VM's and cannot see a host compile at all, which is why the sampler now counts host `rustc`/`cargo`/`clippy-driver`/`rustdoc` and the `Gate` says in its own doc what it is blind to. **The design that closes this row is written and not yet run**: `docs/plans/split-region.md` §11, *How debts-v1.1.md #40 gets closed* — the real-binary measurement against a control and two in-container load levels, with the discard rule (a round that did not pass 300 regions or raise the load does not count as a zero), the criteria and the verdict rule all fixed before the numbers, and a 25-minute budget **And the two 400 s timeouts have a likely answer at last**: the same in-process test, after this lane's build volume went from **331 GB to 1 GB** on a disk that was 86% full, ran in **110 s** — half its best previous time, and under load still decaying from the twelve-spinner arm. One run, so it is support and not proof, but the direction is the disk and not anything that accumulates. |
| 34 | ~~**A region can be without a leader for more than thirty seconds at 192–325 regions, and it is repeatedly region 1.**~~ **Closed 2026-09-09 by the same nine rounds that closed #40**, which is what this row asked for: the system and the harness, separated. | **Four real `esker server` processes, three quiet rounds and six loaded** — 605 to 696 regions, `load1` measured inside the container at up to **17.54**, and **not one wait over thirty seconds**, nor one `not the leader` wait at all. The load reached the cluster (the same test takes 18 s quiet and 61 s at twelve spinners), so the zero is a zero. The in-process stall this row recorded was the harness's capacity, and the mechanism inside it is pinned in the core by `a_batch_of_ticks_flattens_the_randomised_timeout` with [ADR 0101](../adr/0101-a-batch-of-ticks-never-carries-a-whole-election.md)'s rule. **What the row leaves behind is worth more than its closure**: region 1 is the catalog's host and on the path of every statement — now `docs/DESIGN.md` §15's open question and [ADR 0102](../adr/0102-the-catalogs-read-path.md) — and the evidence that the earlier timeouts were the **disk** (the same test ran 110 s after this lane's build volume went from 331 GB to 1 GB, half its best previous time). `crates/esker-sql/tests/routing_differential.rs`'s `how_long_a_writer_waits_for_a_region_between_leaders` (a measurement) and `a_splitting_bulk_load_never_fails_for_want_of_attempts` (a reproducer), both `#[ignore]`d unknown, and the first question is not the fix: **the system and the harness are not separated** — four stores in one process driving three hundred Raft groups at a 5 ms tick is its own explanation and nothing rules it out yet h1 **four runs, fourteen sightings, none recovered**: 30.0–33.4 s each at 192, 252, 276 and 325 regions, where thirty seconds is where the instrument gives up — so every number is a floor. Seven of the fourteen name **region 1**, the original whole-key-space region. Found by [ADR 0100](../adr/0100-a-region-between-leaders-waits-on-the-callers-deadline.md)'s measurement, which was asked whether a retry budget should give way to the caller's deadline and answered that the budget is not what stands in the way. For contrast, [ADR 0094](../adr/0094-a-split-childs-leader-is-the-parents-leader.md) measured a split child's leaderless window at a **median of 10 ms** across 132 splits on the same harness at ~130 regions — so what this row is really about is how that window behaves as the region count grows. **And why it is region 1 seven times out of fourteen is settled, statically**: the catalog lives under `'m'` and every SQL key under `'x'`, so the catalog sorts below all data and stays in the **left-most region for the life of the cluster**, while `Catalog::view` reads `catalog_version` from the store **once per transaction**. Region 1 is not special to Raft — it is the catalog's host, on the path of every statement, so one window there is seen by every statement in it and those seven sightings are as likely to be **one** window as seven. The design observation is its own open question, now in `docs/DESIGN.md` §15: a single region read by every statement on every node. **SEPARATED 2026-09-09, and it is the harness.** Two controls, both against the same load: (a) **four real `esker server` processes** reached **663 regions** — twice where the in-process harness stalls — with **0 sightings**; (b) the in-process harness at **5 ms tick / 4 driver threads** stalled 3 times at 325 regions and cleared none inside 20 s, while at **20 ms / 8 threads** it reached **452 regions** with **0 sightings**. A higher region count with more headroom is fine and a lower one without it is not, so **what produces this is Raft per driver thread per tick, not the region count**. What it is *not* is [ADR 0099](../adr/0099-one-core-per-region-per-store.md)'s state: the sweep taken **during** a stall shows every store's handle answered by its own core. What it **is**, on all three stores at once: `PreCandidate`, `leader None`, and the term climbing — 209, 210, then 617 on the same region — which is a **pre-vote livelock** and is the shape #9 was originally named for |

## 3. Where the evidence is

**`$S` and `$R` are outside this repository and `$S` does not outlive its session**, which is why
every citation above also names a *hash* or a file **name**: a gate log is
`gate-<lane>-<hash>.log` wherever it is kept, and the hash is in this repository's history for
ever. `$S` is the coordinator's scratchpad for the night of 2026-09-08/09 and `$R` is
`esker-rails-harness/results`; the index of both is `$S/v11-evidence.txt`, and the durable half of
each row is the sentence, not the path. The three that matter most for a reader
coming to this cold:

* **the landing chain**, thirteen batches with a `ci-tree` gate log each (`$S/gate-<lane>-<hash>.log`,
  carrying the `FMT`/`CLIPPY`/`DOC`/`DENY`/`TEST` return codes and the nextest summary);
* **the scoreboard**: run 104 10,048/10,134 = 99.15 %, run 105 10,067 = 99.34 %, run 106 10,071 =
  99.38 %, each with its routing table under `$R`;
* **the gates that went red and passed on a re-run of the same tree** — four of them, and every
  one was load-sensitive rather than a defect in the batch: they are rows 9, 10 and 11 above. Row
  11 turned out to be a *bound* that only looks load-sensitive — five asks is fifteen heartbeats
  idle and less than one loaded — which is the difference worth learning from those four: "passes
  on a re-run" is a symptom, and it has both an innocent cause and a guilty one.

## 4. ADR numbering, re-taken

Checked mechanically over `docs/adr/*.md` at this commit:

| Check | Result |
|---|---|
| files matching `NNNN-*.md` | **84** |
| distinct numbers | **84** |
| range | **0001 – 0084** |
| gaps in 1..84 | **none** |
| numbers used by two files | **none** |

Twenty-one ADRs since v1's sixty-three. Numbering has collided twice in this project's history —
ADR numbers and, separately, the catalog record version — and both were caught at merge rather
than at write time, so the standing rule holds: **claim the number out loud in the report the
moment it is taken.**

## 5. How to re-take this

```sh
# ADR contiguity and duplicates
ls docs/adr/[0-9]*.md | sed 's|.*/||' | cut -c1-4 | sort | uniq -d
ls docs/adr/[0-9]*.md | wc -l

# every declared divergence, by family — the rule-4 scan's own list
rg -n 'types: &\[' -A 40 crates/esker-sql/tests/*.rs | rg '^\s*"SELECT'

# what is still UNMEASURED, which is the number `provenance_budget` holds
rg -c 'UNMEASURED' crates/esker-sql/tests/*.rs | sort -t: -k2 -rn | head
```
