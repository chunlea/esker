# Debts still open at v1.1

**One row carries over from v1 — #4 — nine are new, one of the nine closed between being
written and being committed** (#11, and it is left in place with what closed it), **and one was
ruled rather than paid** (#16: keep the behaviour, reason ADR 0083). So the count of debts a
reader has to act on is eight, and the table below has ten rows: a register that deletes a settled
row is a register that gets the same question asked again.

> **A row written from a gate log is one landing behind by construction, and three rows proved it
> in one night.** #4 said *"needs a PD-held graph, owner pdha"* and the measurement said the lock
> was missing rather than the graph; §J11 of `phase-16-mpp.md` said the join rewrite was *left* and
> `git log -S` found it built four days earlier, by a commit whose own message recorded the
> permission that section was still asking for; #10 asked for a fix that two commits had already
> landed, citing a gate log older than both. Each cost a unit being issued — #10's to two lanes at
> once. **So: before a row is acted on, re-read it against `git log` for the file it names.** One
> command, and it is cheaper than the unit.

**Then #17 was paid and two rows were opened by paying it** (#19, #20 — both found by measuring
the shapes rather than by reading the code, which is the argument for measuring), so the count is
eleven to act on across thirteen rows (#21 and #22 arrived the same way, from the same corpus; #22
was #12's consequence until b4's int4 ladder landed and it stayed). **#21 and #22 were then paid, and measuring each
first found a live defect in the thing it was said to be missing** — #21 two, in the `= ANY` that
already existed, and #22 a `22003` on a statement a real server answers. Paying #22 also opened
three rows (#23, #24, #25), because the measurement said the one row was three things: so the count kept moving on both sides. **Where it stands: §1 has four rows and all four are open** — #19, #37, #44, #46 — and §2 holds thirty-seven that are done. (#43, #42 and #47 all moved on 2026-09-10; #42 on top of #43, and #47 opened and closed the same day from r1's run-112b evidence.) **Counted from the tables and not from the sentence before it**, which is the third time this line has been wrong about itself: two lanes rewrote it in one afternoon and both were arithmetic about a table neither had just read. (#34 and #40 closed together on 2026-09-09, by one experiment whose verdict rule was written before its numbers.) (#41 was written as #40 and renumbered before it landed: h1 claimed that number for the pre-vote livelock and got there first. Third time this week, and the rule holds — the later committer moves.) #38 and #39 came out of #35 the way #26 and #27 came out of #19: closing a row measures the two shapes next to it. #33 closed the day it was written, by the method note above it: the second reader was found by asking who else decides the same thing. #35 and #36 came out of groups 5 and 6 the way #26 and #27 came out of #19: the measurement that closed a type also said what the *next* thing about it would cost. (#35 was written as #34 and renumbered, h1 having claimed that number first — the register's own rule, and the second time it has been applied this week.) #26 and #27 arrived from #19's own family and were paid the same night, which is the pattern this register keeps producing: a measurement finds more than the row it was taken for, and most of what it finds is smaller than the row, with #11, #16, #17, #21 and #22 settled. A register that grows
while debts are paid is a register that was wrong about the size, which is the whole reason each
row now says how it was measured. #17 moved to §2 with what closed it, which is this file's own
rule: close a row where the row lives, in the commit that closes it. The numbering continues
[`debts-v1.md`](debts-v1.md)'s rather than restarting, because the numbers are quoted across lanes
and a register that renumbers makes every quotation of it wrong. A closed row leaves a **gap**.

**A third rule, and it is about where a defect hides rather than about this file.** Twice in one
week a bug turned out to be **two readers of one fact**, disagreeing because one of them was
written later and nobody joined them up: `parse::lower`'s constant *fold* decided which casts exist
without ever asking `pg_catalog::casts_to`, which is the gate every cast over an *expression* goes
through — so `'101'::bit(3)::int2` answered `101` where the same pair through a column was `42846`
— and g1's `BETWEEN` splitter read a bound the deparser had already re-parenthesised, which is the
same shape one layer up. Neither is visible from either reader alone; both are visible the moment
somebody asks *who else answers this question*.

**Its sibling: a fix that makes a type reachable promotes the next face of the same question.**
Making `regclass` a column type (#35) let `ARRAY['t'::regclass]` start *folding* through
`Datum::from_text`, which has no catalog — a path that had been unreachable while the type could
not be declared, and which the runtime constructor already had a seam for one arm over, written for
`regtype` for exactly this reason. So when a change makes something newly expressible, grep for the
paths that were only unreachable rather than absent. **So when a row below names a wrong answer, grep
for the second site that decides the same thing before sizing it** — three of the fixes that came
out of this register were one line once the pair was found, and none of them was findable from the
site the symptom appeared at.

A third comes from this register's own reversals, and it is a method rather than a rule about
where a row lives:

> **When a fix lives somewhere else, leave the mechanism behind as an assertion, not a comment.**
> #40's livelock is a property of `esker-raft` and its fix is in the driver that decides how many
> ticks travel together, so the core's test now asserts that batched delivery *still* livelocks it.
> A comment saying "this is why the driver does that" rots in silence; an assertion that stops
> holding tells the next person the ground moved. The same shape closed
> [ADR 0100](../adr/0100-a-region-between-leaders-waits-on-the-callers-deadline.md): its
> measurement refused the change, so the test that would have driven it prints the numbers and pins
> today's behaviour instead.

Two rules come from that file's own failures, and both are applied here rather than restated:

> **Close a row where the row lives, in the commit that closes it.** Every lag `debts-v1.md` had
> came from a closure recorded somewhere the register does not read — once in an ADR whose first
> line said it closed two rows of it.
>
> **Asking "is the site still there" is not asking "is the symptom still there."** Six of that
> file's eight rows turned out to be closed when somebody looked. So every row below was put to the
> tree or to a log before it was written, and each says which; the two that could only be taken
> from another lane's evidence say that too.
>
> **A reader nobody wrote for is invisible to a register that reads code.** Six readers of a
> stored expression had a writer that shaped the text they print; the seventh — an exclusion
> constraint's predicate — had none, so `pg_get_indexdef` on the index behind it printed whatever
> the user typed, and no row here said so because nothing in the tree looks wrong: the writer that
> is missing has no site to inspect. It took a cross product to name it. The same shape put
> [ADR 0090](../adr/0090-a-stored-expression-is-deparsed-by-the-statement-that-writes-it.md)'s own
> decision — *every reader returns those bytes unchanged* — in the odd position of being **an
> aspiration its file recorded as a fact** for as long as two of its readers were re-parenthesising
> on the way out. A decision record is checkable: walk its sentence over every caller, not over the
> ones it lists.
>
> **The direction where this node is *more permissive* than PostgreSQL had never been asked by a
> corpus.** Every capture here was taken by writing what a real server accepts and comparing the
> answers, so a row could only be opened where the node refused or answered differently — and a
> shape the node accepts and a real server rejects produces no capture at all, because nobody
> wrote the statement. The deparse census found the same blind spot one layer down, in printing:
> a reader that was measured on the shapes that happened to reach it is **wrong and green**, and
> stays that way until something asks it the cross product. Both are the same rule — *the corpus
> can only answer the question it was written to ask* — and both are cheapest to fix by writing
> the enumeration rather than the example.

[`docs/acceptance/v1.1.md`](../acceptance/v1.1.md) carries the numbers — run 107 on main
`9d642ef0`, 10,081 of 10,134 — and points back here for what is left. This file is the what
is left.

---

## 1. Open

**Only open rows live here.** A row that closes is struck and moves **whole** into §2, carrying the
note that says who closed it and how — ruled 2026-09-09, after three rows sat here struck-in-place
and a reader could no longer tell "still open" from "in this section". §1's six columns fold into
§2's three: the site, the size, the owner and the evidence go on the end of *Closed by*, because a
closed row's sizing is the useful half of a register and deleting it is how the same debt gets
re-measured a month later.

| # | Debt | Site | Size | Owner | Verified how |
|---|---|---|---|---|---|
| 19 | **No collation derivation**, and the family is now measured rather than named. PostgreSQL refuses a *stored* expression whose collation it cannot derive from a **column** — `42P22`, naming the operation — and the family has two independent halves. **Which operations need one**: `upper`/`lower` (case mapping), `<`/`=`/`replace` (string comparison) and `LIKE` do; `||`, `length`, `octet_length`, `md5`, `substr`, `btrim`, `CASE`, `COALESCE`, a cast and a bare literal do not — `replace` compares and `substr` does not, which is what says this is not a rule about names. **Which contexts ask**: a generated column, an index key and a `CHECK` do; a **`DEFAULT` does not**, and neither does a query, so `upper('a')` is refused in three places and answered in two. And only a column settles it: an explicit `COLLATE` on a literal does not, in any placement. Two sqlstates to implement, not one — `42P22` for the indeterminate case and `42P21 collation mismatch` for two explicit collations that disagree. This node also **drops an explicit `COLLATE` from a stored expression's printed form** (`upper(t COLLATE "C")` prints `upper(t)`), which any derivation would have to carry through. | `crates/esker-sql/tests/collation_family.rs` and `tests/corpus/pg19_collation_family.txt` (118 statements, 16 declared); `COLLATE` is recorded per column in `catalog::ColumnDef` and never derived | medium, and it is a **type-system** unit: a collation has to flow through every expression node, which is the same shape as the type surface rather than a DDL change. Needs an ADR — the number is g1's to ask for | unassigned | the whole family measured on 19beta1 one placement at a time; every refusal guarded by its own savepoint after two of them swallowed the replay |
| 37 | **A domain is not a type a client can be sent.** `information_schema.tables.table_name` is the domain `sql_identifier` on a real server — `typtype = 'd'`, base `name`, with an array type of its own — so `pg_typeof` of the column is `information_schema.sql_identifier` and `array_agg` of it is `_sql_identifier` (13360). This node answers the *base* type and its array, `name` and `_name` (1003). **The values are identical**: a domain adds a constraint, not a representation, so every row this node returns is the row a real server returns and what differs is the name a `RowDescription` carries. The reason it cannot be an arm somewhere is structural: `ColumnType` is a closed enum of storage types, a domain is carried beside a column (`catalog::ColumnDef`) rather than as one, and the oid sent to a client comes from the enum — so a domain has nowhere to live on the wire. `CREATE DOMAIN` already works and its constraint is already enforced; what is missing is the *type identity*. Group 10 of r1's wire-108 baseline is this row and nothing else, and it is deliberately not in the sweep: it needs an ADR and a milestone the user sets, not a wire fix — [ADR 0103](../adr/0103-a-domain-is-a-type-a-client-can-be-sent.md) is that ADR, proposed rather than decided, with the three shapes and what each costs. | `value::ColumnType` is the closed enum; `catalog::pg_catalog::typname`/`typtype` have no domain row to emit; the four declared rows are in `tests/aggregate_groups.rs` | medium — a type-system unit with an ADR in it, and the `information_schema` columns are five domains rather than one | unassigned | `crates/esker-sql/tests/captures/pg19_aggregate_groups.txt`, the `sql_identifier` block |


| 46 | **An index on a `jsonb` column**, which a real server builds and this node refuses with `0A000 an index on a column of type jsonb`. Every other row of the no-equality census is closed; this one is left because it is not the same kind of work. A comparison only has to decide *which* of two values is larger, and `crate::value::json::compare` does — that is what made ordering a `jsonb` column answer (`SortKey::ty`). An **index** key has to carry that order in its *bytes*, memcomparable, because the engine compares keys without ever seeing a type. So the encoding must put the kind first — `[] < null < string < number < boolean < array < object`, measured — then, inside a kind, a `numeric` ordering for numbers (`1.0` and `1.00` are one value and `2 < 10`), a length before contents for arrays, and a pair count before keys for objects. That is a **new on-disk key format for a type this node already stores**, so by the constitution it is the user's call, not a lane's: existing `jsonb` columns have no index to rebuild today, which makes now the cheap moment and a later one expensive. **The empty array is the trap**: it sorts below every scalar and `[1]` does not, so a kind-then-value encoding is wrong on it — the same cell that a model of the ordering was refuted by. Measured in `tests/captures/pg19_jsonb_order.txt`, 53 cells, and the census row is line 108 of `tests/corpus/pg19_no_equality_types.txt`. Owner: unassigned, pending the user. |
| 44 | **The cast matrix's groups 4 and 5: seven renderings and ten refusal orderings.** (Opened as eight and seven; the matrix then reached `pg_cast`'s integer rows and each family grew, and then five renderings were paid by #43's own fix — the count has moved twice in a day and is counted from the declarations each time, never from this sentence.) Eight pairs where both servers answer and the *values* differ — this node prints an `inet` without its prefix length (`10.0.0.1` where PostgreSQL keeps `10.0.0.1/32`), renders a `boolean` into any character type as `t` where PostgreSQL writes `true`, and gives `uuid -> bytea` the uuid's **text** where PostgreSQL gives its sixteen bytes (`\x0000…0001`) — and seven where both refuse with different sentences: a `jsonb` object cast to a number is `cannot cast jsonb object to type numeric` there and `22P02 invalid input syntax` here, because this node routes the cast through the target's *input function* rather than refusing the **shape** first. | `crates/esker-sql/tests/captures/pg19_cast_matrix.txt` carries all seventeen with PostgreSQL's answer beside this node's; `tests/cast_matrix.rs` declares them in two groups, each with one reason attached to every member. The renderings are output functions (`value::inet`, the boolean and uuid renderings); the orderings are `exec::cursor`'s `Expr::Cast` arm reaching the input function before it checks the source's shape. | medium, and **deliberately not part of #43**: fixing the evaluator's missing *conversions* will not move either group. `boolean::text` giving `t` is the one an application sees — a client that writes a boolean into a `varchar` column gets `t` here and `true` there — so this is not only a catalog-shaped difference. | unassigned | **measured as a matrix rather than as instances**: 94 `pg_cast` pairs, each probed with a *column* operand so the answer comes from the evaluator rather than from the parse-time fold. 63 of the 157 disagree; these are the 22 that are not missing conversions. **It was 15 of 94** — the probe table declared 34 types and the integers were not among them; extending it to `castprobe2` added four renderings (`int2`/`int4`/`int8` to `bytea` give the ASCII of the digits where a real server gives the number's bytes, and `int4::"char"` gives `6` where a real server gives `A`) and three orderings (a `jsonb` object to each integer width). Both additions were the families this row already names. **Then #43's own fix paid five of the renderings** — `int2`/`int4`/`int8 -> bytea`, `int4 -> "char"` and `uuid -> bytea`, the row this family was named after — because they are not renderings at all: they are the same text round trip `bytea -> int4` was, seen from the other side, and one table fixed both directions. What is left here is genuinely output-shaped: `inet` without its prefix length, a `boolean` written as `t`. **A family named from its symptom held two mechanisms**, and the half that shared a mechanism with #43 left with it. |


## 2. Closed since v1, and recorded here because the register is where a reader looks

| # | Debt | Closed by |
|---|---|---|
| 47 | ~~**A client that dies mid-statement leaks its session and its socket until the statement ends on its own.** Measured by r1 on the real cluster, run 112b: a client killed while **idle** is reaped within 8 seconds and its socket goes with it — five sessions, clean — while a client killed **during** a statement leaves the session and a `CLOSE_WAIT` socket in place until that statement finishes naturally: `psql -c "SELECT pg_sleep(60)"` killed at once still holds both for the full sixty seconds. The reader only notices the peer is gone when it next goes to read, which is after the executor has returned. In the 112 series the runner's statements were killed by an 1800 s watchdog and **never** ended, so ~3,000 sessions accumulated permanently — `pg_stat_activity` went 2,983 → 3,006 and never came down. **And the instrument is blind too**: `client_addr`, `backend_start` and `application_name` are all empty in that view, so the leak cannot be attributed to a client or aged from the view that reports it. The two halves are one row because they are one file and one fix session: a lifecycle that does not notice the peer, and a view that cannot say whose sessions these are.~~ | **Paid, both halves, 2026-09-10.** The leak first: the socket is read **while the statement runs**, and a read that returns zero — the only way a socket says its peer is gone — sets the cancellation flag. **It was not the cancellation feature the size cell opposite calls it**, and that is worth reading twice: the node already had the flag (`pg_cancel_backend` and the protocol's `CancelRequest` both set it) and `exec::cancel` already checked it in the scan walk, the row wait and `pg_sleep`, which steps every 10 ms. What was missing was the *noticing*, because between statements the loop is blocked in a read and during one nobody reads at all. The size was written from the symptom before the code was opened. **One race, found by reading and not by running**: `exec::cancel::with_session` clears the flag at the start of every statement — deliberately, so a `CancelRequest` arriving while a session is idle cannot kill the *next* one — and it does that on the blocking thread, after the watcher has started. A client already gone would have its one store wiped. Harmless for a `SELECT`; the whole bug here, where the statement is blocked on a lock held by another dead session and never ends. The watcher insists every 50 ms for as long as the statement runs. **The counterfactual was run**: watcher removed, the pin fails in r1's own words — *the session outlived its client: still 3 sessions after 8 s* — and takes 60.0 s, the `pg_sleep` it waits out; watcher in, 0.06 s. The idle arm passes either way, which is what makes it a control. **Then the instrument**: `usename`, `application_name`, `client_addr`, `client_port` and `backend_start` are filled, measured against 19beta1 — and `client_addr` is an `inet` where this column had said `text`, which nothing could catch while it only ever held NULL. `backend_start` reads the **wall clock**, ruled acceptable because the constitution forbids it for *ordering* and this orders nothing; the measurement asked for with that ruling contradicted the prediction behind it — the node's own clock and the wall clock came out ten microseconds apart on this backend, not the days expected — and is printed rather than pinned, because one sample of one configuration is not a rule. **What is left is not this row**: `xact_start`, `query_start` and `state_change` are still NULL, and they are properties of a *statement*, which does have a transaction to read invariant 6's clock from — a unit of its own and not a wall-clock question. Worth a row when somebody wants those three. Evidence: `tests/client_leaves_mid_statement.rs`, `tests/session_identity.rs`, r1's `results/run-112b/session-leak.md`. **Site**: `esker-sql`'s pgwire session lifecycle — the read side that owns the socket, and the executor loop it hands a statement to; `pg_stat_activity`'s row builder for the second half. r1's repro and evidence: `esker-rails-harness/results/run-112b/session-leak.md`, four lines: `psql -c "SELECT pg_sleep(60)" &` · `kill -KILL %1` · `lsof -nP -iTCP:<port>` shows `CLOSE_WAIT` that does not go · `SELECT count(*) FROM pg_stat_activity` is +1 and does not come down until the sixtieth second. **Size as opened**: medium, and it is a **cancellation** feature rather than a reaping one: the read side has to watch the socket for EOF/RST *while* the statement runs, set a cancel flag, and the executor has to check it at row and operation boundaries and abort — a wait like `pg_sleep` has to be interruptible too, which is the part a row-boundary check alone does not reach. **Ranked above everything after #42** by the user, 2026-09-10: it is the hardest systems evidence the 112 series produced, and it is what makes a killed harness poison the cluster for every later run. **Owner**: b4-types **Verified how**: **measured on the real topology, both arms, with the control**: idle-kill reaps in 8 s, mid-statement kill does not until the statement ends — one number apart, which is what makes it a lifecycle finding and not a load one. The pin to write is r1's four lines as a test (kill mid-statement, session count back within 8 s, no `CLOSE_WAIT` left) beside the idle-kill control, because a test with only the failing arm cannot tell a fixed lifecycle from a reaper that runs on a timer. |
| 42 | ~~**A fold at plan time destroys a node PostgreSQL keeps and prints**, and the deparse census is where it surfaced. `lower_cast` folds a literal under a cast into a `Literal::Typed` datum, and `ToText` over a constant folds too, so `((1)::bigint)::text` is one `Datum::Text` before any reader can print it: the `bigint` is not recoverable, the `DEFAULT` falls back to the text the user wrote, and that text is `sqlparser`'s `Display` — which is where the upper-cased `::TEXT` in this node's answers comes from. A real server keeps the node.~~ | **Paid in two halves, 2026-09-09 and 2026-09-10, and the deparse census's group D is empty.** The `text` half first: `lower_cast` stopped discarding a node whose target is `text`, because `ToText` is the type's own output function and deferring it to evaluation costs nothing. Then the rest of `lower_cast`, once its recorded blocker was **re-measured** and turned out to be one red rather than three (the other two had been paid by #43's first mechanism a week later, and nobody had gone back to say so). Then the last entry, which was a different site: `lower_array_constructor` folding a constant constructor into a `Literal::Typed(Datum::Array)`, after which `(ARRAY[1, 2])::text` and `'{1,2}'::integer[]::text` are the same value and nothing can tell them apart. **What the fold was holding up was not only printing**, which is the row's real lesson: `lower_cast`'s half closed a declared divergence in `assignment_cast_date.rs` (`'…'::timestamptz::date` rendered without a session, a day out), and the array half exposed three rules leaning on the fold — `exec::query::resolve` re-settling an element type lowering had already decided, `= ANY`'s reader expecting a folded value, and an `unknown` element widening the array to `text` where `select_common_type` reads it as the typed elements' type. The last of those was a **wrong type on an `INSERT`** and had been invisible for months because the fold produced a value `Literal::assign` could re-read. Evidence: `tests/deparse_census.rs` group D (now a note and no entries), `tests/array_constructor.rs`'s measured element-type rule, `tests/array_of_array.rs`. **One divergence stays and is named**: `ARRAY['a'::name, 'b'::text]` is `name[]` there and `text[]` here — `select_common_type`'s preferred-type rule between two *typed* elements — which was true before this row closed too and is in no corpus. **Site**: `parse::lower::lower_cast`'s fold, `exec::query`'s `ToText` arm, and `exec::ddl::reprinted_by_pg_get_expr`, which refuses the `Typed(Text)` top node the fold leaves. Four rows in `crates/esker-sql/tests/deparse_census.rs`, group D-fold, carry the citations. **Size as opened**: **The scalar fold is closed; one row is left and it is a different site.** The `text` half landed first: `lower_cast` no longer discards a node when the target is `text`, because `ToText` is the type's own output function and deferring it to evaluation costs nothing. The rest landed on 2026-09-10 — the guard is now `from != ty` for every target, so a cast whose operand is not already of the target type keeps its node and the constant under it is the one that was written. Same ground as [ADR 0086](../adr/0086-a-folded-cast-keeps-the-type-it-named.md), which fixed the *type* half of this and left the *printing* half. **What is left is the array constructor's own fold**, one site down in `lower_array_constructor`: `(ARRAY[1, 2])::text` prints `('{1,2}'::integer[])::text` where a real server prints the constructor it kept, and it is the single remaining entry in the deparse census's group D-fold. **Owner**: type surface **Verified how**: **measured with a one-off build that bypassed `reads_back`**, because the four rows do not all fail for the same reason and the register should say which. Three are *unchanged* by the bypass — they never reach the fixpoint guard at all. The fourth does change, to `('{1,2}'::integer[])::text`: an array constant printed as a literal where a real server prints the constructor it kept, and the guard was right to refuse it. **And the blocker was re-measured rather than read.** This row and #43 both said the widening turns *three* green tests red; widening it again on 2026-09-10 turned **one** red. Two of the three had been paid by #43's first mechanism — the evaluator has every `pg_cast` conversion now — and nobody had gone back to say the blocker had shrunk. The three statements were written into `lower_cast`'s own comment when they were measured, which is what made re-running them a one-line experiment instead of a rediscovery; a recorded blocker is a hypothesis with a date on it. |
| 43 | ~~**The constant fold is load-bearing beyond printing, and #42 is what proved it.** Keeping the `Cast` node for a non-`text` target — the obvious way to finish #42 — turns three green tests red, and each names a real gap the fold has been covering at parse time: `567.89::numeric::money` becomes `42846 cannot cast type numeric to money` and `1::money UNION ALL SELECT 2::numeric` changes its refusal sentence, because the evaluator's `Cast` arm knows fewer conversions than the fold does; and `1::oid = 1::int8` becomes `42883 operator does not exist: oid = bigint`, because a comparison retypes a **literal** against the other side and a `Cast` node is not one.~~ | **Paid, both mechanisms, 2026-09-08 to 2026-09-10.** The first was the evaluator's cast coverage and its real size was 38 missing conversions, all 38 paid. The second was `reconcile` retyping only literals, and it closed by measurement rather than by reading: there are **three** ways two types can meet in a comparison — two columns, two literals, and a column against a typed literal — and each takes different arms. All three were put to the oracle as a full matrix, 8,321 rows, and all three agree. The last pairing alone held 86 disagreements in four mechanisms, each visible only once the one in front of it had gone: a range value that `fits` every column there is (41), `comparable_with` ending in the **assignment** rule (31), `retype` narrowing where it should compare (11), and three hand-written type lists inside `comparable_with` of which the integer one stopped at `oid` (9). The fix is one sentence — ask `same_family`, the measured table, not `Datum::fits` — and it deleted three lists rather than adding a fourth. **It also closed a declared divergence**: `'…'::timestamptz::date` was folded at lowering where there is no session, and #42's remaining half stopped the fold from swallowing the node. Evidence: `tests/comparison_matrix.rs` and its two captures, `tests/oid_type.rs` for the `pg_cmp` arms, `tests/cast_matrix.rs` for the first mechanism. **A wrong value came out of it too**, which no answers-versus-refuses matrix could see: every oid-ish value ranked *as* an `Oid` in `pg_cmp`'s total order and every integer ranked with it, so a pair with no arm of its own answered `Equal` — `'pg_class'::regclass = 1::int2` was `t`. **Site**: `exec::cursor`'s `Expr::Cast` arm (the conversions), `exec::query::reconcile` and `retype` (the literal-only retyping). The three statements are in `parse::lower::lower_cast`'s comment, beside the fold, so the next attempt starts from them rather than rediscovering them. **Size as opened**: medium, and it is two mechanisms rather than one — the evaluator's cast coverage and the resolver's literal-only path — either of which can be done alone. **It was the precondition for #42's remaining half**, and both are paid: #42 closed on 2026-09-10 on top of this row. **What is left is one unmeasured pairing.** The matrix put every ordered pair of the 52 scalars to the oracle twice — over two columns and over two literals — and the node now agrees on both, 2,809 rows each. A **column against a typed literal** is the third pairing and has never been measured: it takes `reconcile`'s `Ordinal` arms, which reach `Literal::comparable_with`'s fallback to `Datum::fits` — the *assignment* rule — exactly as the cast arms did before this row's fix. 2,704 statements to capture, one probe table away, and it is the shape to measure before touching that fallback. **Owner**: type surface **Verified how**: **measured by trying it**: the wider change was written, run, and the three reds read one by one before it was narrowed to `text`. Each is reproducible by widening the guard in `lower_cast` to any target and running `binary(money)`, `binary(set_operation)` and `binary(oid_type)`. **Progress, 2026-09-09.** The first mechanism was measured as a matrix before any of it was fixed: 94 `pg_cast` pairs, each probed with a *column* operand so the answer comes from the evaluator and not from the parse-time fold (`tests/cast_matrix.rs`, `tests/captures/pg19_cast_matrix.txt`). 33 of the 94 disagree, 18 of them missing conversions, and **no pair at all where this node answers and PostgreSQL refuses** — nothing here is too permissive. The geometric family, fourteen of the eighteen, is paid, and paying it found the fold answering four of them through the text with two of those wrong. `regclass -> oid` turned out not to be a conversion gap at all (#45: ADR 0097's boundary). `numeric <-> money` and `interval -> time` are what is left of the first mechanism; the second, `reconcile`'s literal-only retyping, is untouched. The other fifteen disagreements are #44 and are deliberately not part of this row. **The fold's `casts_to` gate stopped being a list**, which was the cheapest thing in this row. It began as the bit strings, gained `"char"`, `money`, `regproc` and `regtype` in two days — three families in a row, each added because a probe found this node **folding** a pair a real server refuses (`int2 -> "char"`, `2::int2::money`, `'int4in'::regproc::int2`), each invisible until the fold was compared to the oracle — and it now asks for every pair, as `exec::query` already did for every cast over an expression. **Verified by enumeration rather than by sampling**, which is what made it possible under a single-binary rule: every folded cast in every corpus and every test body is a (source, target) pair — 160 of them, taken by adjacency so `a::A::B::C` counts as two — and asking `casts_to` for each named the **30** the gate would newly refuse. Twenty-nine are already refusals the corpora assert, with the same `CannotCast` sentence `refused_cast` gives them one screen earlier; the thirtieth, `int4 -> regtype`, is answered in four corpus rows and turned out never to reach the gate at all, because a cast *to* a `reg*` type is lowered by an arm of its own. Fifteen binaries hold all thirty and all fifteen are green. The scan's boundary, stated: it reads corpora and test bodies, not statements a test builds at run time. **The 94 was a floor and the rest is now measured**: `castprobe` declared 34 types and the integers were not among them, so `pg_cast`'s largest family had never been probed. `castprobe2` adds `int2`, `int4`, `int8` and the 66 pairs they unlock, and the matrix is now every `pg_cast` row between two of the **41** scalars this node and the oracle both have — `citext`, `hstore`, `ltree` and `lquery` have no extension on the oracle and are the whole of what is unmeasured. **157 pairs, 63 disagree**, and still **no pair where this node answers and PostgreSQL refuses**. The first mechanism's real size is **38 missing conversions, and 37 are paid**: the geometric 14, the eleven the text round trip could not perform (`bool -> int4`, `int4 -> bool`, `bytea` and both floats to all three integer widths), the nine `pg_cast` rows `CASTS` did not have (both integers to `money`, all three to `regproc`, `regproc` and `regtype` back to an integer), `numeric <-> money` and `interval -> time`. **All 38 are paid.** The last was `"char" -> int4` over a column, and it is the shape worth keeping: a `"char"` and a `text` are the same `Datum::Text`, so the *value* could not say which cast it was — where `text -> int4` really is the I/O conversion it looks like — and the same cast over a *literal* had been right all along because the fold knows the type that was written. The **plan** knows too: a column reference carries its column's type (`plan::Expr::Ordinal`'s `ty`), a cast carries the type it named, and a folded literal carries its own. `exec::cursor::declared_type_of` asks it, for this one pair. **The general fix is still right and still unwritten**: a `Cast` node carrying the type it casts *from*, or a `"char"` carrying its own type the way `Datum::Geometry` and `Datum::Bit` carry theirs (ADR 0050). Nothing needs it today; the next type that shares a `Datum` with another and has a cast of its own will. Three further rows are declared and are not this mechanism's: `int2`/`int4`/`int8 -> regproc` convert correctly and *name* differently, because oid 65 is `int4eq` in every PostgreSQL and this node's `pg_proc` holds the fifteen functions `BUILTIN_FUNCTIONS` lists. **The second mechanism — `reconcile` retypes only literals — is untouched and is now the whole of this row.** The matrix stands at 157 pairs, **23 disagreeing**, and none of the 23 is a missing conversion: three are the two catalogs differing, seven and ten are #44's two families, three are #45's oid space. **The second mechanism was measured as a matrix too, and closed, 2026-09-09 to 2026-09-10.** Its named symptom — `1::oid = 1::int8` — did not reproduce, because with the `text`-only fold both operands still folded to literals; measuring the row's own example would have measured nothing. So the surface it lives in was measured instead: every ordered pair of the 52 scalars both sides have, **2,704 pairs put to the oracle twice**, once over two columns and once over two literals, and the two forms agree with each other there — which is itself the finding, since the node's two paths are different code and this row is about exactly that difference. The node disagreed on 59, and they closed in four steps, each a different reader of one fact: a flat family tag that could not express `oid`'s partial overlap with the numbers (47, the larger and worse direction — this node **answered** where a real server refuses); `one_representation` asked in the direction it is actually asked, `citext` *held* against a text-shaped target (12); a cast beside a typed literal that `reconcile` retyped instead of family-checking (26); and `'[1,3)'::int8range` refusing *itself*, because `Datum::Range`'s `column_type` answers a representative and the kept cast then looked like one between two types. `void` and `lquery` joined the no-equality list on the way. **The matrix is `tests/comparison_matrix.rs` and it is green.** |
| 45 | ~~`regclass` cast to `oid` overflows for a real relation, so `min`/`max` over one cannot answer.~~ | **Not a defect — the boundary of [ADR 0097](../adr/0097-an-oid-is-four-bytes-and-a-derived-one-is-not.md), and recorded here because the register is where a reader looks for it.** Relation ids are `u64` and an `oid` is four bytes, so `'pg_class'::regclass::oid` on a live relation is `value 9223372036854774786 is out of range for type oid`; the aggregates reach it because their argument decays through the implicit cast to `oid`. Narrowing the id is the allocation change ADR 0097 already weighed and declined, and answering a truncated oid would be a wrong value wearing a right type — the failure this whole register exists to refuse. Ruled 2026-09-09. Measured by r1's wire census, run 111: `results/run-111/wire-baseline-54-before.txt` and the baseline that replaced it (two NEW rows there, 54 to 49 overall). Owner b4. |
| 41 | ~~A relation name does not reach a `regclass` wherever the value does.~~ | **Paid in two commits, and the split was the measurement's rather than a preference.** PostgreSQL is not uniform here: a *comparison* reads a bare literal as an **oid** — `WHERE r = 'ra'` is `22P02`, because `=` is `oideq` — and an *assignment* resolves the name through `regclassin`. The first half needed no catalog and is a two-line arm beside the one ADR 0098 wrote for `regproc` (`bf0d1d91`). The second is the phase-ordering half: the rules for resolving a name are all `Executor`'s (`stored_name_written` rewrites a `pg_temp` prefix, then walks the resolved `search_path` against the catalog view), and `Env` carries enough to write a *nearly* faithful copy — which would have been a second reader of one name grammar, the mistake this crate has already paid 4,873 tests for. So the executor builds the rule and hands it down, exactly as #35 hands the *oid to name* direction into `decode_row`: `Settings` carries a `&dyn Fn(&str) -> Result<i64>` (there, not `Env`, because `Settings` is already threaded through every recursive `Cursor::open` — two construction sites instead of a new parameter on all of them), and each shape becomes one arm. **Three shapes, and the third was r1's rather than this row's**: `SELECT c::regclass FROM (VALUES ('pg_class')) s(c)` — a cast whose operand only exists per row — which the run-110 wire gate found and which `CatalogFunc::RegClassName` now decides by the *datum*, the way `RegTypeName` already did. **`IN` is the fourth and it needed a plan change**: `x IN ('ra')` resolves the name where `x = ANY('{ra}')` is `22P02`, and this crate folds `= ANY` into the same `InList` node so an index seek can use it — so the node carries an `any` flag now, because the two are one rule everywhere *but* a bare literal in the list. It also had to run **before** the common-type coercion, which was giving every unknown the list's type and so applying the comparison's rule one step early. Sites: `exec::cursor::Settings`, `Executor::name_rule`, `exec::dml`, `exec::query::resolve_in_list`, `plan::Expr::InList`. Tests: `tests/regclass_literal.rs` and `tests/regclass_name_at_runtime.rs`, both written red first. Owner b4. |
| 39 | ~~No secondary index on a `regclass` column.~~ | **Paid, and the asymmetry in the row was the diagnosis.** A *primary key* over the column already worked, because the row key is `encode_row`-shaped and took #35's eight-byte form for free; only the secondary index has its own encoding and its own list. So it was two entries removed from `is_index_key` and `decode_key_column`, and one arm each side encoding the oid exactly as an `int8` — which is what `pg_cmp` compares two of them by, so the key order and the comparison cannot disagree. The DDL refusal a user saw was already derived (`exec::ddl` asks `is_index_key`), so nothing had to be removed there. **The measured surprise is the ordering**: the key is the oid, so `ORDER BY r` over relations created `ra` then `rb`, with `ra` renamed to `rz`, is `rz` before `rb` — a key over the printed name would sort them the other way *and* would have to be rebuilt on every rename, which is the same argument that kept the name out of the row. A rename does not move the key and cannot make a duplicate acceptable under a unique index; both measured. Sites: `esker_keys::row`'s `is_index_key`, `encode_key_column`, `decode_key_column`. Test: `tests/regclass_index.rs`, written red first. Owner b4. |
| 38 | ~~A `regclass[]` cannot be a stored column.~~ | **Paid, and it was the other half of #35's own design rather than a separate problem.** The refusal was written from a failure whose real cause was a *fold* — `ARRAY['t'::regclass]` reaching `Datum::from_text`, which has no catalog — and closing that inside #35 left the write path already correct: `esker_keys::row` encodes an array **element by element**, so a `Datum::RegClass` element writes its eight bytes exactly as a scalar does and a `regclass[]` column already held nothing but numbers. What was missing was the resolution on the way *out*: `crate::row::decode_row` put names back into scalars and did not walk into an array. One function, one arm, one level deep — this crate has no array of an array. Measured element-wise and it is the scalar's story throughout: a rename reaches inside (`{ra,rb}` becomes `{rz,rb}` with no rewrite), a dropped relation leaves its number beside a live name, a NULL element survives both, and a name that answers to nothing fails at the cast with the *element's* `42P01`. The helper that refused a `regclass[]` column, written for the gap, is deleted with it. **The lesson is the sizing**: this row was opened from a symptom rather than from a cause, and the cause had already been fixed by the commit that opened it — worth one grep before a unit, not after. Site: `crate::row::resolve`. Test: `tests/stored_regclass_array.rs`, written red first, with the three pins the ruling asked for. Owner b4. |
| 36 | ~~A bit column's length is not enforced on write.~~ | **Paid, and the row was half the defect.** Its diagnosis was right — `value::fit_to_typmod` serves both the cast and the row write, and what it held was the *cast's* answer, so five bits went into a `bit varying(3)` column and came back three — and its sizing said "the string types are the worked example". **They were not.** The two halves are exactly swapped, and each type was wired to whichever caller it was written for: `'abcdef'::varchar(3)` was `22001` where a real server truncates to `abc`, because `value::truncate_to_typmod` (the cast's rule) had **one** caller — the runtime cast in `exec::cursor` — while the *folded literal* cast in `parse::lower` and the *domain* cast in `exec` both asked the write's. So the fix is one seam and three callers: `truncate_to_typmod` gains the bit arm that pads and truncates, `fit_to_typmod`'s bit arm calls the `bit::fit_to_column` that was written with the right rule and had no caller, and the two casts ask the cast's side. Measured, one rule over four types and both directions: a cast truncates (`abc`, `abc`, `101`, `101`, and `'1'::bit(3)` pads to `100`) and an assignment refuses — `22001` for the types whose modifier is a **maximum** and `22026` for `bit(n)`, whose modifier is an **exact width** and which therefore refuses a value that is too *short* as well. A cast to a domain takes its base type's rule, measured. **Five declared entries across four files came off under rule 2** — three in `tests/varbit.rs` and one each in `date`, `time`, `interval` and `uuid`, the last four all the same `::char(n)` cast — and every one of them had named `fit_to_typmod` as the cause, which is what made them closable: an entry that names its site is an entry somebody can close. Sites: `value::truncate_to_typmod`, `value::fit_to_typmod`, `parse::lower`, `exec::mod`. Test: `tests/typmod_seam.rs`, written red first. Owner b4. |
| 35 | ~~A `regclass` cannot be a stored column.~~ | **Paid, and the row's own diagnosis was the design.** The blocker was never the declaration — it was that this node's `Datum::RegClass` carries the printed name beside the oid, so storing one wrote the name into the row and a name in a row goes stale. So the row holds **eight bytes and no name** (`esker_keys::row`), and the name is put back on the way out by a real `crate::row::decode_row` that takes the rule as an argument. **Every one of its callers passes one explicitly** — twenty-six sites, not the eleven this row estimated — and the ones that pass `None` are the paths where no value is printed: a foreign-key check compares oids, a table rewrite re-encodes what it decoded, a differential diff wants the stable form, and `tests/insert.rs` asserts what is *on disk* and must not resolve anything. The raw codec is no longer re-exported from that module, so the only way to decode a row in `esker-sql` is to say which of the two you meant. **The unresolved form is not a placeholder**: an oid naming nothing prints its digits on a real server, measured, so a reader with no catalog degrades to a real answer rather than to nonsense — which is what makes `None` a safe default rather than a trap. Verified against the oracle with **no declared divergence**: a stored value follows an `ALTER TABLE … RENAME` with no rewrite, a dropped relation leaves the number (`IS NULL` is `f` and `r::text = r::oid::text` is `t`), `'nosuchrel'::regclass` fails at the cast before a row is written, and a `regclass` is a primary key. **Two shapes are named rather than approximated and would each be a row if wanted**: a `regclass[]` *column* (the array write path rebuilds elements through the element type's input function, and `regclassin` needs a catalog it cannot reach there — the *expression* type is untouched and still answers 2210) and a **secondary index** on such a column, which a real server allows where a primary key already works here. One regression was caught and fixed inside the unit, the shape being its own lesson: making `regclass` a column type let `ARRAY['t'::regclass]` start folding through `Datum::from_text`, which has no catalog — fixing one face promotes the next, and the runtime constructor already had the seam. Sites: `esker_keys::row`, `crate::row`, `parse::lower`. Test: `tests/stored_regclass.rs` and `tests/captures/pg19_stored_regclass.txt`. Owner b4. |
| 33 | ~~`pg_typeof` and the `RowDescription` disagree about `GREATEST` over a `character(n)`.~~ | **Paid, and it was one name missing from one list — the fourth instance of this shape and the fourth to be one line.** `exec::query::resolve` reads a `bpchar` argument as `text` on the way into a catalog function, blanks stripped, which is what makes `length(c)` answer 1 and not 4; the functions excluded from that are the ones that do not consume the value as text. `pg_typeof` does not consume it at all — it reports the argument's *type* — so reading it as `text` first made it report the coercion this crate had just inserted rather than the expression the user wrote. Adding `CatalogFunc::PgTypeof` to that list is the whole fix. **The row named `GREATEST` and the defect was never only `GREATEST`**: `LEAST`, `COALESCE` and `greatest(c, c)` — no literal anywhere in it — all answered `text` too, which is what says it was in the argument and not in the common-type ladder the row found it beside. The row's own sizing was right ("a seam question, and the three earlier instances each turned out to be one line") and its *diagnosis* was not: the two readers do not differ by **when** they ask, they differ by **what they are asked about** — one sees the expression and the other saw the expression plus a cast of its own. Site: `crates/esker-sql/src/exec/query.rs`'s `resolve`. Test: `tests/expression_typmod.rs`'s `pg_typeof_and_the_row_description_agree_about_a_bpchar`, which asks both readers about six expressions in one session and was verified red before the fix — no corpus row could have caught it, because a corpus compares one reader at a time. Owner b4. |
| 30 | ~~A folded cast is lossy in two ways.~~ | **Paid, and both halves were smaller than the row's sizing: one line each, in two different files.** (a) **The spelling.** `-1::bigint` is an operator over a cast and `(-1)::bigint` a cast over a constant — and the *plan already held the difference*: `lower_expr` builds a `Negate` over the cast for the first and folds the sign into the constant for the second. What printed the two alike was `exec::ddl::reprinted_by_pg_get_expr`, where `Negate` sat on the "nothing to reprint" list with the reason that a folded negative literal is stored as a value — true of `DEFAULT - 1`, which keeps no node at all, and false of this one. Moving it in with the operators printed `(- (1)::bigint)` immediately, because the operand — an `int8` holding a value that fits an `int4`, so it can only have come from a cast — already printed `(1)::bigint` through #24's `numeric_constant`. **Exactly the correction `Literal` had received one revision earlier, for the same reason**, which is the second time that list's "nothing to reprint" reasoning was true of the value and false of the text. (b) **The two losses.** A cast whose fold does not keep the number that was written now keeps its `Cast` node over the literal **as written**, and converts per row — which is where a real server does it, since it never folds one at all: every form in `tests/captures/pg19_negative_constant.txt` holds two nodes. The rule is narrow on purpose. Rounding to an integer is always lossy; a float target is asked whether the digits survive; **no other type is asked, deliberately** — `'2020-1-1'::date` folds to a `date` that prints `2020-01-01` and a real server normalises it the same way, so keeping a node there would print a constant nobody wrote. A spelling difference is not a loss. **Keeping the node found a live gap**: `2.5::numeric::int` became `22P02 invalid input syntax for type integer` because the per-row cast was a text round trip and `int4in` cannot read `2.5`. The rounding rule moved out of `parse::lower` and in beside every other datum-level cast (`value::numeric_to_integer`), where both callers reach it — the fold for its range error at parse time, the evaluator for the value at run time. `(2147483647::numeric + 1)::int4` closed with it. Five declared divergences deleted across three files, and `negative_constant_deparse.rs` declares nothing at all now. |
| 14 | ~~Four type families this node does not have.~~ | **Paid, all four, and the row's own count was the measurement each time.** `regtype` closed first ([ADR 0077](../adr/0077-regtype-is-an-oid-that-prints-as-a-name.md)); then `"char"` ([ADR 0095](../adr/0095-char-is-one-byte-and-the-quotes-are-part-of-the-name.md)), `oid` ([ADR 0097](../adr/0097-an-oid-is-four-bytes-and-a-derived-one-is-not.md)) and `regproc` ([ADR 0098](../adr/0098-regproc-is-an-oid-that-prints-as-a-function.md)). **The census was the unit each time, and each census found more than the report that started it**: r1's wire sweep filed four `"char"` columns and a query against `pg_attribute` found eighteen; the same query with `atttypid = 26` found 46 `oid` columns and with 24 found 40 `regproc` ones, of which this node has one. Two rules came out that reasoning gets backwards — a `"char"` compares with `text` and shares no common type with it, and a `regproc` reads a bare literal in a comparison as an `oid` while an assignment resolves it as a name — and the second of those found `tests/array_delimiter.rs` writing `WHERE typinput = 'array_in'`, a statement a real server **refuses**, which passed only because the column was `text`. `oid` did not close whole: a column that names a *relation* stays a `bigint`, because a primary key's index and a catalog view take ids from bases at bits 56–62, and the ruling of 2026-09-09 is to keep `u64` relation ids (ADR 0097's Options). |
| 12 | ~~`int4` is measured and not implemented.~~ | **Paid by b4's literal ladder ([ADR 0087](../adr/0087-an-integer-literal-is-the-narrowest-type-that-holds-it.md)), and this row outlived it by a week.** `pg_typeof(1)` is `integer` here now, in both signs and at both boundaries. Found by the §1 reconciliation: the row's own evidence was a section of `tests/integer_literal_type.rs` titled "the `int4` rung is measured and **not** implemented, deliberately" — still there, still saying `bigint`, and **the test below it started at `2147483648`**, so the one rung the section was about was the one rung nothing asserted. Both are fixed: the prose says what happened and the table now carries `1`, `2147483647`, `-2147483648` and `-2147483649`. |
| 13 | ~~No `void` type.~~ | **Paid by `ea5e72e5`, "void is a type, and it is this vocabulary's first pseudo-type" ([ADR 0092](../adr/0092-void-is-a-type-and-it-is-the-first-pseudo-type.md)).** `ColumnType::Void` exists and `tests/advisory_lock.rs` — the row's cited evidence — declares **nothing**: both its `types` and `answers` lists are empty. |
| 32 | ~~**The `QueryCanceled` coin** — `transaction_test.rb`'s *raises QueryCanceled when canceling statement due to user request* read 12, 11 and 10 FAIL of 12 across three builds and was carried as a race.~~ | **Closed.** It was a defect, not a coin: r1's tap of both servers on one seed had identical SQL and binds at **513.3 ms here against 513.2 ms on PG19**, one row against `57014` — the cancel arrived and ended the wait at the same instant, and only the raise was missing. `ddc8b891` (with `6cc1f160` for the readiness check) put `cancel::check` at the top of `wait_for_row`'s loop and in front of the statement restart; **run 103 is its last sighting and run 104's sweep read 0 FAIL of 12**, silent through run 108. Re-measured at this HEAD 2026-09-09: cancellation is checked in **six** places — the row wait, the statement restart, the advisory wait, the scan's page boundary, the cursor's three loops and DDL's two — and there is no gap. The **second half of the pair has no discriminating test and cannot have one**: its own case is the flag set after the last loop-top check and before the re-run, a window of microseconds inside one blocking call that no test can schedule from outside. The counterfactual table is in `crates/esker-sql/tests/cross_session_cancel.rs`'s `a_cancel_survives_the_lock_coming_free_underneath_it`, where someone about to delete one of the two as redundant will read it |
| 17 | **`pg_get_expr` parenthesised only the outermost operator**, and a default's literal showed no coercion inside a call. | **This unit.** The rule was not missing, its caller was: `exec::ddl::deparse` already printed every operator node in its own pair and was reachable from the index key list alone. Routing `index_expression` through it unconditionally, normalising `ALTER TABLE ... ADD COLUMN ... GENERATED` the way `CREATE TABLE` already did, and giving a `DEFAULT` the same treatment at all three statements that store one closed the six declared divergences in `tests/generated_parens.rs` to zero. Three shapes needed the tree and could never have come from parenthesising text — `LIKE` prints as `~~`, `IN` as `= ANY (ARRAY[…])`, `BETWEEN` as two comparisons — and are measured in `tests/corpus/pg19_deparse_parens.txt`, 86 statements, 84 agreeing. |
| 18 | **`yes_or_no`'s length.** `information_schema.columns.is_nullable` is a domain over `character varying(3)` and this node declared `character varying` — a catalog column list carried a type and no length. | this file's own batch: the list carries a length now, and the measurement behind it is that **no `pg_catalog` column has a typmod at all** on a real server, and of `information_schema`'s five domains only `yes_or_no` and `time_stamp` (which no view here serves) carry one |
| 21 | ~~No `ALL` quantifier, so `NOT IN` cannot be printed the way a real server prints it.~~ | **Paid, and the register's own sentence was the thing that was wrong.** The node had the whole three-valued rule and had had it for as long as subqueries compared — `SubqueryKind::Quantified { op, all }` over all six operators, decided by `exec::subquery::quantified`, whose doc already recorded the four rules this unit went and measured. What was missing was the *array* right-hand side for every spelling but `= ANY`. So the fix is one node carrying `op` and `all`, one lowering that prefers the `IN` form for the two spellings that have one, and one delegation — 22 sites, no second copy of the rule. Measuring it first found **two live defects in the `= ANY` that already existed**: `NULL = ANY (ARRAY[]::integer[])` answered NULL where a real server answers `f` (the operand was checked before the array's emptiness, and an empty array settles a quantifier with no comparison at all), and an empty array taken through the `IN` shortcut hit an evaluator written for a grammar that has no empty list. |
| 22 | ~~A deparsed numeric literal never shows its type.~~ | **Paid, and the measurement was the work exactly as the row said.** `pg_get_expr` prints a numeric constant in one of **three** forms and which one is a property of the node: bare for the literal form's own type, `(N)::type` for a cast over one, `'N'::type` for a bare constant whose type is not that form's. The node has no cast node — it folds them — and it does not need one, because which node PostgreSQL had is decidable from the constant: an `int8` that fits `int4` can only have come from a cast, one that does not is what the scanner produces; `smallint`, `real` and `double precision` have no literal syntax at all; a negative constant is a folded unary minus. 46 of 49 statements agree, across all four readers (generated column, `DEFAULT`, index key, `CHECK`). **And building the corpus found a live `22003`**: `i4 > 9223372036854775807` was refused where a real server answers, because a comparison's literal was narrowed to the column's type — fixed by the same sentence `retype`'s `numeric` arm already carried. What remains is #23, #24 and #25, each a different thing and none of them about the form a constant prints in. |
| 23 | ~~A comparison's literal is narrowed to the column's type.~~ | **Paid, and the measurement was two lines of the corpus this row cited.** `exec::query::retype` leaves an integer literal alone against an integer column now, which is the sentence its own `numeric` arm already carried — narrowing is what an *assignment* does, and `22003` is the right answer to an `INSERT` and the wrong one to a `WHERE`. `i2 > 1` prints `(i2 > 1)` on both. **`int4` and `int8` were right by accident**: a literal's datum stays an `i64` whatever width it is *declared* ([ADR 0087](../adr/0087-an-integer-literal-is-the-narrowest-type-that-holds-it.md)), so narrowing to either was a no-op and `smallint` was the only width where the rule was visible — written as the rule rather than as that width, because a fix aimed at `smallint` would have been a fix to the symptom. The wrong refusal this row also named (`i4 > 9223372036854775807` → `22003`) was closed by #22's batch. `tests/numeric_literal_deparse.rs`'s `a_comparisons_literal_keeps_its_own_type` asserts the printed text and the values beside it, so the half that was already right cannot quietly move; the row's declared divergence went from three differing columns to two. |
| 24 | ~~A folded cast loses the node PostgreSQL prints from.~~ | **Paid, and the corpus said the tree is recoverable — the cast node is not needed after all.** The row was sized as "keeping the cast node changes what every reader of a stored expression prints"; `tests/corpus/pg19_negative_constant.txt` measured both signs against every target type in all six readers and found one regular rule instead. A negative constant has **no literal form** — the scanner reads the digits and folds the unary minus in — so PostgreSQL holds a quoted `Const` of the type the *digits* would have had, and that type is a function of the value: `integer` if they fit one, `bigint` if they fit that, `numeric` otherwise. Print the constant under that **natural** type and wrap it in a cast when the wanted type is not the natural one, and every one of the 26 measured forms falls out: `(-1)::bigint` is `('-1'::integer)::bigint`, `(-1)::integer` is `'-1'::integer` with nothing wrapped, `(-1.5)::double precision` is `('-1.5'::numeric)::double precision`, `(-9223372036854775807)::numeric` is `('-9223372036854775807'::bigint)::numeric`. `numeric_constant`'s three enumerated forms became two functions and the enumeration went away. **And the sign turned out not to be part of it**: the same shape appears with a positive constant whose natural type is not `integer`, where the old printer was also wrong and unmeasured — `(9223372036854775807)::numeric` is `('9223372036854775807'::bigint)::numeric`. Two more closures came with it: `exec::query::retype` was normalising `Literal::Typed(Int8)` back to `Literal::Integer`, which is where `c_i8_cast`'s declared width went (`i8 > 1::bigint` printed `(i8 > 1)`), and a bare numeric constant was on `reprinted_by_pg_get_expr`'s `false` list on the reasoning that a literal is stored as a value — true of the value and false of the text, since `parse::fold_column_default` keeps the unfolded expression beside the folded datum. `numeric_literal_deparse.rs` lost two of its three declared divergences to this, including the 32-column row. What is left is #30, and it is a different debt: the fold is *lossy* where the target type cannot hold the value. |
| 25 | ~~The deparser's `CatalogFunc` arm has no arguments.~~ | **Paid, and the corpus was the work exactly as the row said: 29 declared divergences down to 3.** The arm now prints a call's arguments, each under its *parameter's* type so a literal shows the coercion `pg_get_expr` shows — `btrim(t, 'x'::text)`, `split_part(t, ','::text, 1)` where the integer stays bare — from a measured table (`exec::ddl::catalog_parameter_types`) whose `None` keeps the old placeholder, because printing an invented coercion is worse than keeping written text. The four **grammar productions** print upper-cased and coerce to the call's common type, taken from **their own arguments** rather than from the type the parent threaded down — the bug that found that is `CHECK (greatest(b, 1) > 0)`, where the comparison threaded the literal's width and the `bigint` column's never arrived. `NULLIF` is the production that coerces *nothing*, which is the same fact as its result type being the comparison's left input; it did not exist here at all and is `ac8ac0c7`. Two more shapes fell out: a catalog function spelled as an **operator** now takes its operand types per *variant* (`(j \|\| '{}'::jsonb)` where the symbol alone said `text`), and a **cast over a quoted string constant** prints as one node, `'{}'::jsonb` rather than `('{}'::text)::jsonb`, because that is the one node a real server's parser builds. A `CHECK` became the **fifth reader** routed through the deparser (`normalise_checks`), which also closed `tests/check_constraint.rs`'s last `answers` divergence and its one `UNMEASURED` provenance, and `42P17` in a generated column now says PostgreSQL's own sentence — `generation expression is not immutable` — instead of the index one. The three left name a type this node lacks (`regconfig`, `"char"`) or a spelling it does not keep (`substring`'s call form); the sixth reader is #29. |
| 26 | ~~`md5('a')` in a generated column is refused, and the message may be naming the wrong thing.~~ | **Paid, and the distinction the prior decision waited for turned out not to be needed.** `tests/index_expression_volatility.rs` had recorded this mislabelling on purpose, with its reason: lowering carries an unresolved name as `CatalogFunc::UserFunc` so the *executor* can look it up, while `refuse_unless_immutable` runs earlier with a `TableDef` and no catalog, so telling "absent" from "present and volatile" there was a seam change. But the two cases a `UserFunc` can be — a name nobody declared, and a user function the catalog does hold — get the **same** answer from this node, because it cannot evaluate a user function in a stored expression either way. So the guard names the function without knowing which case it is, and gives the message the query path already gives: `0A000 the function md5 is not supported`. The guard still answers `42P17` where volatility *is* the reason (`date_trunc`, which this node has and PostgreSQL calls `STABLE`) — one refusal per cause. `md5` itself is not implemented: the suite sends it **0 times**, censused, so building it would be building what nothing asks for. Still open and named in the test: a user function declared `IMMUTABLE` should eventually be *accepted* by inlining, which a refusal that names the function is easier to change into than one that names the wrong cause. **And the audit this row started was finished a unit later**, because #25's corpus was refused `replace(t, 'a', 'b')` in a generated column for a call whose `provolatile` is `i`: `is_immutable` had grown one family per defect four times over, so `tests/captures/pg19_provolatile_census.txt` is now `pg_proc.provolatile` for **all 79 names** this node resolves, in one query. Twenty-two came back `i` and were refused here — 22 of 22 in both readers, index key and generated column — and all twenty-two now compute the oracle's own values and types. `date_trunc` stays refused and stays documented: it is `i` or `s` by *argument type*, which the volatility question cannot see from where it is asked. |
| 27 | ~~`information_schema.columns` has no `collation_name` column.~~ | **Paid, and it is three columns rather than one.** Measured on 19beta1 over five columns of one table: `collation_catalog`, `collation_schema` and `collation_name` are all `name`, and all three are **NULL together** — non-NULL exactly when the column asked for a collation, which is the same condition `pg_attribute.attcollation <> typcollation` names and the condition `catalog::ColumnDef::collation` already records as `Some`. `collation_schema` is `pg_catalog`, where `C` and `POSIX` live (ADR 0076), and `collation_catalog` is `crate::parse::DATABASE_NAME` — the same string `pg_database.datname` carries, so the two agree whatever a client connected as. Appended at the end of the column list, which is the rule that list already follows: `SELECT *` expands in declared order, so a column inserted anywhere else moves every one after it. |
| 20 | ~~A capture cannot hold a `\|` or a newline in a value, and both failures are silent.~~ | **The reader's half is paid, the writer's is r1's, and the row was three defects rather than two.** (1) **A type name with a comma** — `numeric(10,2)` — was split into two types, so a row declaring three was compared as five and could not agree whatever this node answered; six statements in `tests/numeric.rs` were declared for it. Decidable without any escape, because a comma separating two types is never inside parentheses: `declared_types` splits at parenthesis depth zero, one of the six agreed immediately, and the other five turned out to be **#28** (a cast's result carries no typmod) wearing the format's coat. (2) **A `\|` inside a value** is not decidable and needs an escape, so there is now a contract — a value's backslash, pipe, semicolon, newline, carriage return and tab are written escaped and everything else is kept as written — **opt-in per file** via a `#!escaped` header line. Opt-in is measured rather than cautious: 19 rows across `tests/corpus/` already hold a doubled backslash or a backslash-n inside a value, and un-escaping unconditionally would silently change what they assert, which is this row's own defect. (3) **Silence**, which was the worst of it: a row whose cell count contradicts its declared type count now *fails* with the line, the counts and what to do, instead of reading as a disagreement between two lines of text that look identical. That check found the three live rows — `pg19_to_text.txt`, `pg19_typmod.txt`, `pg19_tsvector.txt` — which were **three declared divergences, two of them `UNMEASURED`**, and all three are gone. Correcting `typmod`'s opened **#31**: its reason said `length` and `\|\|` were operations this node does not have, it has both, and what actually differs is a `character(n)`'s trailing blanks. `tests/corpus_format.rs` asserts the contract through the node and is **the specification the capture tool has to hit**: `sesscap.py` writes no escapes and still drops every output line beginning with a space, so a multi-line value cannot be captured at all. That half is r1's, and the corpus beside the test is the red sample. |
| 28 | ~~An expression carries no typmod.~~ | **Paid, and the row's sizing was wrong the same way #24's was.** It said "medium — giving expressions a typmod is a type-surface change, the same shape as #19, and touches every reader of a `RowDescription`". `OutputColumn` already carried a `typmod` and `plan::Expr::Cast` already held the one it was written with; what was missing was the rule saying which expressions pass one along. Measured over every shape in `tests/corpus/pg19_expression_typmod.txt`: a **cast** names its own, **`NULLIF`** takes its left argument's unless the comparison changed the type, **`COALESCE`/`GREATEST`/`LEAST`** keep one only when every input has the same, **`CASE`** has none, and everything else has none. One function (`exec::query::typmod_of`) and one gate. The second half is one clause in the cast fold, which already kept a `Cast` node "when the value cannot speak for itself" and had to keep one when the value cannot speak for its *modifier* either — a `numeric` datum can say it is numeric and never that it is `numeric(10,2)`. **Nineteen declared divergences deleted across five files**: 6 in `numeric.rs`, 7 in `bit_string.rs`, 3 in `time.rs`, 2 in `interval.rs`, 1 in `nullif.rs`. Keeping the node exposed one live defect it had been hiding — `'101'::bit(3) = '101'::text` answered `f` where a real server says `42883 operator does not exist: bit = text`, because the pair matched no arm in `reconcile` once neither side was a bare literal; a cast now types the other operand exactly as a column does. **Two oracles disagree about `CASE`** and the corpus follows the one a client reads: `CREATE TABLE AS` gives the created column the first branch's modifier, `\gdesc` gives none. |
| 29 | ~~A partial index's predicate is the sixth reader of the deparser and the one not routed through it.~~ | **Paid, and both obstacles the row named had an answer that made something else better.** (a) The reader re-parenthesises a top-level `AND`/`OR` chain's operands itself, so deparsing the chain doubled every pair. The answer is to deparse the **operands** and re-join them — and to do that with the reader's *own* scanner rather than a second copy of "find a top-level AND", so `catalog::parenthesised_operands` was split into `catalog::boolean_chain` (the split) and the wrapping, and the two callers now cannot disagree about where an operand ends. (b) The `ON CONFLICT` arbiter matches a partial index by its predicate **text**, so a deparsed predicate stopped matching the `WHERE "b" IS NOT NULL` `ActiveRecord` writes. The answer is that the arbiter puts what the statement wrote through the **same printer** before comparing — and still tries the raw text, so a predicate the printer has nothing to say about, or one stored before it existed, keeps matching: two chances to agree and none to disagree, which is the safe direction for an inference rule. Closed the `indpred` divergence in *both* `pg19_catalog_func_deparse.txt` and `pg19_negative_constant.txt`. `tests/index_deparse.rs::a_deparsed_predicate_is_still_the_one_on_conflict_names` holds both halves in one test, because the `ON CONFLICT` half also passes when nothing is deparsed at all — and removing the arbiter's new branch turns both it and `tests/on_conflict.rs` back into `42P10`, which is how it is known to be load-bearing. |
| 31 | ~~A `character(n)`'s trailing blanks do not vanish where PostgreSQL makes them vanish.~~ | **Paid, and the rule is sharper than the row's name.** It is not "a `bpchar` read as `text` is right-trimmed" everywhere: **a function reads a `bpchar` argument as `text` exactly when its result *is* `text`**, which is the same fact as #28's typmod surviving, seen from the value side — `upper(c)` is `X` and `greatest(d, d)` comes back `y ` **padded**, because a production that keeps its argument's type never coerced it. Three readers keep the padding and are in the corpus for exactly that reason: `octet_length` reports the storage (`4`) where `length` reports the value (`1`), `concat` takes `"any"` and goes through the output function, and **`LIKE` matches the padded value** — `c LIKE 'x'` is `false` while `c = 'x'` is `true`, which is the sharpest pair in the file. A cast is the other half: leaving `bpchar` strips (`c::varchar`, `c::name`, `c::text` are 1 byte) and arriving at it does not (`c::char(6)` is 6, `c::bpchar` is 4) — measured with `octet_length`, the only reader that can tell. The node already had the node that strips (`Expr::ToText`); what was missing was inserting it where PostgreSQL inserts a coercion. **And `NULLIF` was the fifth reader**: it is a comparison, so it now goes through the same `reconcile` a `=` does, which is where the blank-padding of the other side lives. Closed the last four divergences in `tests/expression_typmod.rs` and the last one in `tests/typmod.rs` — a row that took three units to empty, because #20 had to escape the value before the difference could be read at all. |
| 10 | ~~The mpp differential's time-sensitive assertion.~~ | **Closed 2026-09-09, and not by the lane it was assigned to.** The row asked for *"wait on the artefact, not the clock"*; `37514f28` made the differential wait for the state each query is about and record the ones it could not test, and `fab5405e` made it take that reading **once** — a learner that has caught up can fall behind, so planning the query again after the wait is a second sample of a state that is not monotone. What is left is structurally unable to fail on time: `columnar_plan_within` returns `Some` **only** when the plan says `Engine: columnar`, so the assertion it feeds is true by construction, and the clock can only *defer* a query — with `guarded \|\| on_the_columns > 0` after the loop refusing the degenerate round where everything deferred and the row engine was compared with itself. **Site:** `crates/esker-cli/tests/multi_region_differential.rs` — **and the row said `crates/esker-sql/tests`, which was also stale** · **Owner:** closed by `37514f28` + `fab5405e` · **Sized:** — · **Verified:** read at HEAD, and the evidence this row cited (`gate-h1-c31da348.log`) **predates both commits** |
| 11 | ~~A cursor's continued scan routes to the wrong region.~~ | **Closed before this file was committed**, and the row is kept because it shows what the register is for: the gate's `08006 … key is not in region 0` was not a routing defect but a **bound** — `Router::route` raises it itself when the placement driver has not yet learned of a split, and the wait for that was fixed at five asks (~310 ms), which is fifteen heartbeats on an idle box and less than one effective cycle on a loaded one. `75ab41a9` gives the lookup **the caller's own deadline** instead of a number invented in the router. **Site:** closed by `75ab41a9`, with the test at `ab130d48` · **Owner:** h1 · **Sized:** — · **Verified:** I wrote this row from `$S/gate-h1-ab130d48.log` as "h1 diagnosing, batch held"; both had landed by the time the file was committed, so the row was re-read against `git log` before it was written down. **A register written from a gate log is one landing behind by construction** |
| 16 | ~~`DROP INDEX CONCURRENTLY` is asynchronous.~~ | **裁决：保持，理由 ADR 0083** — ruled by the user on 2026-09-09 and therefore **not an open debt**; the row stays because a reader who meets the asynchronous drop needs to find the decision, and a deleted row sends them to read the code and rediscover the question. The original entry follows, unchanged: **`DROP INDEX CONCURRENTLY` is asynchronous.** `CREATE INDEX CONCURRENTLY` answers when the build is done ([ADR 0083](../adr/0083-a-concurrent-build-answers-when-it-is-built.md)); the removal direction still returns as soon as the job exists. Deliberate: its last step waits the **retention** window rather than the step interval, because what it must outlast is a reader, and a statement that held a client for an hour would be worse than one that returns. **Site:** `crates/esker-sql/src/exec/ddl.rs`'s concurrent drop · **Owner:** g1 · **Sized:** medium, and it needs a decision before it needs code · **Verified:** ADR 0083 records it as a decision, not an oversight; nothing in the suite executes the concurrent drop — `active_schema_test`'s two `remove_index … concurrently` cases assert generated SQL only |
| 15 | ~~`name[]` has no array type.~~ | **Paid, and it was paid by a different unit — nobody moved the row.** `ColumnType::NameArray` landed with [ADR 0084](../adr/0084-name-is-a-stored-type-and-its-tag-is-additive.md) and took six of the seven corpus entries with it; the seventh, `array_agg(enum.enumlabel ORDER BY enum.enumsortorder)` over `pg_enum`, went out with `a62d26e5` — the `regclass`/`oid` unit — because that is what gave the catalog's identifier columns their real types, and the aggregate then derived `name[]` from a `name` argument instead of `text` from a `text` one. Re-measured at this HEAD against 19beta1, all four shapes agree: `array_agg` over a `name` column, over `pg_enum.enumlabel`, and `current_schemas(true)` are all **1003**, and `min(name)` is **25** — the rule that runs the other way, because a real server has no `min(name)` and coerces. All four are pinned in `tests/name_array.rs`, which is why this could be confirmed rather than assumed. **The lesson is the row, not the fix**: a debt closed as a side effect of another unit stays open on the register until someone re-measures it, and this one had been closed for a day. Owner b4. |
| 4 | ~~`SELECT … FOR UPDATE` is not a cluster-wide lock.~~ **Closed 2026-09-09 by [ADR 0088](../adr/0088-a-row-lock-across-nodes.md) (a′), in two units.** The register called this *cross-node deadlock detection*, and the measurement says the debt is larger than its name: a row lock lives in one `esker-sql` process's table, so a second node does not see it, does not wait for it, and cannot deadlock on it. What is missing is not a graph — it is the lock. | **Site**: measured on a real cluster in `crates/esker-sql/tests/cross_node_deadlock.rs`: one node answers `40P01` and kills one of the two, two nodes commit both (rows end `(1,9)` and `(2,9)`) and `pg_locks` is empty · **Size**: medium — the lock becomes a Percolator lock ([ADR 0088](../adr/0088-a-row-lock-across-nodes.md) (a'), accepted 2026-09-09), which needs **no new wire tag**: `Check` (tag 5) and `Kind::Lock` already exist and the acquisition is an ordinary prewrite sent early · **Owner**: **h1**, not PD — the row said "needs a PD-held graph, owner pdha" until 2026-09-09, which sized option (b), the one the ADR refuses · **Verified**: the ADR's measurement, against the single-node control in the same file; the earlier half of #4 — a cross-node cycle is unreachable on the **write** path — is `28ec2474` and `crates/esker-client/tests/prewrite_ordering.rs`, a different mechanism reaching the same conclusion · **Unit 1** (`4eb3767a`): the lock is acquired as a Percolator lock on the row's key when the statement runs — an ordinary `Prewrite` of a `Check` mutation sent early, so **no new wire tag and no new method**; the victim of a cycle is chosen by wound-wait on `start_ts` (the TSO's, invariant 6), so every node breaks the tie the same way with no graph and no round trip. Two nodes given the crossed sequence now answer `40P01` with one victim and one commit, where they both committed before. **Unit 2** (`2d708e46`): the heartbeat sender, because a lock held for the length of a transaction outlives a three-second lease and `TxnKv::Heartbeat` had a handler and no caller — a client now renews at a third of the lease and stops at every ending, `Drop` included. **The five rules the build produced are in the ADR**, each found by a test going red rather than by reading: a transaction upgrades its own lock to the write it took it for; a reader never blocks on a lock-only lock; a reader never wounds; a losing transaction takes its locks with it; and a lock-only record is not a conflict — that last reversing a documented rule, with the argument and both faces pinned. · closed by `4eb3767a` + `2d708e46`, and the split face `a_row_lock_survives_the_split_that_moves_its_row` is what makes reusing the Percolator lock the right half of the choice |
| 9 | ~~**A displaced core and an orphaned handle: a store refuses to serve a region its own peer is leading.**~~ Filed as *`esker-raft` pre-vote term climb*, closed on ten green rounds, **reopened five rounds later**, and then found not to be a term climb at all. **Closed 2026-09-09 by [ADR 0099](../adr/0099-one-core-per-region-per-store.md).** The lesson stays first: those ten rounds each finished in 2.5–13 s and the failing one took 181 s — they were not in the same state at all, so "it did not appear" was never the same claim as "it cannot". | **Site**: `crates/esker-store/tests/promotion.rs`'s `a_learner_on_a_fresh_store_becomes_a_voter_under_load`, which prints the counters on the way out as well as down — that print is what caught it · **Size**: closed · **Owner**: h1 · **Verified**: **one line, and no single core can produce it**: store 1 at `term=7 is_leader=false believes_leader=None` beside `raft_role=Leader`, `campaigns_pre=1108` `campaigns_real=91`; stores 2 and 3 at **term 263**, followers, both believing 54 leads. `become_leader` sets `role` and `leader` two lines apart and `publish_leader` runs after every message, so the pair cannot disagree — and 91 adopted terms cannot end at 7. Read as **two cores under one region id** every field fits: 2/3 are right, 54 leads at 263, and the handle the request path holds belongs to a peer whose core was displaced. Membership agreed on all three, so ADR 0085's guard was never what was at work · **the fix is [ADR 0099](../adr/0099-one-core-per-region-per-store.md)**: registering claims a place instead of overwriting it, `RaftPeer::start` returns a reservation the caller commits after the region map takes it, retiring names the registration rather than the region, and ADR 0094's campaign runs after the commit. Red first across all five refusal branches in `every_refusal_leaves_nothing_driving_the_region_it_refused` . **Measured after the fix, in the coordinator's window of 2026-09-09: 32 rounds of that test (12 quiet, 20 under a six-thread arm at container load 4-20), all green, and the end-of-run sweep found no region whose handle was answered by another peer's core — 0 orphans in 32.** What those rounds do **not** show is the state region #9 was in: every one of them ran with `campaigns_pre` at 1 or 2 against that round's 1108, and the longest took 72 s against 181 s. They are evidence of no regression, not of a reproduction that came back clean |
| 40 | ~~**A pre-vote livelock when ticks arrive in batches**: three voters all `PreCandidate` at once, no leader, and the term climbing.~~ **Closed 2026-09-09 as in-process-harness capacity**, by the measurement the row's own verdict rule was written for. | **Nine rounds on four real `esker server` processes**, three quiet and six under load, every one of them valid under the discard rule — 605 to 696 regions, and `load1` measured inside the container at 0.99–3.02 (control), 4.65–7.60 (six spinners) and **9.96–17.54** (twelve). **Not one wait over thirty seconds, and not one `not the leader` wait at all.** The load reached the cluster — the same test takes 18 s quiet and 61 s at twelve spinners — so the zero is a zero and not an arm that missed. By the rule written before the numbers, that closes this as the harness's capacity rather than a system debt, and **ADR 0104 is not needed**. What stays is [ADR 0101](../adr/0101-a-batch-of-ticks-never-carries-a-whole-election.md)'s rule and `a_batch_of_ticks_flattens_the_randomised_timeout`, which pins the mechanism in the core where it is real. **the mechanism, and it is a property of the core rather than of the load**: an election timeout is randomised per peer, and [ADR 0081](../adr/0081-the-tick-driver-catches-up.md) has a starved driver deliver the ticks it slept through *as a batch*. A batch as wide as the randomisation's own span covers every draw in it, so every peer crosses its threshold inside one batch — **the randomisation is flattened**, all three campaign together, the vote splits, and the next pre-vote round does it again. The stall's own snapshot is in #34's row. The red test is `esker-raft`'s to write and is deterministic: the core is a pure state machine, so a batch is just `tick()` called n times before `step` — written, and it reads *one at a time: one leader / batched: none, term 6, pre 6 real 6*. **[ADR 0101](../adr/0101-a-batch-of-ticks-never-carries-a-whole-election.md) removed that flattening and did NOT close this row**, and the run that says so is the one that was supposed to verify it. Twice, at 5 ms and 4 threads: `how_the_leaderless_window_moves_with_the_drivers` saw **0 sightings** at 332 and 300 regions, while `how_long_a_writer_waits_for_a_region_between_leaders` — the **same** harness, threshold, tick, thread count and row count, in the next process — stalled **3 of 3** both times, at 321 and 235 regions, 30–32 s each. Same parameters, opposite answers, twice: **the sweep is a detector that does not fire, so its zero is not evidence and it cannot verify anything.** What the stall looks like now: always **region 1**, the term climbing between sightings (57 → 105 → 167, then 111 → 199 → 270 — fifty to ninety terms per two hundred and fifty rows), every handle still answered by its own core, and **once a store was a `Follower` with `leader Some(10)`** while the other two pre-campaigned — so a leader is sometimes elected and does not survive, which is churn rather than a group that cannot elect. Next: find why the sweep never fires before trusting either number — the item-by-item comparison is in the sweep's own doc comment, and the one experiment that decides it is to run the two in the other order. **And one candidate mechanism is now tested and refused**: a batch that puts a follower's ticks in front of the leader's heartbeat *does* depose a live leader — `a_batch_that_puts_ticks_before_a_heartbeat_deposes_a_live_leader` shows it at the pre-0101 width of 21 — but **not at ADR 0101's capped width of 9**, where every drive delivers a heartbeat that resets `election_elapsed` before nine ticks can reach a draw of ten. So the field's *elected but does not survive* is not this, and 0101 turns out to guard the leader as well as the election. **And the flip settled the sweep question: the answer follows the position, not the test.** Run first, the sibling saw **0**; run third, the same test with the same parameters stalled **3 of 3**, twice. The sweep fired in none of **six** opportunities across three positions, so it is not a detector and neither of its numbers says anything about the tick or the thread count. One confounder is left and is named rather than hidden: the third position in the flipped round held the 20 ms / 8 configuration, which may be immune on its own. The experiment that separates them — the same test three times in a row — **produced no data**: both runs hit a 400 s timeout with nothing printed, where the same test finished in 214 s an hour earlier on the same box at the same load. That doubling is **unattributed**, and three hypotheses have been refused with evidence rather than argument. **A gate**: the chain log says the box was idle 17:30:24–17:59:25. **Another lane**: re-checked with the *dates* on, not just the clock times — the three host compiles that looked like the answer were from the day the disk filled, and nobody ran anything heavy in the window. **The deadline swallowing a compile**: my own log says `Finished \`test\` profile … in 0.09s` on its first line, so both runs spent the whole 400 s inside the test. What is left is the box itself, and one candidate is now testable for free: at the time this lane's build volume held **331 GB** on a disk **86% full**, and it now holds **1 GB** — so the next control round of the same test is itself the experiment. Back near 214 s is evidence; still past 400 s says the disk was not it either. Three more candidates were ruled out on the way and each is worth keeping: leftover processes and data directories are impossible by construction (`in.sh` is `docker run --rm` and every store's data directory is `tempfile::tempdir()` **inside** the container, while the mounts are the repo, the build target and the cargo registry), and memory pressure is ruled out by measurement (swap 0 of 0, no swapins or swapouts on a 128 GB box — a small `free` figure on macOS is caching, not pressure). **And the first attribution was wrong**: I read a gate off a twelve-second sample extrapolated backwards, and the chain log says the box was idle then. **The separation this row asked for is finished** — see #40 in §2: nine rounds on four real store processes, six of them loaded to `load1` 17.5, not one wait over thirty seconds. This row's own question is answered and only its closure is left to rule on. A container's `/proc` is the VM's and cannot see a host compile at all, which is why the sampler now counts host `rustc`/`cargo`/`clippy-driver`/`rustdoc` and the `Gate` says in its own doc what it is blind to. **The design that closes this row is written and not yet run**: `docs/plans/split-region.md` §11, *How debts-v1.1.md #40 gets closed* — the real-binary measurement against a control and two in-container load levels, with the discard rule (a round that did not pass 300 regions or raise the load does not count as a zero), the criteria and the verdict rule all fixed before the numbers, and a 25-minute budget **And the two 400 s timeouts have a likely answer at last**: the same in-process test, after this lane's build volume went from **331 GB to 1 GB** on a disk that was 86% full, ran in **110 s** — half its best previous time, and under load still decaying from the twelve-spinner arm. One run, so it is support and not proof, but the direction is the disk and not anything that accumulates. |
| 34 | ~~**A region can be without a leader for more than thirty seconds at 192–325 regions, and it is repeatedly region 1.**~~ **Closed 2026-09-09 by the same nine rounds that closed #40**, which is what this row asked for: the system and the harness, separated. | **Four real `esker server` processes, three quiet rounds and six loaded** — 605 to 696 regions, `load1` measured inside the container at up to **17.54**, and **not one wait over thirty seconds**, nor one `not the leader` wait at all. The load reached the cluster (the same test takes 18 s quiet and 61 s at twelve spinners), so the zero is a zero. The in-process stall this row recorded was the harness's capacity, and the mechanism inside it is pinned in the core by `a_batch_of_ticks_flattens_the_randomised_timeout` with [ADR 0101](../adr/0101-a-batch-of-ticks-never-carries-a-whole-election.md)'s rule. **What the row leaves behind is worth more than its closure**: region 1 is the catalog's host and on the path of every statement — now `docs/DESIGN.md` §15's open question and [ADR 0102](../adr/0102-the-catalogs-read-path.md) — and the evidence that the earlier timeouts were the **disk** (the same test ran 110 s after this lane's build volume went from 331 GB to 1 GB, half its best previous time). `crates/esker-sql/tests/routing_differential.rs`'s `how_long_a_writer_waits_for_a_region_between_leaders` (a measurement) and `a_splitting_bulk_load_never_fails_for_want_of_attempts` (a reproducer), both `#[ignore]`d unknown, and the first question is not the fix: **the system and the harness are not separated** — four stores in one process driving three hundred Raft groups at a 5 ms tick is its own explanation and nothing rules it out yet h1 **four runs, fourteen sightings, none recovered**: 30.0–33.4 s each at 192, 252, 276 and 325 regions, where thirty seconds is where the instrument gives up — so every number is a floor. Seven of the fourteen name **region 1**, the original whole-key-space region. Found by [ADR 0100](../adr/0100-a-region-between-leaders-waits-on-the-callers-deadline.md)'s measurement, which was asked whether a retry budget should give way to the caller's deadline and answered that the budget is not what stands in the way. For contrast, [ADR 0094](../adr/0094-a-split-childs-leader-is-the-parents-leader.md) measured a split child's leaderless window at a **median of 10 ms** across 132 splits on the same harness at ~130 regions — so what this row is really about is how that window behaves as the region count grows. **And why it is region 1 seven times out of fourteen is settled, statically**: the catalog lives under `'m'` and every SQL key under `'x'`, so the catalog sorts below all data and stays in the **left-most region for the life of the cluster**, while `Catalog::view` reads `catalog_version` from the store **once per transaction**. Region 1 is not special to Raft — it is the catalog's host, on the path of every statement, so one window there is seen by every statement in it and those seven sightings are as likely to be **one** window as seven. The design observation is its own open question, now in `docs/DESIGN.md` §15: a single region read by every statement on every node. **SEPARATED 2026-09-09, and it is the harness.** Two controls, both against the same load: (a) **four real `esker server` processes** reached **663 regions** — twice where the in-process harness stalls — with **0 sightings**; (b) the in-process harness at **5 ms tick / 4 driver threads** stalled 3 times at 325 regions and cleared none inside 20 s, while at **20 ms / 8 threads** it reached **452 regions** with **0 sightings**. A higher region count with more headroom is fine and a lower one without it is not, so **what produces this is Raft per driver thread per tick, not the region count**. What it is *not* is [ADR 0099](../adr/0099-one-core-per-region-per-store.md)'s state: the sweep taken **during** a stall shows every store's handle answered by its own core. What it **is**, on all three stores at once: `PreCandidate`, `leader None`, and the term climbing — 209, 210, then 617 on the same region — which is a **pre-vote livelock** and is the shape #9 was originally named for |

## 3. Where the evidence is

**`$S` and `$R` are outside this repository and `$S` does not outlive its session**, which is why
every citation above also names a *hash* or a file **name**: a gate log is
`gate-<lane>-<hash>.log` wherever it is kept, and the hash is in this repository's history for
ever. `$S` is the coordinator's scratchpad for the night of 2026-09-08/09 and `$R` is
`esker-rails-harness/results`; the index of both is `$S/v11-evidence.txt`, and the durable half of
each row is the sentence, not the path. The three that matter most for a reader
coming to this cold:

* **the landing chain**, thirteen batches with a `ci-tree` gate log each (`$S/gate-<lane>-<hash>.log`,
  carrying the `FMT`/`CLIPPY`/`DOC`/`DENY`/`TEST` return codes and the nextest summary);
* **the scoreboard**: run 104 10,048/10,134 = 99.15 %, run 105 10,067 = 99.34 %, run 106 10,071 =
  99.38 %, each with its routing table under `$R`;
* **the gates that went red and passed on a re-run of the same tree** — four of them, and every
  one was load-sensitive rather than a defect in the batch: they are rows 9, 10 and 11 above. Row
  11 turned out to be a *bound* that only looks load-sensitive — five asks is fifteen heartbeats
  idle and less than one loaded — which is the difference worth learning from those four: "passes
  on a re-run" is a symptom, and it has both an innocent cause and a guilty one.

## 4. ADR numbering, re-taken

Checked mechanically over `docs/adr/*.md` at this commit:

| Check | Result |
|---|---|
| files matching `NNNN-*.md` | **84** |
| distinct numbers | **84** |
| range | **0001 – 0084** |
| gaps in 1..84 | **none** |
| numbers used by two files | **none** |

Twenty-one ADRs since v1's sixty-three. Numbering has collided twice in this project's history —
ADR numbers and, separately, the catalog record version — and both were caught at merge rather
than at write time, so the standing rule holds: **claim the number out loud in the report the
moment it is taken.**

## 5. How to re-take this

```sh
# ADR contiguity and duplicates
ls docs/adr/[0-9]*.md | sed 's|.*/||' | cut -c1-4 | sort | uniq -d
ls docs/adr/[0-9]*.md | wc -l

# every declared divergence, by family — the rule-4 scan's own list
rg -n 'types: &\[' -A 40 crates/esker-sql/tests/*.rs | rg '^\s*"SELECT'

# what is still UNMEASURED, which is the number `provenance_budget` holds
rg -c 'UNMEASURED' crates/esker-sql/tests/*.rs | sort -t: -k2 -rn | head
```
