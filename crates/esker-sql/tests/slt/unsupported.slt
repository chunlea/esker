# Contract C2: a statement PostgreSQL accepts and this node does not run comes back `0A000` naming
# the construct. Never a syntax error, never a wrong answer, never a silent no-op.
#
# This file is the deliverable §3 calls "out, as executed features -- and therefore in as C2
# responses". Every line here parses.

statement ok
CREATE TABLE t (id int8 PRIMARY KEY, a int8, b text)

# A table alias runs now (phase 9 unit 5, tests/alias.rs). What it brought within reach and is
# still not a feature names itself: a **column** alias list renames the table's columns, and
# ignoring it would answer a query about `c` with a column called `id`.
statement error (0A000)
SELECT c FROM t AS x (c)

# `GROUP BY`, `HAVING`, `DISTINCT` and the five aggregates run now (phase 9 unit 1,
# tests/slt/aggregate.slt). What sits next to them still does not, and each still names itself.
statement error (0A000)
SELECT a FROM t GROUP BY ROLLUP (a)

statement error (0A000)
SELECT a FROM t GROUP BY GROUPING SETS ((a), ())

statement error (0A000)
SELECT DISTINCT ON (a) a FROM t

statement error (0A000)
WITH RECURSIVE x AS (SELECT 1) SELECT * FROM x

statement error (0A000)
SELECT count(*) OVER () FROM t

statement error (0A000)
SELECT count(*) FILTER (WHERE a > 0) FROM t


# `ON CONFLICT` left this file with `upsert_all`. What is still refused is the arbiter's `WHERE`,
# which `sqlparser` 0.62.0 cannot read at all -- a C1 gap rather than a clause declined here.
statement error (0A000)
INSERT INTO t VALUES (1, 2, 'x') ON CONFLICT (a) DO UPDATE SET b = excluded.b WHERE t.b > 0

statement error (0A000)
INSERT INTO t SELECT * FROM t

# **`CREATE TEMPORARY TABLE` left this file** with ADR 0054: a temp table is an ordinary relation
# in a schema that belongs to one session, and the search path does the rest. What is still refused
# beside it is `CREATE UNLOGGED` — and the two together are a syntax error on a real server too.

# **`CREATE TABLE ... AS` left this file** (`tests/create_table_as.rs`). It is a statement of its
# own now, typed from the query's plan on the path `CREATE MATERIALIZED VIEW` already used, and its
# tag is `SELECT <n>` rather than a `CREATE` tag — measured. `WITH NO DATA` is the half it does not
# honour, and it is not refused here because the corpus never sends it.

# **Nothing about a column `DEFAULT` is refused here any more.** Two lines stood in this place: one
# refused `DEFAULT random()` at CREATE TABLE, which the DEFAULT-is-an-expression unit took, and one
# refused `DEFAULT 1 + 1`, which arithmetic on `main` took. What a default still refuses is
# PostgreSQL's own three shapes, and those are not "unsupported" — a real server refuses them too.

# Tier 1 is complete since the typmod unit and `numeric` since the numeric one, so the three
# lines that stood here are gone. No column type in ADR 0033's first two tiers is refused now.

# **`public.` is no longer a refusal.** `public` is this node's only schema — it is what
# `current_schema()` answers — so a qualifier naming it names the same relation the bare name does,
# and `relation_name` drops it exactly as it already dropped `pg_catalog.`. A qualifier naming any
# *other* schema is still `0A000`: `s.t` and `t` would be different tables on a real server, and
# answering about the second would be a wrong answer rather than a missing feature.
statement ok
CREATE TABLE public.q (id int8 PRIMARY KEY)

statement ok
DROP TABLE public.q

# A schema that is not there is `3F000`, PostgreSQL's own class for it — the *schema* is what is
# missing, and it is a different answer from a missing relation (`42P01`). It was `0A000` until
# `CREATE SCHEMA` existed.
statement error (3F000)
CREATE TABLE other_schema.q (id int8 PRIMARY KEY)


# `ALTER TABLE ADD COLUMN` runs for a nullable column and for one with a **constant** default
# (see alter_table.slt). What is left is what would need a row rewritten, and each is named rather
# than lumped together.
#
# `NOT NULL` with no default was here as a `0A000`, on the argument that there is "nothing to pad
# the rows already stored with". That is true only when there *are* rows: an empty table takes the
# column, which is what PostgreSQL does and what every test in the suite sends. A populated one is
# `23502` — a wrong value, not a missing feature — so it belongs in `tests/add_column_not_null.rs`
# rather than in this file.

# `ADD COLUMN ... DEFAULT random()` and `... DEFAULT (1+1)` were both here, refused for the table
# rewrite PostgreSQL does and this ALTER did not. It does it now: every row already stored is read,
# given its own value from the expression, and written back (`tests/add_column_volatile_default.rs`).
# Measured on 19beta1, both of these have `atthasmissing = f` — a rewrite, not a missing value —
# so agreeing meant rewriting rather than padding. What is still refused is bare `NOT NULL` with no
# default, which has no value to pad *or* to compute, and `serial` on a table with rows.

statement error (0A000)
ALTER TABLE t ADD COLUMN c int8 UNIQUE

# `ADD COLUMN ... PRIMARY KEY` was here. It declares a key now, on a table that has none
# (`tests/add_column_primary_key.rs`), and so does `ADD CONSTRAINT ... PRIMARY KEY`. What is still
# refused is a `serial` added to a table with rows, which PostgreSQL fills from the sequence.

# `ALTER COLUMN a TYPE text` converts now (`tests/alter_column_type.rs`), and so does a `USING`
# that computes rather than converts — `USING string_to_array(a, ',')` left this file when the
# per-row evaluator arrived (`tests/alter_column_type_using.rs`). Nothing of `ALTER COLUMN … TYPE`
# is refused by name any more; what a `USING` expression cannot do is reported per row.

statement error (0A000)
ALTER TABLE ONLY t ADD COLUMN c int8

# `CREATE VIEW` left this file with `tests/view.rs`, and the materialized form left with
# `tests/matview.rs` — it is a table that carries its definition (ADR 0064), so what used to be
# refused here is now measured against the oracle there.

statement error (0A000)
PREPARE p AS SELECT 1

statement error (0A000)
COPY t FROM STDIN

statement error (0A000)
GRANT SELECT ON t TO alice

# And the statement after any of them still runs: a refusal is not a poisoned session.
query I
SELECT 1
----
1
