# **Every cast between two of the probe list's types, asked two ways** — PostgreSQL 19beta1 and
# this node, 2026-09-11, `127.0.0.1:55432`, one `BEGIN … ROLLBACK` with `citext`, `hstore` and
# `ltree` created inside it (this oracle has none of the three installed, and the transaction
# leaves none behind).
#
# ## The instrument, which is the thing this file got wrong twice
#
# A row is `source <TAB> value <TAB> target <TAB> mode <TAB> node <TAB> pg`. **The value is in the
# row** and the **mode** says how it was asked, because the previous two takes carried neither and
# each cost a probe run to find out that rows were graded against a value the file did not hold:
#
#     literal   SELECT ('<value>'::<source>::<target>) IS NOT NULL
#     bind      PREPARE … AS SELECT ($1::<source>::<target>) IS NOT NULL; EXECUTE …('<value>')
#               — on this node, `Parse`/`Describe`/`Bind`/`Execute` with **no declared type**,
#                 which is the shape a driver sends
#
# Every ordered pair of the probe list's **101** type spellings, self-pairs excluded: **10,100
# pairs × 2 modes = 20,200 probes per server.** Values are each source's own from
# `esker-coord/wire-v3-probes.txt`.
#
# ## What 19beta1 answers, and **the two modes never differ there**
#
#     ok        1,051      !42846  8,422      !22P02      530
#     !22007       40      !22023       14      !42601       10
#     !42P01       10      !42883       10      !42704       10
#     !22P03        2      !2202E        1
#
# **Not one of the 10,100 pairs answers differently as a literal and as a bound parameter on a real
# server.** That is what makes the mode column worth carrying: every difference below is this
# node's.
#
# ## What this node answers, per mode — **and the two modes agree everywhere**
#
#                                    literal     bind
#     agree                          10,095   10,095
#     node refuses, PG answers            4        4
#     node answers, PG refuses            0        0
#     both refuse, different code         1        1
#
# **Mode-dependent pairs: 0**, down from 31 when the second mode was first asked.
#
# **Four pairs are left and all four are a ruling** (five until the row below closed). `regclass -> integer|oid` and their arrays
# refuse because this node's relation ids are 64 bits wide and an `oid` is four bytes — ADR 0097's
# own consequence, declared by the user on 2026-09-11 and pinned in
# `tests/user_decided_divergences.rs` in **both** modes.
#
#     text[] -> hstore   node `42846 cannot cast type text[] to hstore`   19beta1 `2202E`
#
# was the one open row in the whole matrix, and is **closed, 2026-09-11**. A real server *attempts*
# it — `hstore(text[])` is a real function and the probe's `{x}` is one element, so the refusal is
# `2202E array must have even number of elements`, about the **array** and not about the pair. This
# node had no `text[] -> hstore` conversion at all. It needed the constructor, not a sqlstate: with
# it, `'{a,1}'` becomes `"a"=>"1"` and an odd list keeps PostgreSQL's own `2202E`. Both halves are
# built — the `pg_cast` row `text[] -> hstore  e  f` and `value::hstore::from_text_array` — and the
# rest of the rule is measured in `pg19_text_array_to_hstore.txt`: a NULL value is a value, a NULL
# key is `22004`, the first of a repeated key wins, and the order is hstore's canonical one. So the
# matrix's residue is now **four rows and all four are a ruling**.
#
# ## The finding this instrument was built for, and what it found
#
# On 19beta1 no pair of the 10,100 answers differently as a literal and as a bound parameter. When
# this node was first asked both ways, **31 did** — and `ActiveRecord` binds by default, so the
# mode that matters was the one nothing had ever swept. All 31 closed, in four families and **two
# directions**: `oidvector -> T[]` and `regclass[] -> …` were the bind path (11 each),
# `line -> <geometry>` was the **literal** path blaming a value for a pair with no cast (6, wire v3
# family F12), and `regclass -> oid` was one declared boundary refusing with two sentences (3).
#
# A sweep of either mode alone would have shown a clean column or a wrong one, with nothing to
# compare it against but an oracle that agrees with itself.
#
# ## The sqlstate family, closed 2026-09-11
#
# Ten rows per mode, and nine of them were two mechanisms already named here:
#
#      7  `jsonb[] -> <number>[]`   node `22P02`, PG `22023`. The scalar asks what **kind** of
#                                   document it has before handing digits to a number's input
#                                   function; an element never reached that rule, because the arm
#                                   holding it asked the *operand's* declared type and an element's
#                                   operand is the array. Moved into `cast_one_value` beside the
#                                   `"char"` rule, which is the same seam one type over.
#      2  `citext -> regclass|regtype`  node `42804 an oid is an integer, not Citext(…)`, PG
#                                   `42P01`/`42704`. A `citext` **is** a string, and it is the one
#                                   string type in this vocabulary with its own `Datum` variant, so
#                                   the arms that resolve a name had to be told. A real server
#                                   tries the lookup; so does this one now.
#
# ## The pairs this node gets wrong, whole (10 rows over both modes)
#
regclass	pg_class	integer	bind	!22003	ok
regclass	pg_class	integer	literal	!22003	ok
regclass	pg_class	oid	bind	!22003	ok
regclass	pg_class	oid	literal	!22003	ok
regclass[]	{"pg_class"}	integer[]	bind	!22003	ok
regclass[]	{"pg_class"}	integer[]	literal	!22003	ok
regclass[]	{"pg_class"}	oid[]	bind	!22003	ok
regclass[]	{"pg_class"}	oid[]	literal	!22003	ok
text[]	{"x"}	hstore	bind	!2202E	!2202E
text[]	{"x"}	hstore	literal	!2202E	!2202E
#
# ## Earlier takes, kept because how the numbers moved is the useful half of a record
#
# **Everything below this line was measured with the old instrument** — one mode, and the value
# not in the row — so its tallies are not comparable with the ones above and its four `RE-SWEPT`
# sections are dated snapshots rather than current state. What survives re-reading is the
# *mechanisms*: each section names one, and every one of them is closed.
#
# ## What this node answered, before and after the literal-array guard
#
#                                  before    after
#     node answers, PG refuses        391       95     <- ADR 0031's worst class
#     node refuses, PG answers        121      121
#     both refuse, different code   3,501      293
#                                   -----     -----
#                                   4,013       509
#
# **3,504 rows closed and none regressed**, by one check. `parse::lower`'s array arm reads a
# literal array's *text* and hands it to the target's `array_in` — which is right for a pair that
# has a cast and wrong for a pair that does not — and it returns before the general `casts_to`
# guard below it. So `'{x}'::"char"[]::bytea[]` answered `{"\x78"}` while the **scalar**
# `'x'::"char"::bytea` and the same pair through a **column** both refused correctly.
#
# The sqlstate half is the same fix: a pair with no cast used to reach `array_in` and fail as the
# element reader's `22P02` — "invalid input syntax" for a conversion that does not exist — where a
# real server says `42846` before reading anything.
#
# ## What was left after the literal-array guard, and it was not one thing
#
#     95   node answers, PG refuses    — mostly `text ->` something the reader accepts too freely
#     121  node refuses, PG answers    — **not** 121 missing `pg_cast` rows; see the breakdown below
#     293  both refuse, different code
#
# ## RE-SWEPT 2026-09-10 after the element-wise array cast: 509 -> 353
#
# The node side was re-taken over the same 1,425 pairs the grading above names (the 509 divergent
# and the 916 that agreed), with each source's own probe value from `esker-coord/wire-v3-probes.txt`:
#
#                                     before   now agrees   still
#     node refuses, PG answers          121        **71**      50
#     node answers, PG refuses           95        **79**      16     <- ADR 0031's worst class
#     both refuse, different code       293          6        287
#                                      ----       -----     -----
#                                       509        156        353
#
# **One mechanism, and the "121 missing `pg_cast` rows" reading was wrong.** Grouped by the
# sqlstate the node gave, the 121 were: `22001` 34 (`<T>[] -> character[]`), `22P02` 34 (pairs
# whose scalar cast **exists**), `42846` 40 (the genuinely missing rows — F9's forty), `22026` 5,
# `42704` 4 (`<integer>[] -> regtype[]`), `22003` 4 (`regclass -> integer`, whose ids are 64-bit
# here). Only the 40 were about a missing row.
#
# The other families were **one defect**: an array's cast read each element's *text* through the
# target element's input function instead of applying the **element cast**. The scalar pair and
# the array pair disagreed, and all three routes in — a literal, an `ARRAY[…]` constructor and a
# **column** — shared it:
#
#     1.5::float8::integer            2       '{1.5,2.5}'::float8[]::integer[]      22P02
#     '2020-01-01'::date::character   2       '{2020-01-01}'::date[]::character[]   22001
#
# **It was not the literal-array arm**, which is where the previous unit's note pointed: a column
# of `float8[]` reached the same `22P02`. Three changes, each symmetric with something that
# already existed — `exec::cursor` casts element by element through the same function the scalar
# fallback uses, `parse::lower` stops folding an array cast that **has** a source type (no source
# type is the input function, a source type is a cast — `pg_cast`'s own boundary), and
# `value::truncate_to_typmod` gains the container arm `fit_to_typmod` already had.
#
# **Why the second class moved at all**: 79 of the 95 rows where this node *answered* and 19beta1
# refuses were the same mechanism seen from the other side — a pair with no element cast used to
# be read by `array_in`, which accepts what the element reader accepts.
#
# ## What the 357 are now, and they are four separate items plus one family
#
# **54 node refuses, PG answers** — by the sqlstate this node gives:
#
#     42846  40   `hstore -> json|jsonb` and `int2vector`/`oidvector` -> the arrays.
#                 **The genuinely missing `pg_cast` rows**, which is family F9's forty. A unit.
#     42704   4   `<integer>[] -> regtype[]`. `pg_cast` has **no** `text -> regtype` row either, so
#                 both spellings are I/O conversions and `regtypein` reads all digits as an oid —
#                 `'23'::regtype` is `integer` there and `42704 type "23" does not exist` here.
#                 The scalar has it too; four rows here and two spellings. A unit.
#     22003   4   `regclass -> integer|oid` and their arrays. This node's relation ids are 64-bit
#                 (ADR 0097) and a real server's oids are four bytes, so the overflow is a
#                 consequence of the id and not a cast defect. **Probably a declared divergence**
#                 rather than a fix, and it should be decided rather than carried.
#     22P02   5   `"char"[] -> integer[]` is the byte (`'1'::"char"::integer` is 49) and an element
#                 never reaches the arm that knows, because that arm asks the *operand's* declared
#                 type and an element's is the array's — one parameter away. The other four are
#                 `regproc -> oid` and three of the unreproducible rows below.
#     42601   1   `text -> hstore`, the fourth unreproducible row.
#
# **11 node answers, PG refuses** — the other direction of one small table: seven are
# `<something> -> oidvector`, where `casts_to` says yes and `pg_cast` has no row, and the rest are
# `regclass <-> regtype` and `bit -> oid`. A `casts_to` unit, and the smallest of the four.
#
# **292 both refuse with different codes** is a family of its own and not a cast defect: what is
# wrong there is the *sqlstate*, which is what a client branches on. It is the largest group left
# and the least urgent by ADR 0031.
#
# ## RE-SWEPT 2026-09-10 (evening) after the `42846 / ok` residue: 357 -> 31
#
# The node side re-asked over the divergence list below, **as literals** —
# `SELECT ('<value>'::<source>::<target>) IS NOT NULL`, each source's own probe value from
# `esker-coord/wire-v3-probes.txt`. The sweep this file records bound the value as a **parameter**
# instead; the instrument is stated here because the two are different roads through this node (the
# fold versus the evaluator) and this section's numbers are the literal one's. Read over the **352**
# rows the list holds after the five bad ones were corrected:
#
#     now agree                      **326**
#     node refuses, PG answers         **6**   <- was 54; then 2 were fixed and 4 declared, so **0**
#     node answers, PG refuses         **0**   <- was 11
#     both refuse, different code     **20**   <- was 292
#
# **The `42846 / ok` forty are closed, and they were four mechanisms rather than one missing
# table.** (1) `refused_cast`'s `stringy` was three names where PostgreSQL asks a *category*, so
# `interval|money|time|uuid -> name|citext` were refused — eight rows, and `typcategory` already
# said `S` for all five one module over. (2) `time -> interval` is a real `pg_cast` row the same
# table hid, and the arm beside it already carried the finding in words. (3) `hstore -> json|jsonb`
# are two rows the extension carries, added to `CASTS` with the conversion in
# `value::convert_without_text` — one arm for the fold, the column and the array element. (4) The
# twenty-five vector rows are **one rule**: a vector casts to every array its element casts to, so
# `casts_to` asks `casts_to(int2, T)` where it had two tuples, and three readers of the operand's
# text had to stop treating `1 2` as an array literal (`lower_array_cast`'s typmod'd elements,
# `lower_regclass_array`, and the evaluator's `regclass[]` arm, which sat above the vector one).
#
# **The four `42704` rows went with them**: `regtypein` reads all digits as an oid — `'23'::regtype`
# is `integer` — which is `regclassin`'s rule one type over, now `value::oid_spelled` for all three
# roads a `regtype` arrives by. It closed `<integer>[] -> regtype[]`, the vectors' last two rows,
# and a declared divergence in `tests/regtype.rs` whose reason had stated the rule.
#
# **Nothing is left in this column.** The five bad rows are corrected rather than counted, in the
# section above; the two defects are closed; and the last four are **a declared divergence**,
# ruled by the user on 2026-09-11 on [ADR 0097]'s own text:
#
#     22003  4   `regclass -> integer|oid` and their arrays. Relation ids here are `u64` and an
#                `oid` is four bytes, which that ADR decided and wrote into its consequences in
#                the same words this node answers in — "answering a truncated oid would be a wrong
#                value under a right type". Pinned in `tests/user_decided_divergences.rs`, with
#                the two spellings that are **not** a narrowing (`::bigint`, `::text`) beside them.
#
# [ADR 0097]: ../../../../docs/adr/0097-an-oid-is-four-bytes-and-a-derived-one-is-not.md
#
# `"char"[] -> integer[]` **was** the "one parameter away" row and is closed: `cast_one_value`
# takes the operand's declared type now, so an element's cast knows what the element was, and the
# scalar arm that used to hold the rule is that call. `'{x,r}'::"char"[]::integer[]` is
# `{120,114}`, the literal, the constructor and the column all agreeing.
#
# `regproc -> oid` was the fold: the lowering's `oid` arm reads the operand's *characters* with
# `oid`'s input function and a `regproc` prints as `int4in`. The node is kept for that source now,
# and the conversion happens where the value is a `Datum::RegProc` and can say so — which is why
# the same cast over a column and over an array element answered all along.
#
# The `both refuse, different code` group is 20 and is the sqlstate family, not a cast defect:
# seven are `22P02` where a real server says `22023`, six `22P02` where it says `42846`.
#
# **The node column of the list below is stale for about 270 rows**, and this is the note that says
# so rather than a silent rewrite. Those rows record a node sqlstate from before the literal-array
# guard, the element-wise array cast and the `casts_to` gate above `lower_cast`'s match — the list
# says 292 rows refuse with a different code and asking again finds 20. **A re-take needs an
# instrument decision first**, which is why it is not done here: the pg column was taken with the
# value **bound**, and a node column taken with a **literal** is a different road through this node.
# Whoever re-takes it should ask both ways, keep the value in the row, and say which column is
# which — the difference between the two is itself one of this file's findings.
#
# ## RE-TAKEN 2026-09-10 (evening): the five rows the list's own value does not reproduce
#
# `text -> money`, `text -> hstore`, `text -> int2vector`, `text -> oidvector` were graded `ok` and
# `text -> lquery` was graded `!42601`. Asked again with the value the probe list actually carries
# for `text` (`x`), one `BEGIN … ROLLBACK` with `hstore` created inside it, 19beta1 answers:
#
#     'x'::text::money        !22P02 invalid input syntax for type money: "x"
#     'x'::text::hstore       !42601 syntax error in hstore: unexpected end of string
#     'x'::text::int2vector   !22P02 invalid input syntax for type smallint: "x"
#     'x'::text::oidvector    !22P02 invalid input syntax for type oid: "x"
#     'x'::text::lquery       x
#
# **This node answers each of the five identically**, message included, so all five were agreements
# recorded as divergences: they had been taken with a value this file does not carry. The `ok` list
# below and the divergence list at the end are corrected; the tally at the head moves with them —
# `ok` 1,037 -> **1,034**, `!22P02` 522 -> **525**, `!42601` 11 -> 11 (hstore in, lquery out) — and
# the divergence list is **352 rows**, not 357.
#
# **The sweep should carry its values.** This is the third time these rows have been read as
# divergences, and each time the cost was a probe run to find out they are not. A row here is
# `(source, target, node, pg)` and it should be `(source, value, target, node, pg)`.
#
#
# ## Which casts exist — the ok rows, one line per source
#
#     <source> <TAB> <every target it casts to>   (the oracle, and the same in both modes)
#
"char"	character character varying citext integer name text
"char"[]	character character varying character varying[] character[] citext citext[] integer[] name name[] text text[]
bigint	bit bytea character character varying citext double precision integer money name numeric oid real regclass regproc regtype smallint text
bigint[]	bit[] bytea[] character character varying character varying[] character[] citext citext[] double precision[] integer[] money[] name name[] numeric[] oid[] real[] regclass[] regproc[] regtype[] smallint[] text text[]
bit	bigint character character varying citext integer name text
bit[]	bigint[] character character varying character varying[] character[] citext citext[] integer[] name name[] text text[] varbit[]
boolean	character character varying citext integer name text
boolean[]	character character varying character varying[] character[] citext citext[] integer[] name name[] text text[]
box	character character varying circle citext lseg name point polygon text
box[]	character character varying character varying[] character[] circle[] citext citext[] lseg[] name name[] point[] polygon[] text text[]
bytea	bigint character character varying citext integer name smallint text
bytea[]	bigint[] character character varying character varying[] character[] citext citext[] integer[] name name[] smallint[] text text[]
character	"char" bit bytea character varying citext lquery ltree name text tsquery tsvector void xml
character varying	"char" bit bytea character citext lquery ltree name text tsquery tsvector void xml
character varying[]	"char"[] bit[] bytea[] character character varying character[] citext citext[] ltree[] name name[] text text[] tsquery[] tsvector[] varbit[] xml[]
character[]	"char"[] bit[] bytea[] character character varying character varying[] citext citext[] ltree[] name name[] text text[] tsquery[] tsvector[] varbit[] xml[]
cidr	character character varying citext inet name text
cidr[]	character character varying character varying[] character[] citext citext[] inet[] name name[] text text[]
circle	box character character varying citext name point polygon text
circle[]	box[] character character varying character varying[] character[] citext citext[] name name[] point[] polygon[] text text[]
citext	"char" bit bytea character character varying lquery ltree name text tsquery tsvector void xml
citext[]	"char"[] bit[] bytea[] character character varying character varying[] character[] citext ltree[] name name[] text text[] tsquery[] tsvector[] varbit[] xml[]
date	character character varying citext name text timestamp timestamp with time zone
date[]	character character varying character varying[] character[] citext citext[] name name[] text text[] timestamp with time zone[]
daterange	character character varying citext name text
daterange[]	character character varying character varying[] character[] citext citext[] name name[] text text[]
double precision	bigint character character varying citext integer name numeric real smallint text
double precision[]	bigint[] character character varying character varying[] character[] citext citext[] integer[] name name[] numeric[] real[] smallint[] text text[]
hstore	character character varying citext json jsonb name text
hstore[]	character character varying character varying[] character[] citext citext[] json[] jsonb[] name name[] text text[]
inet	character character varying cidr citext name text
inet[]	character character varying character varying[] character[] cidr[] citext citext[] name name[] text text[]
int2vector	bigint[] bytea[] character character varying character varying[] character[] citext citext[] double precision[] integer[] name name[] numeric[] oid[] real[] regclass[] regproc[] regtype[] smallint[] text text[]
int4range	character character varying citext name text
int4range[]	character character varying character varying[] character[] citext citext[] name name[] text text[]
int8range	character character varying citext name text
int8range[]	character character varying character varying[] character[] citext citext[] name name[] text text[]
integer	"char" bigint bit boolean bytea character character varying citext double precision money name numeric oid real regclass regproc regtype smallint text
integer[]	"char"[] bigint[] bit[] boolean[] bytea[] character character varying character varying[] character[] citext citext[] double precision[] money[] name name[] numeric[] oid[] real[] regclass[] regproc[] regtype[] smallint[] text text[]
interval	character character varying citext name text time
interval[]	character character varying character varying[] character[] citext citext[] name name[] text text[]
json	character character varying citext jsonb name text
json[]	character character varying character varying[] character[] citext citext[] jsonb[] name name[] text text[]
jsonb	character character varying citext json name text
jsonb[]	character character varying character varying[] character[] citext citext[] json[] name name[] text text[]
line	character character varying citext name text
line[]	character character varying character varying[] character[] citext citext[] name name[] text text[]
lquery	character character varying citext name text
lseg	character character varying citext name point text
lseg[]	character character varying character varying[] character[] citext citext[] name name[] point[] text text[]
ltree	character character varying citext name text
ltree[]	character character varying character varying[] character[] citext citext[] name name[] text text[]
macaddr	character character varying citext name text
macaddr[]	character character varying character varying[] character[] citext citext[] name name[] text text[]
money	character character varying citext name numeric text
money[]	character character varying character varying[] character[] citext citext[] name name[] numeric[] text text[]
name	"char" bit bytea character character varying citext lquery ltree text tsquery tsvector void xml
name[]	"char"[] bit[] bytea[] character character varying character varying[] character[] citext citext[] ltree[] name text text[] tsquery[] tsvector[] varbit[] xml[]
numeric	bigint character character varying citext double precision integer money name real smallint text
numeric[]	bigint[] character character varying character varying[] character[] citext citext[] double precision[] integer[] money[] name name[] real[] smallint[] text text[]
numrange	character character varying citext name text
numrange[]	character character varying character varying[] character[] citext citext[] name name[] text text[]
oid	bigint character character varying citext integer name regclass regproc regtype text
oid[]	bigint[] character character varying character varying[] character[] citext citext[] integer[] name name[] regclass[] regproc[] regtype[] text text[]
oidvector	bigint[] character character varying character varying[] character[] citext citext[] integer[] name name[] oid[] regclass[] regproc[] regtype[] text text[]
path	character character varying citext name polygon text
path[]	character character varying character varying[] character[] citext citext[] name name[] polygon[] text text[]
point	box character character varying citext name text
point[]	box[] character character varying character varying[] character[] citext citext[] name name[] text text[]
polygon	box character character varying circle citext name path point text
polygon[]	box[] character character varying character varying[] character[] circle[] citext citext[] name name[] path[] point[] text text[]
real	bigint character character varying citext double precision integer name numeric smallint text
real[]	bigint[] character character varying character varying[] character[] citext citext[] double precision[] integer[] name name[] numeric[] smallint[] text text[]
regclass	bigint character character varying citext integer name oid text
regclass[]	bigint[] character character varying character varying[] character[] citext citext[] integer[] name name[] oid[] text text[]
regproc	bigint character character varying citext integer name oid text
regproc[]	bigint[] character character varying character varying[] character[] citext citext[] integer[] name name[] oid[] text text[]
regtype	bigint character character varying citext integer name oid text
regtype[]	bigint[] character character varying character varying[] character[] citext citext[] integer[] name name[] oid[] text text[]
smallint	bigint bytea character character varying citext double precision integer name numeric oid real regclass regproc regtype text
smallint[]	bigint[] bytea[] character character varying character varying[] character[] citext citext[] double precision[] integer[] name name[] numeric[] oid[] real[] regclass[] regproc[] regtype[] text text[]
text	"char" bit bytea character character varying citext lquery ltree name tsquery tsvector void xml
text[]	"char"[] bit[] bytea[] character character varying character varying[] character[] citext citext[] ltree[] name name[] text tsquery[] tsvector[] varbit[] xml[]
time	character character varying citext interval name text
timestamp	character character varying citext date name text time timestamp with time zone
timestamp with time zone	character character varying citext date name text time timestamp
timestamp with time zone[]	character character varying character varying[] character[] citext citext[] date[] name name[] text text[]
tsquery	character character varying citext name text
tsquery[]	character character varying character varying[] character[] citext citext[] name name[] text text[]
tsrange	character character varying citext name text
tsrange[]	character character varying character varying[] character[] citext citext[] name name[] text text[]
tstzrange	character character varying citext name text
tstzrange[]	character character varying character varying[] character[] citext citext[] name name[] text text[]
tsvector	character character varying citext name text
tsvector[]	character character varying character varying[] character[] citext citext[] name name[] text text[]
uuid	bytea character character varying citext name text
uuid[]	bytea[] character character varying character varying[] character[] citext citext[] name name[] text text[]
varbit[]	bit[] character character varying character varying[] character[] citext citext[] name name[] text text[]
void	character character varying citext name text
xml	character character varying citext name text
xml[]	character character varying character varying[] character[] citext citext[] name name[] text text[]
